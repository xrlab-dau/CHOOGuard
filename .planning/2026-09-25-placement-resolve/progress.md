# Progress Log

## Session: 2026-09-25

### Current Status
- **Phase:** 완료. 브랜치 `feature/tutorial-playable`.
- PlayMode **75 통과 · 0 실패 · 1 건너뜀**.
- 설 자리 없는 유닛 **3/12 → 0/12**, 최소 간격 **2.00m → 6.40m**, 보행 영역 커버 **100%**.
- G9 는 여전히 `NOT_VERIFIED`.

### Actions Taken
- `StationWalkableSolver` 신규. 씬 콜라이더를 0.5m 격자로 훑어 바닥·사람 여유를 확인하고,
  시작점에서 걸어 닿는 영역만 남긴 뒤, 수평 광선으로 벽면을 찾아 후보를 만든다.
  후보마다 **설 자리·판독면 시야·그 자리까지 걸어갈 수 있는지**를 검사한다.
  보행거리 20m 그리디 커버에 유닛 간 최소 간격 3.2m 를 강제했다.
- **씬을 고치지 않는다.** JSON 과 붙여넣을 코드만 낸다 — 배치 변경은 사람이 검토한 뒤에 할 일이다.
- 생성기의 `PlacementsV3` 를 `PlacementsV4` 로 교체. v3 는 비교를 위해 남겼다.
- 튜토리얼 대상은 개방도(6m 안 보행 칸) 264 로 12개 중 가장 트인 자리.

### 도구를 만들며 내가 다섯 번 틀렸다
상세는 `findings.md`. 요약하면 격자 폭주 / 벽 안쪽에서 쏜 광선 / 짧은 탐색 거리 /
**지붕을 바닥으로 오인** / **설 수 있음과 갈 수 있음을 구분 안 함**.

네 번째가 가장 위험했다 — 커버 100%, 간격 5.82m 로 결과가 멀쩡해 보였고 수치만 봤으면
소화기 12개를 지붕(y 13~16m)에 올렸을 것이다. `y` 값을 눈으로 확인해서 잡았다.

여섯 번째는 기하 검사를 전부 통과한 자리가 **어두운 벽감에 묻혀 실루엣만** 보인 것이다.
프레임을 보지 않았으면 통과였다. 개방도 지표를 그래서 넣었다.

### 주장하지 않는 것
법정 적합. NFTC 101 의 보행거리 항목만 계산에 썼고 구획 면적·소화 능력단위 산정은 하지 않았다.
개수 12개는 v3 과 같게 유지했다 — 보행거리만으로는 5개면 덮이지만, 내가 계산하지 않은 요건을
내 판단으로 완화하지 않는다.

### Next
사람이 12개를 다 돌아보는 검수 · 소리 · 본편 승객 NPC.
이 작업에서 만든 보행 격자는 승객 길찾기에 그대로 쓸 수 있다.
