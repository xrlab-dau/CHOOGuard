# Executable progression slice
1. Define validated closed JSON graph and fixed public view DTOs.
2. Reuse RuleExpression/RuleFacts for actual per-operation high-level decisions; retain movement, lease, and own-ACK primitives.
3. Record deduplicated decision/observation traces with actual run/request/op provenance; bind editor component.
4. Source-only bounded checks and freeze graph report. Root owns Unity/native proof; view owner owns UI.
