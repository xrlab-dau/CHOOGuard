# Building publication lane
Complete: three reviewed FBXs copied to Assets; shared height resolver and per-feature generation receipt wired; replacement-aware CityBuilder exclusion; exact existing-scene integration; URP material binding, metre bounds validation and terrain foundation generation. Root owns Unity compile/apply and native verification.
