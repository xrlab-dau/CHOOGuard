# Findings

## Actual-method correction

- OpenTTD's public `network_command.cpp` implements separate waiting/execution queues, server-assigned execution frames, queue synchronization for a joining client, ordered execution, and per-tick distribution limits. `vehicle.cpp` implements depot service that resets service dates, breakdown counts, and reliability, guarded by service-eligibility checks. These are verified public-code facts; using the behavior here is a proposed clean-room design, not a code port.
- OpenRCT2 documents game actions as query-then-execute and its public tick loop updates peeps, rides, park/research/news before processing the action queue. Public peep and ride code preserve entity state across ordinary operation, queues/rides, breakdowns, inspections, and repair. These are verified public-code facts; mapping them to CHOOGuard facility/team recovery is an inference.
- The local FDS 6.11.1 reference hall contains six 1.5 m horizontal `.sf` fields and three `.s3d` Smoke3D files. The current worker samples `.sf` fields pointwise for agents; `MvpPhysicsResult` exposes only scalar summaries, so no actual field grid reaches Unity.
- The current orange marker is a 2 x 2 x 2 m cube created in `MvpStationBuilder.cs`, positioned/toggled by `MvpStationView.cs` and `MvpTrainingDirector.cs`. A truthful first visualization port is a time-stamped extinction-coefficient slice texture at z=1.5 m, not a volumetric-smoke claim.
- FDS supplies formal verification and validation guides, but the local hall receipt explicitly says uncalibrated with no mesh/time convergence or Busan validation. JuPedSim 1.4.2 is pinned and reproduced locally; this does not establish site/model validation for the current scenario.
- Current public modeling inputs are OSM geometry and four Mapzen Terrarium DEM tiles with receipts. There is no precise IFC/BIM or calibrated overlapping photogrammetry set, so the usable method is a reproducible Blender procedural source-layer pipeline with source/design-assumption manifests and metre/axis checks.

- Current canonical direction targets a Korean 20-30 minute ordinary-operation to incident to recovery session, with JWE3 as the primary game/UI reference.
- Existing proposals already establish direct team orders, persistent citizen identity, finite resources, notifications, and contextual actions as proposals rather than implemented facts.
- Live floor evidence says all three slabs had zero thickness and UV0; a separate owner is already applying a 700 mm finish/depth repair, so this lane must not duplicate that edit.
- Current captures: the city/world view is detailed enough for district context but the HUD occupies large fixed left, bottom, and right blocks. Station and floor views show repetitive pole-like fixtures, broad pale uninterrupted surfaces, few doorway/portal silhouettes, weak functional zoning, and sparse/abstract furniture. Floor 2 is especially dominated by a large central plain rectangle; floor 3 has an L-shell with a small chair cluster and kiosk row.
- Existing local JWE3 reference captures show a compact selected-team panel, task/condition rows, thin world-space route/action lines, target rings, small status badges, and a lower-corner objective strip while the playable world remains broadly visible. This is visible-reference evidence only; it does not establish private AI or simulation internals.
- Existing reusable art includes response crew/vehicles/trains/façade, benches, bus stop, planters, lamps, two tree types, three landmarks, and concrete/metal surface textures. Production improvements should recombine these assets and add light procedural shells/markers before commissioning broad new asset sets.
- The selected order is: compact context command UI, actionable notification rail, floor-functional silhouettes after the 700 mm repair, then district-specific composition of the existing 17 FBXs.
- Official-source facts and production inferences are separated in every proposal. The unofficial Busan station convenience map is marked near_match_only and is not treated as survey or rights evidence.
