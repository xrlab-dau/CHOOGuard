# Progress Log

## Session: 2026-09-25

### Current Status
- **Phase:** 완료. 브랜치 `feature/tutorial-playable`.
- PlayMode **75 통과 · 0 실패 · 1 건너뜀**(캡처 하네스, 배치모드에서 의도적으로 건너뜀).
- 캡처 15 장으로 첫 화면부터 점검표 부착까지 확인. 기술자 인계 구간도 처음으로 탔다.
- G9 는 여전히 `NOT_VERIFIED`.

### 고친 것

**점검표가 그려지지 않던 문제.** 전날 PASS 로 적었는데 사실이 아니었다. 원인 두 겹 —
Quad 의 보이는 면이 `-Z` 쪽인데 앵커에 회전이 없어 본체 안쪽을 향했고, 소화기 본체의 종이 라벨
재질을 재사용해 UV 가 2k 텍스처 전체를 끌어와 24px 흑백 체커로 보였다. 앵커에 `Euler(0,180,0)`,
전용 재질 `tutorial_inspection_tag.mat`. 판정은 단언이 아니라 **부착 전/후 픽셀 차이 0 → 578 개**로 했다.

**시작 지점.** 생성기가 유닛 12 개는 배치하면서 플레이어만 두고 갔다. `PlacePlayerBefore` 가
바닥·몸 낌·시야 세 조건을 방향(0·±25·±50·180°)과 거리(2.2~0.9m)로 실제 재서 자리를 찾는다.
못 찾으면 자리가 있는 다른 유닛으로 물러서고, 그래도 없으면 `LogError` 로 알리고 옮기지 않는다.
`(26.00,7.05,-47.00)` → `(25.07,7.00,-53.90)`, 지정 대상까지 1.40m.

**하네스가 배치모드 회귀를 멈춰 세운 문제.** `[Explicit]` 이 지켜지지 않아 배치모드 실행에 섞였고
`WaitForEndOfFrame` 이 반환되지 않아 40 분간 멈췄다(프로세스 강제 종료함). `Application.isBatchMode`
에서 `Assert.Ignore` 하도록 코드로 막았다. 조용히 넘기지 않고 결과에 '건너뜀' 으로 남는다.

### 내가 틀린 것

**"시작 지점이 첫 소화기에서 44.41m" 는 오보고였다.** 하네스가 `session.Units[0]`
(`BSN-CONC-FE-001`)을 기준으로 쟀고, 튜토리얼이 물린 대상은 `session.Target`
(`BSN-CONC-FE-010`)으로 **6.32m** 였다. 44.41m 도 11m 도 실측값이지만 재는 대상이 틀렸다.

같은 형태로 세 번 틀렸다 — 대상 선택, 거리 측정, 인계 조회(`Units[0].Dispatch` 를 봐서
"요구가 발행되지 않았다" 로 실패). 전부 `Units[0]` 을 대상으로 착각한 것이다.
영수증에 `corrections` 로 남기고 설계 문서·루브릭의 서술도 고쳤다. 지우지 않는다.

### 남은 제품 문제 3 건

| 문제 | 실측 |
|---|---|
| 설비가 벽감에 끼어 정면 접근이 막힘 | `BSN-CONC-FE-010` 정면 0° 전 거리 실패(2.0~2.5m 시야 가림, 1.2~1.6m 몸이 낌). 좌 50° 1.4m 에서야 자리 |
| 점검표를 보면 상호작용이 끊김 | 본체 아래(로컬 y=.26) 조준 시 레이캐스트가 아무것도 맞히지 못함 |
| 기술자 인계에 볼 것이 없음 | 59 초 대기, 완료 프레임이 요청 직후와 사실상 동일 |

첫째는 좌표를 NFTC 101 보행거리로만 산정하고 **구조물 간섭을 검토한 적이 없기** 때문이다.

### Next
사람 플레이 검수 · 기술자에게 몸 주기 · 소리 · 설비 좌표의 구조물 간섭 재검토.

## 2026-09-25 (이어서) — 남은 3 건 처리

### 결과
- **점검표 확인 시 상호작용 끊김** — 고침. 콜라이더 복원. 가림은 높이로 분리(.26 / .55 / .88).
- **기술자 인계 시각 표현** — 대역 투입(`TechnicianPresence`). 이동·작업·입회 대기가 보인다.
  캡슐이므로 관찰 가능성 통과로 보지 않는다.
- **설비 3 개 설 자리 없음** — **고치지 않음.** 좌표를 옮기면 NFTC 101 산정 근거가 깨진다.
  생성기 상시 검증으로 매번 경고하게 했다.

### 측정
12 개 중 설 자리 없는 유닛 3 개 — `BSN-CONC-FE-001`, `-002`, `-008`.
`-002` 는 여섯 방향 × 다섯 거리 서른 자리 전부 "바닥 없음"(아래 8m 까지 바닥 없음) = 건물 바닥 밖.

### 내가 만든 버그도 잡았다
`PlacePlayerBefore` 가 플레이어 자신의 콜라이더를 제외하지 않아, 재생성할 때마다 자기 위로 올라
시작 높이가 7.00 → 8.55 로 뛰었다. 측정 중 플레이어 콜라이더를 끄도록 고쳤고 두 번 실행해 동일 확인.

### 패턴 (하루에 네 번)
값을 계산 하나로 고정 → 실제 구조물과 충돌. 판독면 방향 / 중력 착지 / 시작 지점 정면 / 기술자 좌우.
전부 "재고 나서 고른다" 로 바꿔야 풀렸다. **설 자리 없는 유닛 3 개도 같은 원인**이고 그건 배치 데이터 쪽이다.

### 회귀
PlayMode **75 통과 · 0 실패 · 1 건너뜀**(캡처 하네스, 배치모드 차단 작동).

### Next
사람 플레이 검수 · 소리 · 설비 좌표를 실제 메시에 대해 다시 푸는 작업.
