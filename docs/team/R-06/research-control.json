{
  "schemaVersion": 2,
  "artifactId": "artifact:46:R-06-control-spec:candidate",
  "issueNumber": 46,
  "workId": "R-06",
  "phase": "candidate",
  "kind": "research-harness-control-spec",
  "recordType": "candidate_control_spec_not_enforcement",
  "draftStatus": "candidate_draft",
  "canonicalWriteAllowed": false,
  "noAcceptanceClaim": "Candidate control spec. It is exercised by scripts/team/R-06/egress_controls.py against synthetic inputs with an in-process fake transport. It grants no egress, approves no publication and does not change tools/research/research.py.",
  "noNetwork": "No model provider, search API or other network destination was contacted while producing or testing this spec.",
  "inputs": {
    "artifact:21:M1-05-record-schema:candidate": {
      "path": "docs/team/M1-05/record-schema.json",
      "schemaVersion": 1,
      "sha256": "451fcc0bb395e7c75d196bec43c91bac7228dff62dff9864196d1bdedd474427",
      "pipeline": {
        "path": "scripts/team/M1-05/record_pipeline.py",
        "sha256": "8b0f94c0aef552d9787524abe558816435293d5cd487eb5592d2e397faba554b"
      },
      "consumed": [
        "$defs.raw event kinds, outcomes and classifications",
        "$defs.approval required fields and decision values",
        "record_pipeline.build and verify (imported, not copied)"
      ],
      "rule": "Consumed by version, path and digest. A different digest stops the run before any case executes."
    },
    "artifact:16:M0-03-execution-profile:candidate": {
      "path": "docs/team/M0-03/execution-profile.json",
      "fileSha256": "787fbcf6fc8dc7a63e31b54835b87a32eaa2a68d2c71e1b2ab9b4e4b36eaf5fc",
      "policyHash": "220e57ccb131c9ec2b9297277de53cebab93ee7f00a31f401e4373c7ae2ea050",
      "consumed": [
        "policy.dataClasses",
        "policy.policyHash",
        "egress.destinations",
        "egress.classMatrix",
        "roles[*].roleId",
        "roles[*].modelScope"
      ],
      "note": "The accept-phase profile is consumed at accept; this candidate reads the candidate on develop."
    },
    "decisionRecord": {
      "path": "docs/adr/0004-research-harness-and-egress-policy.md",
      "sha256": "3443c68a294fd8988f290ea93078d6b0e9d483ee14d479ed8018fd0043a12718",
      "consumed": [
        "topic manifest id input only",
        "default-deny network including model-generated queries and redirects",
        "isolated output, sanitiser, publication approval as separate steps",
        "excerpt length, HTTPS, approved source domain, no credential query",
        "new files only under docs/research/"
      ]
    },
    "currentHarness": {
      "path": "tools/research/research.py",
      "sha256": "fb3bb218fea6188dac60433805f5580e7e0fa9b4ffc2c3554dabad00b7b4d710",
      "use": "read only; gaps listed under /harnessGaps"
    },
    "notBound": [
      "artifact:18:M1-02-policy-record:accept (not accepted yet; PR #198 open)"
    ]
  },
  "stages": {
    "inputClassification": {
      "field": "inputClassification",
      "values": [
        "admitted:<dataClass>",
        "refused:<code>"
      ],
      "request": {
        "allowedFields": [
          "topicId",
          "destinationId",
          "actorRole",
          "queries"
        ],
        "rule": "Only a registered topic id is accepted. Free text, file, path, upload, model or provider fields in a request are refused."
      },
      "topicManifestEntry": {
        "requiredFields": [
          "topicId",
          "topic",
          "dataClass",
          "allowedSourceDomains",
          "provider",
          "modelId",
          "policyHash"
        ],
        "rules": [
          "dataClass is one of the profile policy.dataClasses",
          "policyHash is the immutable SHA-256 string equal to profile.policy.policyHash.value; never alias the mutable profile metadata object",
          "topicId matches the request and manifest metadata fields have explicit string types; source domains are a nonempty list of ASCII DNS names",
          "the topic text is fixed in the manifest and never taken from the request; transmitted metadata is still checked for the declared sensitive patterns"
        ]
      },
      "refusalCodes": [
        "field_not_allowed:<field>",
        "topic_not_registered",
        "unknown_data_class",
        "policy_hash_mismatch",
        "manifest_entry_incomplete",
        "request_type_invalid",
        "field_type_invalid:<known-field>",
        "field_not_allowed:unknown",
        "manifest_entry_invalid",
        "manifest_contains_<classification>",
        "policy_hash_invalid"
      ]
    },
    "egressDestination": {
      "field": "egressDecision",
      "values": [
        "allowed:<destinationId>",
        "refused:<code>",
        "not_evaluated"
      ],
      "rules": [
        "default deny: the destination id must be listed in the profile egress.destinations",
        "the actor role must be in the destination usedBy list",
        "the topic data class must be in the destination allowedDataClasses and not DENY in egress.classMatrix for model destinations",
        "an egress approval with the destination approvalOwner, decision approved and a non-blank decisionRef must be recorded for the topic and destination",
        "the manifest modelId must match the actor role modelScope allow list and must not match its deny list",
        "every query, including a model-generated one, is checked for secret, personal and absolute path content before it is sent",
        "a redirect or follow-up hop to any host other than the granted destination host is refused and not followed",
        "modelEgress must equal allowed-with-APR-EGRESS-recorded; unknown, malformed or absent dispositions are denied",
        "all matching egress decisions must be approved with non-blank string references; contradictory records require explicit resolution, not guessed chronological precedence",
        "known sensitive patterns are scanned in literal and bounded percent-decoded query and transmitted metadata views",
        "the granted destination needs an explicit valid DNS mapping before the fake transport may count a send",
        "redirects require HTTPS, no userinfo, exact host and default port; successful earlier hops remain counted after a later refusal",
        "only final status 200 is a successful synthetic response; other statuses or malformed response wrappers are refused"
      ],
      "refusalCodes": [
        "destination_not_in_profile",
        "role_not_permitted_for_destination",
        "data_class_above_destination_ceiling",
        "data_class_denied_for_model_egress",
        "egress_approval_not_recorded",
        "model_outside_role_scope",
        "query_contains_secret",
        "query_contains_personal",
        "query_contains_absolute_path",
        "query_contains_restricted_marker",
        "redirect_to_other_host",
        "transport_destination_unconfigured",
        "transport_response_invalid",
        "transport_http_error",
        "redirect_invalid",
        "query_type_invalid",
        "text_invalid_unicode",
        "text_invalid_encoding",
        "text_encoding_depth_exceeded"
      ],
      "researchDestinationsOutsideProfile": [
        {
          "destinationId": "DEST-SEARCH-API",
          "source": "docs/adr/0004 decision 1 (search capability of the candidate harness)",
          "state": "blocked",
          "reason": "not listed in the profile egress.destinations; no approval owner recorded"
        }
      ]
    },
    "outputSanitization": {
      "field": "outputSanitization",
      "rulesetVersion": "R-06/sanitize-v2",
      "values": [
        "clean",
        "refused:<code>",
        "not_evaluated"
      ],
      "outputShape": {
        "claims": [
          {
            "id": "string",
            "claim": "string",
            "evidence": [
              {
                "url": "string",
                "title": "string",
                "excerpt": "string"
              }
            ]
          }
        ]
      },
      "rules": [
        "unknown or non-string fields make the output undecidable and it is refused",
        "every string is checked for secret tokens, e-mail addresses, absolute or UNC paths and restricted-source path markers",
        "URLs must be https, carry no userinfo, carry no credential-like query keys and resolve to a host inside the topic allowedSourceDomains",
        "each excerpt is at most 500 characters",
        "text that asks the harness to ignore instructions, call a tool or run a command is data; it is never dispatched and the output is refused as undecidable",
        "reject ambiguous raw URL syntax, control/whitespace characters, invalid DNS authorities and malformed ports before parsing normalization",
        "scan netloc, path, query names/values and fragments, including bounded percent-decoded views; do not limit checks to selected credential query keys",
        "HTML-escape untrusted Markdown table text before table delimiter escaping",
        "unknown request field names are not reflected into public refusal codes; only fixed declared names or unknown appear",
        "raw records use exclusive creation and POSIX owner-only mode 0600; failed Unicode output stays privately JSON-escaped"
      ],
      "refusalCodes": [
        "output_schema_invalid",
        "output_contains_secret",
        "output_contains_personal",
        "output_contains_absolute_path",
        "output_contains_restricted_marker",
        "url_not_https",
        "url_userinfo",
        "url_credential_query",
        "url_domain_not_allowed",
        "excerpt_too_long",
        "embedded_instruction",
        "url_invalid",
        "url_encoding_depth_exceeded",
        "text_invalid_unicode"
      ],
      "recording": "Every run, including refused ones, is written as an M1-05 raw record outside the repository and passed through record_pipeline.build, which keeps raw bytes private and emits a public_candidate plus public_manifest with approval pending."
    },
    "publicApproval": {
      "field": "publicApproval",
      "values": [
        "unknown",
        "pending",
        "approved",
        "rejected",
        "not_evaluated"
      ],
      "record": {
        "owner": "APR-PUBLISH",
        "approvalRecord": "M1-05 $defs.approval (state approval_record, manifestSha256, decision, decisionRef)"
      },
      "rules": [
        "sanitisation clean is required but is not approval",
        "an egress approval (APR-EGRESS) is not a publication approval",
        "decision approved with a non-blank string decisionRef is required; schemaVersion must be an integer, not boolean",
        "approvalRecord.manifestSha256 must equal the sha256 of the exact r06-publication-manifest bytes (record.publicationManifestSha256), NOT record.manifestSha256 of the text-omitting M1-05 audit projection",
        "the publication manifest binds exact JSON/Markdown filenames, byte counts, SHA-256 values, sanitizer and auditManifestSha256; rederive the candidate immediately before writes and reject substitutions",
        "the slug matches ^[a-z0-9][a-z0-9-]{0,59}$; the ASCII YYYY-MM-DD is a real calendar date",
        "output entries including dangling symlinks must not already exist; exclusive creation never overwrites",
        "record the actual publication outcome through M1-05, privately binding the audit and publication digests and successful file hashes",
        "on I/O failure report refusal and preserve any approved partial output; no crash-atomic multi-file transaction or hostile-writer isolation is claimed"
      ],
      "refusalCodes": [
        "approval_record_invalid",
        "approval_owner_not_publication",
        "publication_not_approved:<decision>",
        "approval_bound_to_other_manifest",
        "sanitization_not_passed",
        "invalid_slug",
        "target_exists_no_overwrite",
        "publication_candidate_missing",
        "publication_candidate_changed",
        "audit_manifest_digest_mismatch",
        "invalid_date",
        "publication_root_unavailable",
        "publication_io_failed",
        "target_outside_research_root"
      ],
      "approvalBinding": {
        "manifestKind": "r06-publication-manifest",
        "manifestSchemaVersion": 1,
        "digestField": "publicationManifestSha256"
      }
    }
  },
  "separation": "inputClassification, egressDecision, outputSanitization and publicApproval are four distinct fields of every run result. A value in one never implies a value in another: an allowed egress can end with a refused output, a clean output can stay pending publication, and an approved publication cannot be produced without its own record bound to the manifest digest.",
  "profileDenyCaseRefs": {
    "DC-03": "content above PUBLIC_SYNTHETIC to a model provider without recorded APR-EGRESS",
    "DC-06": "model outside modelScope",
    "DC-10": "destination not on the profile list"
  },
  "harnessGaps": [
    "tools/research/research.py accepts a free topic string and a --model override; this spec admits only a registered topic id and takes the model from the manifest.",
    "its keyword filter exits with a message and is not a classification, allowlist or sanitiser result.",
    "it writes docs/research/<date>-<slug>.json and .md directly with overwrite; this spec writes only after approval and never overwrites.",
    "the harness is not wired to this control code; wiring is a separate change to tools/research/ outside this work order's write scope."
  ],
  "limits": [
    "Transport is an in-process fake. No DNS, socket, HTTP client, model SDK or search SDK is used, so network-level bypass (proxy, raw socket, subprocess curl) is not tested here; that belongs to OS or firewall enforcement (M1-03).",
    "Sanitiser patterns are a candidate ruleset and not a complete sensitive-data detector; M1-05 additionally withholds all free text from the public candidate.",
    "Approval records are synthetic; the tool does not authenticate who approved.",
    "No independent review has been performed.",
    "These changes are unaccepted candidates. Local regression results do not satisfy independent review, original-input integration, CI, Unity or whole-project acceptance.",
    "Filesystem parents and approval adapters are trusted/exclusively owned inputs. No defense against hostile same-user concurrent writers, fsync durability or atomic multi-file publication is established.",
    "The M1-05 public audit projection intentionally omits free text and can have equal digests for different events. Inspect private raw receipts and publication payload hashes; do not treat that projection digest as content identity."
  ],
  "compatibility": {
    "baseRef": "86bb82bf5655562e2577cf2b83553ff261d8d5a6",
    "supersedesCandidateControlVersion": 1,
    "upstreamPinsUnchanged": true,
    "requiresReapproval": "Old audit-only approvals are rejected; never automatically migrate or renew them.",
    "historicalEvidence": "docs/team/R-06/synthetic-execution.json remains the v1 historical run and is not overwritten or promoted.",
    "completeRepositoryBinding": "Required before acceptance. Synthetic binding fixtures are not the original M0-03/M1-05 input bytes.",
    "publicationAuthority": "No real approval identity is authenticated by this in-process candidate."
  }
}
