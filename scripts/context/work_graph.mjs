#!/usr/bin/env node
// Pure, fail-closed snapshot checks. No network, permissions or distributed locking.
import {readFileSync,existsSync,realpathSync} from 'node:fs';
import {resolve,dirname,relative} from 'node:path';
import {createHash} from 'node:crypto';
import {fileURLToPath} from 'node:url';
export const PHASES=['prepare','candidate','accept'];
export const ARTIFACT_PHASES=['candidate','accept'];
export const RELATIONS=['contains','requires','context','verifies','conflicts_with','parallel_with','implements','supersedes'];
export const LEVELS=['target','present_in_worktree','tracked_at_head','historically_verified','currently_verified','unknown','blocked','superseded','rejected'];
// This conformance-bound module is the single effective-record amendment source.
// Only the fixed issue #10 Round-3 and issue #13 Round-2 records are active.
// Prior review, historical source, and current source pins keep their distinct roles.
export const EFFECTIVE_RECORD_AMENDMENTS=Object.freeze({schemaVersion:1,mode:'local_review_rework_hash_guarded',amendments:Object.freeze([Object.freeze({issueNumber:10,workId:'FMP-01',recordCollection:'runtime',baseRecordCanonicalSha256:'9792e59b93f770ae333ef9ba6ad71d17d91970f371123dfb794ead7e36d8b852',replacementPath:'candidates/issue-rework/010-round3/proposed-replacement-raw-record.json',replacementSha256:'95a941f5cc234d38797a0d7fd0b0323e4b598a4736e046a426fdcb1c626038de',authorityPath:'candidates/issue-rework/010-round3/proposed-authority-amendments.json',authoritySha256:'44aa2b2d7253fcccbd52d8f7c6085923be121709bf33da621fc18637edf82795',authorityStatus:'proposed_not_active_parent_authority_merge_required',remediationPath:'candidates/issue-rework/010-round3/failed-gate-remediation.json',remediationSha256:'a0b0ec0fab9edb55c60f1132b74b76a4e691b87892ac553c8020d2bd5f787c13',remediationStatus:'author_side_selector_correction_not_independent_grade',evidenceReportPath:'candidates/issue-rework/010-round3/evidence-change-report.md',evidenceReportSha256:'1579f789cc4505a5d98566b3cd2e6e4727c206b65fd4971c4a80f4d88ce9495a',previousReviewPacketPath:'candidates/blind-issues/010/round2.json',previousReviewPacketSha256:'bafd62f1cc7a842e65906f465763c035a72a43c48472129971ab4c58e165c495',previousReviewVerdictPath:'reviews/issue-verdicts/010/round2.json',previousReviewVerdictSha256:'ea254f993420ff2126c9edebb6d3305a3fabba2291b4708acec9ef2b259c4e1e',previousReviewDecision:'BOTH_REWORK',baseRound1PacketPath:'candidates/blind-issues/010/round1.json',baseRound1PacketSha256:'973e9a40cabd11f11d05804eaf1c698ae576cea9eb3e013069678608fcec479a',baseRound1VerdictPath:'reviews/issue-verdicts/010/round1.json',baseRound1VerdictSha256:'7923de644f9f859c55b53c68aaf25fdabe51f3fde9eb27edbde58ce7096adcca',verifiedSourcePath:'reviews/issue-support/010/frozen-source-verification-round3.json',verifiedSourceSha256:'c0ca9b067981b9881f15751feee68bb6a76fd19d6acbb03bfc0f10a4ce1a3e17',originalFrozenVerificationPath:'reviews/issue-support/010/frozen-source-verification.json',originalFrozenVerificationSha256:'cf615ee7d5f3df059f0f240db57f1d360e86bc3a23b2ce7e869b636fbe5db07f'}),Object.freeze({issueNumber:13,workId:'M0-00',recordCollection:'governance',baseRecordCanonicalSha256:'b3ee44a2a6ee4643c7277c07c3d019634ad64c09917d534dd3a91081315dc014',replacementPath:'candidates/issue-rework/013-round2/proposed-replacement-raw-record.json',replacementSha256:'aeb865233b7a4f48de5fb1951e358385d895f6232a98cb75994c3489ab175c97',authorityPath:'candidates/issue-rework/013-round2/proposed-authority-amendments.json',authoritySha256:'670292bdfe0624de17e0bbe45b5be34fbc531f4ad7938cac52c9e432f1d70121',authorityStatus:'proposed_not_active_parent_authority_merge_required',remediationPath:'candidates/issue-rework/013-round2/failed-gate-remediation.json',remediationSha256:'e99ae34f50ad76bf4596a2ad3fe12dca4eef5aeb0d50187e37022f679a56dfeb',remediationStatus:'author_side_round2_correction_not_independent_grade',evidenceReportPath:'candidates/issue-rework/013-round2/evidence-change-report.md',evidenceReportSha256:'fca72b9e7a1b45897f85da859590bb9fd2f2460bd7975fee6a91eef54014a1cd',previousReviewPacketPath:'candidates/blind-issues/013/round1.json',previousReviewPacketSha256:'3c974709389c1306847fb567bb5eedc235653e57168d28be7ebefd37bee2bfa6',previousReviewVerdictPath:'reviews/issue-verdicts/013/astra-round1.json',previousReviewVerdictSha256:'76b952a99871dd7236880b14403559653626a0efc79802243f53f5da6acb0b93',previousReviewDecision:'BOTH_REWORK',verifiedSourcePath:'reviews/issue-support/013/frozen-source-verification-round2.json',verifiedSourceSha256:'ab59f580fb69e38a00ac4dbdcbdf1cc8903a80b9c6b31e0043d3421b5a7701b0',historicalPmContractPath:'reviews/issue-support/013/pm-execution-contract-before-shared-g13-fix.md',historicalPmContractSha256:'302c11f9057b063ff3e3e9b761d338bc70620c49058dac94eda32ec2a1e33690',currentPmContractPath:'package/docs/context/pm-execution-contract.md',currentPmContractSha256:'5aa86038de1cc8f822daa24011000f81f0485b63ab23e831c02efcfc3f467d20',parentChangeProofPath:'reviews/issue-support/013/shared-pm-contract-change.json',parentChangeProofSha256:'e50de0cbc079e97d6c015977b1e81d3361aef074ea9420315e7553557722786e',archiveManifestPath:'candidates/issue-rework/013-round2-pre-shared-policy/archive-manifest.json',archiveManifestSha256:'2d5c71aadba1311becd4b1838cd71d1be8c6ff13e7a0a13726bc50f68532097a'})])});
const AMENDMENT_MANIFEST_KEYS=['schemaVersion','mode','amendments'];
const ISSUE10_AMENDMENT_ENTRY_KEYS=['issueNumber','workId','recordCollection','baseRecordCanonicalSha256','replacementPath','replacementSha256','authorityPath','authoritySha256','authorityStatus','remediationPath','remediationSha256','remediationStatus','evidenceReportPath','evidenceReportSha256','previousReviewPacketPath','previousReviewPacketSha256','previousReviewVerdictPath','previousReviewVerdictSha256','previousReviewDecision','baseRound1PacketPath','baseRound1PacketSha256','baseRound1VerdictPath','baseRound1VerdictSha256','verifiedSourcePath','verifiedSourceSha256','originalFrozenVerificationPath','originalFrozenVerificationSha256'];
const ISSUE13_AMENDMENT_ENTRY_KEYS=['issueNumber','workId','recordCollection','baseRecordCanonicalSha256','replacementPath','replacementSha256','authorityPath','authoritySha256','authorityStatus','remediationPath','remediationSha256','remediationStatus','evidenceReportPath','evidenceReportSha256','previousReviewPacketPath','previousReviewPacketSha256','previousReviewVerdictPath','previousReviewVerdictSha256','previousReviewDecision','verifiedSourcePath','verifiedSourceSha256','historicalPmContractPath','historicalPmContractSha256','currentPmContractPath','currentPmContractSha256','parentChangeProofPath','parentChangeProofSha256','archiveManifestPath','archiveManifestSha256'];
const AMENDMENT_ENTRY_KEYS_BY_ISSUE=new Map([[10,ISSUE10_AMENDMENT_ENTRY_KEYS],[13,ISSUE13_AMENDMENT_ENTRY_KEYS]]);
const RECORD_COLLECTIONS=['runtime','uxqa','art','governance'];
function amendmentEntryShape(entry){
 if(!entry||!AMENDMENT_ENTRY_KEYS_BY_ISSUE.has(entry.issueNumber))return false;
 const expected=AMENDMENT_ENTRY_KEYS_BY_ISSUE.get(entry.issueNumber);
 if(!keys(entry,expected)||!text(entry.workId)||!RECORD_COLLECTIONS.includes(entry.recordCollection)||!digest(entry.baseRecordCanonicalSha256)||!digest(entry.replacementSha256)||!digest(entry.authoritySha256)||!text(entry.authorityStatus)||!digest(entry.remediationSha256)||!text(entry.remediationStatus)||!digest(entry.evidenceReportSha256)||!digest(entry.previousReviewPacketSha256)||!digest(entry.previousReviewVerdictSha256)||entry.previousReviewDecision!=='BOTH_REWORK'||!digest(entry.verifiedSourceSha256))return false;
 if(entry.issueNumber===10)return entry.workId==='FMP-01'&&entry.recordCollection==='runtime'&&digest(entry.baseRound1PacketSha256)&&digest(entry.baseRound1VerdictSha256)&&digest(entry.originalFrozenVerificationSha256)&&entry.replacementPath==='candidates/issue-rework/010-round3/proposed-replacement-raw-record.json'&&entry.authorityPath==='candidates/issue-rework/010-round3/proposed-authority-amendments.json'&&entry.remediationPath==='candidates/issue-rework/010-round3/failed-gate-remediation.json'&&entry.evidenceReportPath==='candidates/issue-rework/010-round3/evidence-change-report.md'&&entry.previousReviewPacketPath==='candidates/blind-issues/010/round2.json'&&entry.previousReviewVerdictPath==='reviews/issue-verdicts/010/round2.json'&&entry.baseRound1PacketPath==='candidates/blind-issues/010/round1.json'&&entry.baseRound1VerdictPath==='reviews/issue-verdicts/010/round1.json'&&entry.verifiedSourcePath==='reviews/issue-support/010/frozen-source-verification-round3.json'&&entry.originalFrozenVerificationPath==='reviews/issue-support/010/frozen-source-verification.json';
 return entry.workId==='M0-00'&&entry.recordCollection==='governance'&&entry.replacementPath==='candidates/issue-rework/013-round2/proposed-replacement-raw-record.json'&&entry.authorityPath==='candidates/issue-rework/013-round2/proposed-authority-amendments.json'&&entry.remediationPath==='candidates/issue-rework/013-round2/failed-gate-remediation.json'&&entry.evidenceReportPath==='candidates/issue-rework/013-round2/evidence-change-report.md'&&entry.previousReviewPacketPath==='candidates/blind-issues/013/round1.json'&&entry.previousReviewVerdictPath==='reviews/issue-verdicts/013/astra-round1.json'&&entry.verifiedSourcePath==='reviews/issue-support/013/frozen-source-verification-round2.json'&&entry.historicalPmContractPath==='reviews/issue-support/013/pm-execution-contract-before-shared-g13-fix.md'&&digest(entry.historicalPmContractSha256)&&entry.currentPmContractPath==='package/docs/context/pm-execution-contract.md'&&digest(entry.currentPmContractSha256)&&entry.parentChangeProofPath==='reviews/issue-support/013/shared-pm-contract-change.json'&&digest(entry.parentChangeProofSha256)&&entry.archiveManifestPath==='candidates/issue-rework/013-round2-pre-shared-policy/archive-manifest.json'&&digest(entry.archiveManifestSha256);
}
const stableValue=v=>Array.isArray(v)?v.map(stableValue):(v&&typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=>[k,stableValue(v[k])])):v);
const sha256Bytes=v=>createHash('sha256').update(v).digest('hex');
const canonicalDigest=v=>sha256Bytes(JSON.stringify(stableValue(v)));
function pinnedBytes(root,repoPath,expectedDigest){if(!safePath(repoPath))throw Error('invalid amendment source path '+repoPath);const full=resolve(root,repoPath),rel=relative(resolve(root),full);if(rel.startsWith('..')||rel==='..'||!existsSync(full))throw Error('missing amendment source '+repoPath);const realRoot=realpathSync(resolve(root)),realFile=realpathSync(full),realRel=relative(realRoot,realFile);if(realRel.startsWith('..')||realRel==='..')throw Error('amendment source resolves outside root '+repoPath);const bytes=readFileSync(realFile);if(sha256Bytes(bytes)!==expectedDigest)throw Error('drifted amendment source '+repoPath);return bytes;}
function parsePinnedJson(bytes,repoPath){try{return JSON.parse(bytes);}catch(e){throw Error('invalid amendment JSON '+repoPath+': '+e.message);}}
function pinnedJson(root,repoPath,expectedDigest){return parsePinnedJson(pinnedBytes(root,repoPath,expectedDigest),repoPath);}
const sourcePinMatches=(source,path,sha256)=>source&&source.path===path&&source.wholeFileSha256===sha256;
const reviewPairMatches=(packet,verdict,entry,round)=>packet?.issueNumber===entry.issueNumber&&packet.round===round&&packet.packetId===verdict?.packetId&&verdict?.issueNumber===entry.issueNumber&&verdict.round===round&&verdict.packetHash===entry.previousReviewPacketSha256&&verdict.decision===entry.previousReviewDecision;
const amendmentPins=entry=>entry.issueNumber===10?[
 ['replacement',entry.replacementPath,entry.replacementSha256],['authority',entry.authorityPath,entry.authoritySha256],['remediation',entry.remediationPath,entry.remediationSha256],['evidenceReport',entry.evidenceReportPath,entry.evidenceReportSha256],['previousReviewPacket',entry.previousReviewPacketPath,entry.previousReviewPacketSha256],['previousReviewVerdict',entry.previousReviewVerdictPath,entry.previousReviewVerdictSha256],['baseRound1Packet',entry.baseRound1PacketPath,entry.baseRound1PacketSha256],['baseRound1Verdict',entry.baseRound1VerdictPath,entry.baseRound1VerdictSha256],['verifiedSource',entry.verifiedSourcePath,entry.verifiedSourceSha256],['originalFrozenVerification',entry.originalFrozenVerificationPath,entry.originalFrozenVerificationSha256]
]:[
 ['replacement',entry.replacementPath,entry.replacementSha256],['authority',entry.authorityPath,entry.authoritySha256],['remediation',entry.remediationPath,entry.remediationSha256],['evidenceReport',entry.evidenceReportPath,entry.evidenceReportSha256],['previousReviewPacket',entry.previousReviewPacketPath,entry.previousReviewPacketSha256],['previousReviewVerdict',entry.previousReviewVerdictPath,entry.previousReviewVerdictSha256],['verifiedSource',entry.verifiedSourcePath,entry.verifiedSourceSha256],['historicalPmContract',entry.historicalPmContractPath,entry.historicalPmContractSha256],['currentPmContract',entry.currentPmContractPath,entry.currentPmContractSha256],['parentChangeProof',entry.parentChangeProofPath,entry.parentChangeProofSha256],['archiveManifest',entry.archiveManifestPath,entry.archiveManifestSha256]
];
function requireAmendment(value,message){if(!value)throw Error(message);}
function noQuarantinePath(value){return typeof value==='string'&&!value.startsWith('reviews/quarantine/')&&!value.includes('/quarantine/');}
function pinnedTransitive(root,role,source){requireAmendment(source&&safePath(source.path)&&digest(source.wholeFileSha256)&&noQuarantinePath(source.path),'invalid transitive amendment source '+role);const bytes=pinnedBytes(root,source.path,source.wholeFileSha256);return {role,path:source.path,declaredSha256:source.wholeFileSha256,actualSha256:sha256Bytes(bytes),bytes};}
function pointerValue(value,pointer){requireAmendment(typeof pointer==='string'&&pointer.startsWith('/'),'invalid amendment selector');return pointer.slice(1).split('/').reduce((current,token)=>current?.[token.replaceAll('~1','/').replaceAll('~0','~')],value);}
function loadIssue10Amendment(root,entry,files,pins){
 const replacement=parsePinnedJson(files.replacement,entry.replacementPath),authority=parsePinnedJson(files.authority,entry.authorityPath),remediation=parsePinnedJson(files.remediation,entry.remediationPath),previousReviewPacket=parsePinnedJson(files.previousReviewPacket,entry.previousReviewPacketPath),previousReviewVerdict=parsePinnedJson(files.previousReviewVerdict,entry.previousReviewVerdictPath),baseRound1Packet=parsePinnedJson(files.baseRound1Packet,entry.baseRound1PacketPath),baseRound1Verdict=parsePinnedJson(files.baseRound1Verdict,entry.baseRound1VerdictPath),verified=parsePinnedJson(files.verifiedSource,entry.verifiedSourcePath),originalFrozenVerification=parsePinnedJson(files.originalFrozenVerification,entry.originalFrozenVerificationPath);
 requireAmendment(replacement.number===10&&replacement.workId==='FMP-01','replacement record identity mismatch for issue 10');
 requireAmendment(authority.issueNumber===10&&authority.workId==='FMP-01'&&authority.status===entry.authorityStatus,'authority amendment identity/status mismatch for issue 10');
 requireAmendment(remediation.issueNumber===10&&remediation.status===entry.remediationStatus&&remediation.previousReviewVerdict?.decision===entry.previousReviewDecision,'remediation previous-review identity/status mismatch for issue 10');
 requireAmendment(files.evidenceReport.length>0,'empty amendment evidence report for issue 10');
 requireAmendment(reviewPairMatches(previousReviewPacket,previousReviewVerdict,entry,2),'previous review packet/verdict binding mismatch for issue 10');
 requireAmendment(baseRound1Packet?.issueNumber===10&&baseRound1Packet.round===1&&baseRound1Packet.packetId===baseRound1Verdict?.packetId&&baseRound1Verdict?.issueNumber===10&&baseRound1Verdict.round===1&&baseRound1Verdict.packetHash===entry.baseRound1PacketSha256&&baseRound1Verdict.decision===entry.previousReviewDecision,'historical Round-1 packet/verdict binding mismatch for issue 10');
 requireAmendment((verified.issueNumber??verified.issue)===10&&verified.rerun===false&&(originalFrozenVerification.issueNumber??originalFrozenVerification.issue)===10&&originalFrozenVerification.rerun===false,'frozen-source report binding mismatch for issue 10');
 const sourceAuthority=authority.sourceAuthority;
 requireAmendment(sourceAuthority&&sourcePinMatches(sourceAuthority.round1,entry.baseRound1PacketPath,entry.baseRound1PacketSha256)&&sourcePinMatches(sourceAuthority.previousReviewVerdict,entry.previousReviewVerdictPath,entry.previousReviewVerdictSha256)&&sourcePinMatches(sourceAuthority.frozenVerification,entry.verifiedSourcePath,entry.verifiedSourceSha256)&&sourcePinMatches(sourceAuthority.originalFrozenVerification,entry.originalFrozenVerificationPath,entry.originalFrozenVerificationSha256),'authority source closure mismatch for issue 10');
 const runtimePin=pinnedTransitive(root,'runtimeRecords',sourceAuthority.runtimeRecords);
 return {replacement,authority,remediation,previousReviewPacket,previousReviewVerdict,baseRound1Packet,baseRound1Verdict,verifiedSource:verified,originalFrozenVerification,archiveManifest:null,transitive:[runtimePin,...pins.filter(pin=>['previousReviewPacket','previousReviewVerdict','baseRound1Packet','baseRound1Verdict','verifiedSource','originalFrozenVerification'].includes(pin.role))]};
}
function loadIssue13Amendment(root,entry,files){
 const replacement=parsePinnedJson(files.replacement,entry.replacementPath),authority=parsePinnedJson(files.authority,entry.authorityPath),remediation=parsePinnedJson(files.remediation,entry.remediationPath),previousReviewPacket=parsePinnedJson(files.previousReviewPacket,entry.previousReviewPacketPath),previousReviewVerdict=parsePinnedJson(files.previousReviewVerdict,entry.previousReviewVerdictPath),verified=parsePinnedJson(files.verifiedSource,entry.verifiedSourcePath),parentProof=parsePinnedJson(files.parentChangeProof,entry.parentChangeProofPath),archiveManifest=parsePinnedJson(files.archiveManifest,entry.archiveManifestPath);
 requireAmendment(replacement.number===13&&replacement.workId==='M0-00','replacement record identity mismatch for issue 13');
 requireAmendment(authority.issueNumber===13&&authority.workId==='M0-00'&&authority.status===entry.authorityStatus,'authority amendment identity/status mismatch for issue 13');
 requireAmendment(remediation.issue?.number===13&&remediation.issue.workId==='M0-00'&&remediation.issue.round===2&&remediation.status===entry.remediationStatus&&remediation.officialPriorReview?.decision===entry.previousReviewDecision,'remediation previous-review identity/status mismatch for issue 13');
 requireAmendment(files.evidenceReport.length>0&&reviewPairMatches(previousReviewPacket,previousReviewVerdict,entry,1),'official issue 13 Round-1 packet/verdict binding mismatch');
 const failedGateIds=Object.entries(previousReviewVerdict.options?.B?.gates??{}).filter(([,gate])=>gate?.verdict==='FAIL').map(([id])=>id);
 requireAmendment(JSON.stringify(failedGateIds)===JSON.stringify(['G2','G5','G6','G7','G9','G10','G13']),'official issue 13 failed-gate set mismatch');
 requireAmendment(verified.schemaVersion===1&&verified.kind==='issue-frozen-source-verification'&&verified.issueNumber===13&&verified.workId==='M0-00'&&verified.round===2&&verified.status==='author_side_static_source_verification_not_grade_not_execution'&&verified.writesToActiveAuthority===false&&verified.reviewerVerdictOrGrade===false&&verified.execution&&Object.values(verified.execution).every(value=>value===false),'issue 13 frozen-source report binding mismatch');
 const sourceAuthority=authority.sourceAuthority,sourceKeys=['governanceRecords','round1Packet','round1RenderedCandidate','officialAstraRound1Verdict','workGraph','topology','artifactRegistry','capturedIssue','capturedProjectRow','frozenVerification','frozenGates','frozenRubric','sharedPmContractProvenance'];
 requireAmendment(keys(sourceAuthority,sourceKeys),'unknown or missing issue 13 source-authority field');
 requireAmendment(sourcePinMatches(sourceAuthority.round1Packet,entry.previousReviewPacketPath,entry.previousReviewPacketSha256)&&sourcePinMatches(sourceAuthority.officialAstraRound1Verdict,entry.previousReviewVerdictPath,entry.previousReviewVerdictSha256)&&sourcePinMatches(sourceAuthority.frozenVerification,entry.verifiedSourcePath,entry.verifiedSourceSha256),'issue 13 official review/support source closure mismatch');
 const optionB=previousReviewPacket.options?.B;
 requireAmendment(optionB?.payloadHash===sourceAuthority.round1Packet.proposedPayloadSha256&&canonicalDigest(optionB.payload)===optionB.payloadHash&&canonicalDigest(optionB.payload.canonicalMetadata)===sourceAuthority.round1Packet.selectedProposedRecord?.sha256&&optionB.payload.bodyHash===sourceAuthority.round1RenderedCandidate.bodyHash&&sourceAuthority.round1RenderedCandidate.bodyHash===sourceAuthority.round1RenderedCandidate.wholeFileSha256,'issue 13 frozen Round-1 packet/body declaration mismatch');
 requireAmendment(sourceAuthority.round1RenderedCandidate.path==='candidates/rendered/013-m0-00.md'&&sourceAuthority.workGraph?.path==='package/docs/context/work-graph.json'&&sourceAuthority.workGraph.wholeFileSha256==='2f7713ef58032b7b0bca1c871ac387fbc5d550baf3d3fd036d996f352d1bca27'&&sourceAuthority.workGraph.selectedRecord?.sha256==='033bc5091ab7bd615c5a7b68592b5de07d8f1fd19e553d336f6f633a39a69c80'&&sourceAuthority.topology?.path==='candidates/topology.json'&&sourceAuthority.topology.wholeFileSha256==='9c6117b06cd95c51801d9d0035177fdd36616cef3c3a038c60456f2a1dc19b03'&&sourceAuthority.topology.selectedRecord?.sha256==='6c6e770b100d302b2de545d05aeeffc06928bf4b554fe5a7522a9ad8dfc797ab'&&sourceAuthority.artifactRegistry?.path==='candidates/artifact-registry.json'&&sourceAuthority.artifactRegistry.wholeFileSha256==='42c51414efef704b06ab6d2f4231544949e8bef1c9908880290ca241880b6e30'&&sourceAuthority.artifactRegistry.selectedRecords?.sha256==='da53a18c27f5852aab73941e714b4c87ce787d53c517572972281dce64a2d8ae','issue 13 historical generated-source declaration mismatch');
 const pm=sourceAuthority.sharedPmContractProvenance;
 requireAmendment(pm?.issue13WritesSharedPmContract===false&&pm.parentChangeIsSeparate===true&&pm.historicalRound1Source?.archivedPath===entry.historicalPmContractPath&&pm.historicalRound1Source.wholeFileSha256===entry.historicalPmContractSha256&&pm.historicalRound1Source.currentBytePin===false&&pm.currentSharedProposal?.path===entry.currentPmContractPath&&pm.currentSharedProposal.wholeFileSha256===entry.currentPmContractSha256&&pm.currentSharedProposal.proposalOnly===true&&pm.currentSharedProposal.executiondevelopers===undefined&&pm.currentSharedProposal.executionPermission===false&&pm.parentChangeProof?.path===entry.parentChangeProofPath&&pm.parentChangeProof.wholeFileSha256===entry.parentChangeProofSha256,'issue 13 shared PM-contract provenance mismatch');
 requireAmendment(parentProof.before?.path===entry.historicalPmContractPath&&parentProof.before.sha256===entry.historicalPmContractSha256&&parentProof.after?.path===entry.currentPmContractPath&&parentProof.after.sha256===entry.currentPmContractSha256,'issue 13 parent PM-contract proof mismatch');
 requireAmendment(verified.provenanceAlignment?.historicalRound1PmContract?.archivedPath===entry.historicalPmContractPath&&verified.provenanceAlignment.historicalRound1PmContract.sha256===entry.historicalPmContractSha256&&verified.provenanceAlignment.historicalRound1PmContract.currentBytePin===false&&verified.provenanceAlignment.currentSharedPmContractProposal?.path===entry.currentPmContractPath&&verified.provenanceAlignment.currentSharedPmContractProposal.sha256===entry.currentPmContractSha256&&verified.provenanceAlignment.parentChangeProof?.path===entry.parentChangeProofPath&&verified.provenanceAlignment.parentChangeProof.sha256===entry.parentChangeProofSha256,'issue 13 verifier PM-contract provenance mismatch');
 requireAmendment(authority.proposedReplacementRecord?.path===entry.replacementPath&&authority.proposedReplacementRecord.wholeFileSha256===entry.replacementSha256&&authority.proposedReplacementRecord.canonicalSha256===canonicalDigest(replacement),'issue 13 replacement canonical binding mismatch');
 const transitive=[];
 for(const [role,source] of [['governanceRecords',sourceAuthority.governanceRecords],['capturedIssue',sourceAuthority.capturedIssue],['capturedProjectRow',sourceAuthority.capturedProjectRow],['frozenGates',sourceAuthority.frozenGates],['frozenRubric',sourceAuthority.frozenRubric]])transitive.push(pinnedTransitive(root,role,source));
 const governance=parsePinnedJson(transitive.find(pin=>pin.role==='governanceRecords').bytes,sourceAuthority.governanceRecords.path),selected=pointerValue(governance,sourceAuthority.governanceRecords.selectedRecord?.selector);
 requireAmendment(sourceAuthority.governanceRecords.path==='candidates/governance-records.json'&&sourceAuthority.governanceRecords.selectedRecord?.sha256===entry.baseRecordCanonicalSha256&&canonicalDigest(selected)===entry.baseRecordCanonicalSha256,'issue 13 governance source authority mismatch');
 const project=parsePinnedJson(transitive.find(pin=>pin.role==='capturedProjectRow').bytes,sourceAuthority.capturedProjectRow.path),projectSelected=pointerValue(project,sourceAuthority.capturedProjectRow.selectedRecord?.selector);
 requireAmendment(canonicalDigest(projectSelected)===sourceAuthority.capturedProjectRow.selectedRecord.sha256,'issue 13 captured project-row selector mismatch');
 const archiveKeys=['schemaVersion','status','issueNumber','workId','reason','preservationOrder','files','notClaims'];
 requireAmendment(keys(archiveManifest,archiveKeys)&&archiveManifest.schemaVersion===1&&archiveManifest.status==='immutable_pre_shared_pm_policy_archive'&&archiveManifest.issueNumber===13&&archiveManifest.workId==='M0-00'&&Array.isArray(archiveManifest.files)&&archiveManifest.files.length===5,'issue 13 pre-shared-policy archive manifest mismatch');
 const expectedArchiveOriginals=[entry.replacementPath,entry.authorityPath,entry.remediationPath,entry.evidenceReportPath,entry.verifiedSourcePath];
 for(let index=0;index<archiveManifest.files.length;index++){
  const archived=archiveManifest.files[index];requireAmendment(keys(archived,['originalPath','archivePath','sha256'])&&archived.originalPath===expectedArchiveOriginals[index]&&archived.archivePath.startsWith('candidates/issue-rework/013-round2-pre-shared-policy/')&&noQuarantinePath(archived.archivePath)&&digest(archived.sha256),'issue 13 pre-shared-policy archive entry mismatch');
  transitive.push(pinnedTransitive(root,'preSharedPolicyArchive'+index,{path:archived.archivePath,wholeFileSha256:archived.sha256}));
 }
 requireAmendment(archiveManifest.files[0].sha256===entry.replacementSha256,'issue 13 replacement archive identity mismatch');
 const pathClaims=[entry.replacementPath,entry.authorityPath,entry.remediationPath,entry.evidenceReportPath,entry.previousReviewPacketPath,entry.previousReviewVerdictPath,entry.verifiedSourcePath,entry.historicalPmContractPath,entry.currentPmContractPath,entry.parentChangeProofPath,entry.archiveManifestPath,...sourceKeys.flatMap(key=>sourceAuthority[key]?.path?[sourceAuthority[key].path]:[])];
 requireAmendment(pathClaims.every(noQuarantinePath),'quarantined source cannot authorize issue 13');
 return {replacement,authority,remediation,previousReviewPacket,previousReviewVerdict,baseRound1Packet:null,baseRound1Verdict:null,verifiedSource:verified,originalFrozenVerification:null,parentChangeProof:parentProof,archiveManifest,transitive:transitive.map(({bytes,...pin})=>pin)};
}
function loadEffectiveRecordAmendment(root,entry){
 if(!amendmentEntryShape(entry))throw Error('invalid or unapproved effective-record amendment entry');
 const pins=amendmentPins(entry).map(([role,path,declaredSha256])=>{const bytes=pinnedBytes(root,path,declaredSha256);return {role,path,declaredSha256,actualSha256:sha256Bytes(bytes),bytes};});
 const files=Object.fromEntries(pins.map(pin=>[pin.role,pin.bytes]));
 const loaded=entry.issueNumber===10?loadIssue10Amendment(root,entry,files,pins.map(({bytes,...pin})=>pin)):loadIssue13Amendment(root,entry,files);
 return {...loaded,pins:pins.map(({bytes,...pin})=>pin),transitive:(loaded.transitive||[]).map(({bytes,...pin})=>pin)};
}
export function effectiveRecordAuthorityBindings(root,manifest=EFFECTIVE_RECORD_AMENDMENTS){
 if(!keys(manifest,AMENDMENT_MANIFEST_KEYS)||manifest.schemaVersion!==1||manifest.mode!=='local_review_rework_hash_guarded'||!Array.isArray(manifest.amendments))throw Error('invalid effective-record amendment manifest');
 const seen=new Set(),entries=[];
 for(const entry of manifest.amendments){if(seen.has(entry.issueNumber))throw Error('invalid or unapproved effective-record amendment entry');seen.add(entry.issueNumber);const loaded=loadEffectiveRecordAmendment(root,entry);entries.push({issueNumber:entry.issueNumber,workId:entry.workId,provenance:structuredClone(entry),pins:loaded.pins,transitive:loaded.transitive});}
 return {declarations:manifest.amendments.map(value=>structuredClone(value)),entries};
}
export function applyEffectiveRecordAmendments(root,recordCollections,manifest=EFFECTIVE_RECORD_AMENDMENTS){
 if(!keys(manifest,AMENDMENT_MANIFEST_KEYS)||manifest.schemaVersion!==1||manifest.mode!=='local_review_rework_hash_guarded'||!Array.isArray(manifest.amendments))throw Error('invalid effective-record amendment manifest');
 const objectShape=!Array.isArray(recordCollections);if(!objectShape&&!Array.isArray(recordCollections))throw Error('record collections must be an array or named object');
 const collections=objectShape?Object.fromEntries(Object.entries(recordCollections).map(([k,v])=>[k,structuredClone(v)])):{all:structuredClone(recordCollections)};
 for(const [name,records] of Object.entries(collections))if(!Array.isArray(records))throw Error('invalid record collection '+name);
 const seen=new Set(),authorities=[],compilerSourceAmendments=[];
 for(const entry of manifest.amendments){
  if(seen.has(entry.issueNumber))throw Error('invalid or unapproved effective-record amendment entry');seen.add(entry.issueNumber);
  const loaded=loadEffectiveRecordAmendment(root,entry),collectionKey=objectShape?entry.recordCollection:'all',records=collections[collectionKey];if(!records)throw Error('missing record collection '+entry.recordCollection);
  const matches=records.map((record,index)=>[record,index]).filter(([record])=>record?.number===entry.issueNumber);if(matches.length!==1)throw Error('base record identity is missing or duplicated for issue '+entry.issueNumber);const [base,index]=matches[0];
  if(base.workId!==entry.workId||canonicalDigest(base)!==entry.baseRecordCanonicalSha256)throw Error('base record canonical digest drift for issue '+entry.issueNumber);
  records[index]=structuredClone(loaded.replacement);const entryKeys=AMENDMENT_ENTRY_KEYS_BY_ISSUE.get(entry.issueNumber),provenance=Object.fromEntries(entryKeys.map(key=>[key,entry[key]]));
  authorities.push({issueNumber:entry.issueNumber,workId:entry.workId,authority:structuredClone(loaded.authority),remediation:structuredClone(loaded.remediation),evidenceReport:Buffer.from(amendmentPins(entry).find(([role])=>role==='evidenceReport')?pinnedBytes(root,entry.evidenceReportPath,entry.evidenceReportSha256):[]).toString('utf8'),replacementRecord:structuredClone(loaded.replacement),verifiedSource:structuredClone(loaded.verifiedSource),originalFrozenVerification:loaded.originalFrozenVerification?structuredClone(loaded.originalFrozenVerification):null,previousReviewPacket:structuredClone(loaded.previousReviewPacket),previousReviewVerdict:structuredClone(loaded.previousReviewVerdict),baseRound1Packet:loaded.baseRound1Packet?structuredClone(loaded.baseRound1Packet):null,baseRound1Verdict:loaded.baseRound1Verdict?structuredClone(loaded.baseRound1Verdict):null,parentChangeProof:loaded.parentChangeProof?structuredClone(loaded.parentChangeProof):null,archiveManifest:loaded.archiveManifest?structuredClone(loaded.archiveManifest):null,pins:structuredClone(loaded.pins),transitive:structuredClone(loaded.transitive),provenance});
  compilerSourceAmendments.push(provenance);
 }
 return {records:objectShape?collections:collections.all,authorities,compilerSourceAmendments};
}
const STATUSES=['Backlog','Ready','In progress','Review','Blocked'];
const TEMPLATES=['workspace-files-v1','unity-editor-v1','measurement-run-v1'];
const RESOURCE_PHASES=['candidate','accept'];
const WORKSPACE_FIELDS=['workspaceId','realCheckoutPath','volumeIdentity','branchRef','baseRef','selectedWritePaths','leaseOwner','fencingToken'];
const ISSUE13_WORKSPACE_FIELDS=['workspaceId','realCheckoutPath','volumeIdentity','branchRef','baseRef','selectedWritePaths','generatedClosure','leaseOwner','fencingToken'];
const ISSUE13_WORKSPACE_EXPANSION='workspaceId + canonicalized real paths; generatedClosure must equal [] for #13';
const ISSUE13_WORKSPACE_FAILURE='Reject missing/extra path, alias overlap, different base, stale holder, duplicate fence, or any generated/scratch write. Preserve current work before wait/rebase/relocate/serialize.';
const UNITY_FIELDS=['workspaceId','editorInstanceId','editorPid','editorProcessStart','generatedOutputRoots','projectSettingsPaths'];
const MEASUREMENT_FIELDS=['runId','outputRoot','hostResourceNamespace','processGroup','ports','devices','serviceRooms','leaseOwner','fencingToken'];
const WORKSPACE_EXPANSION='workspaceId + canonicalized real paths, including generated closure';
const WORKSPACE_FAILURE='reject overlap in same physical workspace; independent branches may prepare proposals, canonical integration still needs requiredLocks';
const WORKSPACE_PERMISSIONS='read/read allowed; any overlapping write rejected in same bound workspace';
const UNITY_IDENTITIES=['unity-workspace:{workspaceId}','editor-process:{hostProcessNamespace}:{editorPid}:{editorProcessStart}','fs-output:{realOutputPath}'];
const UNITY_RULE='Same checkout conflicts even if Editor PID differs. Different checkouts/Editors/outputs can run concurrently when static integration locks also disjoint.';
const MEASUREMENT_IDENTITIES=['run-output:{realOutputRoot}','process:{hostResourceNamespace}:{pid}:{start}','port:{networkNamespace}:{protocol}:{bindAddress}:{port}','device:{physicalDeviceId}','room:{serviceEndpoint}:{roomId}'];
const MEASUREMENT_RULE='runId or source SHA alone never defines exclusivity; empty resource list means none used, not unknown; record actual shared ports/devices/processes.';
const RESOURCE_CANONICALIZATION={
 'workspace-files-v1':'realpath + filesystem volume/inode identity, symlink aliases collapse; workspace identity is not source digest',
 'unity-editor-v1':'checkout identity AND real Editor process identity AND every generated root; checkout-level mutex prevents two Editors writing one project',
 'measurement-run-v1':'output path, network namespace/protocol/address/port, actual devices, service room, process identity; compare actual resources not textual placeholder names'
};
const ID=n=>`issue.${n}`,nodeNumber=s=>/^issue\.[1-9]\d*$/.test(s)?Number(s.slice(6)):NaN;
const obj=x=>x!==null&&typeof x==='object'&&!Array.isArray(x),text=x=>typeof x==='string'&&x.trim().length>0;
const integer=x=>Number.isSafeInteger(x)&&x>0,unique=a=>new Set(a).size===a.length;
const sha=x=>typeof x==='string'&&/^(?:[a-f0-9]{40}|[a-f0-9]{64})$/.test(x),digest=x=>typeof x==='string'&&/^[a-f0-9]{64}$/.test(x);
const content=x=>text(x)||(Array.isArray(x)&&x.length>0&&x.every(content))||(obj(x)&&Object.keys(x).length>0&&Object.values(x).every(content));
const keys=(o,allowed)=>obj(o)&&Object.keys(o).every(k=>allowed.includes(k));
const phaseList=x=>Array.isArray(x)&&x.length>0&&unique(x)&&x.every(p=>PHASES.includes(p));
const issueURL=(u,n)=>u===`https://github.com/xrlab-dau/CHOOGuard/issues/${n}`;
const ACCESS_GUARD_KEYS=['id','kind','activation','consumerPhase','manifestProviderArtifact','operations','predicate'];
const ACCESS_OPERATION_KEYS=['path','operation','declaredAvailability','observedAt','localAtCapture','developAtCapture','captureEvidence'];
const ACCESS_CAPTURE_EVIDENCE_KEYS=['snapshotBaseRef','publishedRef','localSha256','localGitBlobSha','developGitBlobSha','sourceFilesPointer'];
const ACCESS_PREDICATE_KEYS=['allSelectedPathsCoveredByManifest','accessibleSourceRefRequired','pathDigestMatchRequired','localPresenceAloneQualifies','unavailableOrUnlistedPathBlocksOnlyThisConsumingPhase','payloadManifestMaySatisfyWithoutDirectIssue120Edge'];
const GUARD_PREDICATE_KEYS=['id','kind','consumerPhase','state','required','nativeIssueEdge'];
function accessCaptureEvidenceShape(e){return keys(e,ACCESS_CAPTURE_EVIDENCE_KEYS)&&sha(e.snapshotBaseRef)&&e.snapshotBaseRef.length===40&&e.publishedRef===null&&digest(e.localSha256)&&sha(e.localGitBlobSha)&&e.localGitBlobSha.length===40&&(e.developGitBlobSha===null||(sha(e.developGitBlobSha)&&e.developGitBlobSha.length===40))&&typeof e.sourceFilesPointer==='string'&&/^baseline\/source-files\.json\[[0-9]+\]$/.test(e.sourceFilesPointer);}
function accessGuardShape(a){
 if(!keys(a,ACCESS_GUARD_KEYS)||!text(a.id)||a.kind!=='selected_path_source_access'||!ARTIFACT_PHASES.includes(a.consumerPhase)||!text(a.manifestProviderArtifact)||!Array.isArray(a.operations)||!a.operations.length||!unique(a.operations.map(x=>x?.path))||!keys(a.predicate,ACCESS_PREDICATE_KEYS))return false;
 const p=a.predicate;
 if(p.allSelectedPathsCoveredByManifest!==true||p.accessibleSourceRefRequired!==true||p.pathDigestMatchRequired!==true||p.localPresenceAloneQualifies!==false||p.unavailableOrUnlistedPathBlocksOnlyThisConsumingPhase!==true||(p.payloadManifestMaySatisfyWithoutDirectIssue120Edge!==undefined&&p.payloadManifestMaySatisfyWithoutDirectIssue120Edge!==true))return false;
 if(p.payloadManifestMaySatisfyWithoutDirectIssue120Edge!==undefined&&a.activation?.type!=='artifact_payload_manifest')return false;
 for(const x of a.operations)if(!keys(x,ACCESS_OPERATION_KEYS)||!safePath(x.path)||!['read','read_write'].includes(x.operation)||x.declaredAvailability!=='local_unpublished'||!Number.isFinite(Date.parse(x.observedAt))||!(x.localAtCapture===null||typeof x.localAtCapture==='boolean')||!(x.developAtCapture===null||typeof x.developAtCapture==='boolean')||(x.captureEvidence!==undefined&&!accessCaptureEvidenceShape(x.captureEvidence)))return false;
 const v=a.activation;
 if(v?.type==='phase')return keys(v,['type','phase'])&&v.phase===a.consumerPhase;
 if(v?.type==='selected_or_alternative')return keys(v,['type','group','selectedArtifact'])&&text(v.group)&&text(v.selectedArtifact);
 if(v?.type==='artifact_payload_manifest')return keys(v,['type','selectedArtifact'])&&text(v.selectedArtifact)&&p.payloadManifestMaySatisfyWithoutDirectIssue120Edge===true;
 return false;
}
function accessGuardMatchesItemContext(item,a){return a.operations.every(operation=>{const context=item.context?.find(entry=>entry.path===operation.path);return !context||context.availability===operation.declaredAvailability;});}
function accessGuardContext(a,{consumerPhase,providerArtifact,group=null,selectedArtifact=null,direct=false}){if(!accessGuardShape(a)||a.consumerPhase!==consumerPhase||a.manifestProviderArtifact!==providerArtifact)return false;if(direct)return a.activation.type==='phase'||(a.activation.type==='artifact_payload_manifest'&&a.activation.selectedArtifact===providerArtifact);return a.activation.type==='selected_or_alternative'&&a.activation.group===group&&a.activation.selectedArtifact===selectedArtifact;}
const guardPredicateShape=p=>keys(p,GUARD_PREDICATE_KEYS)&&text(p.id)&&text(p.kind)&&ARTIFACT_PHASES.includes(p.consumerPhase)&&p.state==='unverified'&&text(p.required)&&p.nativeIssueEdge===false;
export function safePath(p,glob=false){if(!text(p)||p!==p.trim()||p!==p.normalize('NFC')||/[\\\x00-\x20\x7f:%?#\[\]{}]/.test(p)||p.startsWith('/'))return false;const q=glob&&p.endsWith('/**')?p.slice(0,-3):p;return !q.includes('*')&&q.length>0&&q.split('/').every(s=>s&&s!=='.'&&s!=='..');}
// Requirement evidence is embedded, not a permission to read private/local source bytes.
const REQUIREMENT_KEYS=['id','code','kind','title','definition','issueNumbers','mappingStatus','evidence','legacySource','limits'];
const REQUIREMENT_SOURCE_KEYS=['path','selector','quote','availability','ref','digest','access','qualification','limits'];
const REQUIREMENT_EDGE_KEYS=['from','to','relation','reason','mappingStatus','policy'];
const requirementPath=p=>safePath(p)&&!p.split('/').some(s=>['.git','.planning','.claude','.agents','.remember','.memlog','Library','Temp','Logs','UserSettings','private-data','checkpoints','node_modules','Builds','Build','__pycache__'].includes(s)||s==='.env'||s.startsWith('.env.'))&&!/\.(log|ulf|pem|key)$/i.test(p);
const exactKeys=(o,allowed)=>keys(o,allowed)&&allowed.every(k=>Object.hasOwn(o,k));
function requirementSource(s){return exactKeys(s,REQUIREMENT_SOURCE_KEYS)&&requirementPath(s.path)&&['selector','quote','access','qualification','limits'].every(k=>text(s[k]))&&exactKeys(s.digest,['algorithm','value'])&&s.digest.algorithm==='sha256'&&digest(s.digest.value)&&((s.availability==='local_snapshot'&&s.ref===null)||(s.availability==='qualified_ref'&&typeof s.ref==='string'&&/^[a-f0-9]{40}$/.test(s.ref)));}
// Edges are canonical. Both indices must already agree; never silently repair or infer mappings.
export function requirementProjection(g){
 const require=(ok,message)=>{if(!ok)throw Error('requirements: '+message);};
 require(Array.isArray(g.requirements)&&Array.isArray(g.items)&&Array.isArray(g.edges),'arrays required');
 const registry=new Map(),items=new Map(g.items.map(i=>[i.number,i])),byIssue=Object.fromEntries(g.items.map(i=>[i.number,[]])),byRequirement={};
 const declared=new Set(),indexed=new Set(),mapped=new Set(),pair=(n,id)=>JSON.stringify([n,id]);
 for(const r of g.requirements){
  require(exactKeys(r,REQUIREMENT_KEYS)&&typeof r.code==='string'&&/^F-[A-Z0-9]+(?:-[A-Z0-9]+)*$/.test(r.code)&&r.id==='requirement.'+r.code&&r.kind==='requirement'&&['title','definition','limits'].every(k=>text(r[k])),'identity/definition/fields');
  require(!registry.has(r.id),'duplicate identity');registry.set(r.id,r);byRequirement[r.id]=[];
  require(Array.isArray(r.issueNumbers)&&unique(r.issueNumbers)&&r.issueNumbers.every(n=>integer(n)&&items.has(n)),'invalid issueNumbers');
  require(['mapped','historical_on_hold','unresolved'].includes(r.mappingStatus)&&(r.mappingStatus==='unresolved')===(r.issueNumbers.length===0),'mapping status');
  require(exactKeys(r.legacySource,['pointer','availability','generatorRecovered'])&&text(r.legacySource.pointer)&&requirementPath(r.legacySource.pointer.split('#')[0])&&r.legacySource.availability==='unavailable'&&r.legacySource.generatorRecovered===false,'legacy source must remain unavailable');
  require(Array.isArray(r.evidence)&&unique(r.evidence.map(e=>e?.issue))&&r.evidence.length===r.issueNumbers.length,'one evidence record per pair');
  for(const e of r.evidence){
   require(exactKeys(e,['issue','kind','reason','sources','limits'])&&integer(e.issue)&&r.issueNumbers.includes(e.issue)&&['explicit_selector','definition_trace_outcome','historical_link'].includes(e.kind)&&text(e.reason)&&text(e.limits)&&Array.isArray(e.sources)&&e.sources.length>0&&e.sources.every(requirementSource),'invalid pair evidence/source');
  }
  for(const n of r.issueNumbers)declared.add(pair(n,r.id));
 }
 for(const i of g.items){const ids=i.requirements??[];require(Array.isArray(ids)&&unique(ids)&&ids.every(id=>registry.has(id)),'invalid item reverse index');for(const id of ids)indexed.add(pair(i.number,id));}
 for(const e of g.edges.filter(e=>e.relation==='implements')){
  const n=nodeNumber(e.from),r=registry.get(e.to);require(exactKeys(e,REQUIREMENT_EDGE_KEYS)&&items.has(n)&&r&&text(e.reason),'implements active work -> requirement only');
  require(['mapped','historical_on_hold'].includes(e.mappingStatus)&&e.mappingStatus===r.mappingStatus,'edge status mismatch');
  require(e.mappingStatus==='historical_on_hold'?exactKeys(e.policy,['path','selector'])&&requirementPath(e.policy.path)&&e.policy.path==='docs/context/work-orders/policy.json'&&e.policy.selector==='/goalOverrides/'+n:e.policy===null,'held policy pointer required; no execution authority');
  const key=pair(n,r.id);require(!mapped.has(key),'duplicate implements pair');mapped.add(key);byRequirement[r.id].push(n);
  const evidence=r.evidence.filter(x=>x.issue===n);require(evidence.length===1,'missing pair evidence');
  byIssue[n].push(structuredClone({id:r.id,code:r.code,title:r.title,definition:r.definition,mappingStatus:e.mappingStatus,policy:e.policy,reason:e.reason,evidence,legacySource:r.legacySource,limits:r.limits}));
 }
 const equal=(a,b)=>a.size===b.size&&[...a].every(x=>b.has(x));require(equal(mapped,declared)&&equal(mapped,indexed),'registry/item/implements pair mismatch');
 for(const numbers of Object.values(byRequirement))numbers.sort((a,b)=>a-b);
 for(const requirements of Object.values(byIssue))requirements.sort((a,b)=>a.id.localeCompare(b.id,'en'));
 return {byIssue,byRequirement};
}
const normalized=p=>p.replace(/\/\*\*$/,'').toLowerCase();
export function overlap(a,b){if(!safePath(a,true)||!safePath(b,true))return true;const x=normalized(a),y=normalized(b);return x===y||x.startsWith(y+'/')||y.startsWith(x+'/');}
const covers=(s,p)=>safePath(s,true)&&safePath(p)&&((s.endsWith('/**')&&(normalized(p)===normalized(s)||normalized(p).startsWith(normalized(s)+'/')))||normalized(s)===normalized(p));
const PHASE_SCOPE_KEYS=['path','mode','physicalBinding','canonicalWriteAllowed','requiresRegistration','originalMode','reason','childPolicy'];
const OUTPUT_BINDING_KEYS=['artifact','path','pathKind','bindingKind','scopePath','scopeMode','lockId','canonicalWriteAllowed','rawReceiptPolicy','receiptStorage'];
const OUTPUT_BINDING_PHASES=['candidate','accept','readOnlySnapshots'];
const RECEIPT_STORAGE_KEYS=['mode','appendOnly','completeRunRecordRequired','stableIndexPath','rawScopePath','rawLockId'];
function receiptStorageShape(x){if(!keys(x,RECEIPT_STORAGE_KEYS)||x.appendOnly!==true)return false;if(x.mode==='embedded_append_only_index')return x.completeRunRecordRequired===true&&x.stableIndexPath===undefined&&x.rawScopePath===undefined&&x.rawLockId===undefined;if(x.mode==='separate_immutable_raw_records')return x.completeRunRecordRequired===true&&safePath(x.stableIndexPath)&&safePath(x.rawScopePath,true)&&text(x.rawLockId);return false;}
const trimTerminalSlash=p=>p?.endsWith('/')?p.slice(0,-1):p;
function phaseScopePathValid(p,phase){if(!text(p))return false;if(phase==='prepare'){if(!p.startsWith('{proposalRoot}/')||p.slice('{proposalRoot}/'.length).includes('{')||p.slice('{proposalRoot}/'.length).includes('}'))return false;return safePath(trimTerminalSlash(p.slice('{proposalRoot}/'.length)),true);}return safePath(trimTerminalSlash(p),true);}
function phaseWriteScopeShape(s,phase){if(!keys(s,PHASE_SCOPE_KEYS)||!phaseScopePathValid(s.path,phase)||!['isolated_proposal','exclusive'].includes(s.mode)||!text(s.physicalBinding)||typeof s.canonicalWriteAllowed!=='boolean'||(s.originalMode!==undefined&&!['isolated_proposal','exclusive'].includes(s.originalMode))||(s.reason!==undefined&&!text(s.reason))||(s.childPolicy!==undefined&&(!text(s.childPolicy)||!s.path.endsWith('/'))))return false;if(phase==='prepare')return s.mode==='isolated_proposal'&&s.physicalBinding==='output:'+s.path&&s.canonicalWriteAllowed===false&&s.requiresRegistration===true&&s.originalMode===undefined;if(s.requiresRegistration!==undefined)return false;if(phase==='candidate')return s.mode==='isolated_proposal'&&s.physicalBinding==='workspace-file:{workspaceId}:'+s.path&&s.canonicalWriteAllowed===false;if(s.canonicalWriteAllowed!==(s.mode==='exclusive'))return false;if(s.mode==='isolated_proposal')return s.physicalBinding==='workspace-file:{workspaceId}:'+s.path;return s.physicalBinding==='workspace-file:{workspaceId}:'+s.path||s.physicalBinding==='repo-path:xrlab-dau/CHOOGuard:'+s.path||s.physicalBinding==='repo-file:xrlab-dau/CHOOGuard:'+s.path;}
function phaseWriteScopesShape(x){return keys(x,PHASES)&&PHASES.every(phase=>Array.isArray(x[phase])&&unique(x[phase].map(s=>s?.path))&&x[phase].every(s=>phaseWriteScopeShape(s,phase)));}
function artifactOutputBindingShape(b){return keys(b,OUTPUT_BINDING_KEYS)&&text(b.artifact)&&text(b.path)&&['file','bundle_glob','immutable_receipt_index'].includes(b.pathKind)&&['phase_output','requalifiable_phase_output','diagnostic_context_output_not_runnable','existing_read_only_snapshot'].includes(b.bindingKind)&&text(b.scopePath)&&safePath(trimTerminalSlash(b.scopePath),true)&&['isolated_proposal','exclusive','read_only'].includes(b.scopeMode)&&(b.lockId===null||text(b.lockId))&&typeof b.canonicalWriteAllowed==='boolean'&&(b.rawReceiptPolicy===null||text(b.rawReceiptPolicy))&&(b.receiptStorage===undefined||receiptStorageShape(b.receiptStorage))&&(b.pathKind==='bundle_glob'?safePath(b.path,true)&&b.path.endsWith('/**'):safePath(b.path))&&(b.pathKind==='immutable_receipt_index'?text(b.rawReceiptPolicy):b.rawReceiptPolicy===null&&b.receiptStorage===undefined);}
function artifactOutputBindingsShape(x){return keys(x,OUTPUT_BINDING_PHASES)&&OUTPUT_BINDING_PHASES.every(phase=>Array.isArray(x[phase])&&unique(x[phase].map(b=>b?.artifact))&&x[phase].every(artifactOutputBindingShape));}
const bindingOutputPath=b=>b?.pathKind==='bundle_glob'?b.path.slice(0,-3):b?.path;
const writable=(i,phase)=>phaseWriteScopesShape(i?.phaseWriteScopes)?(phase?i.phaseWriteScopes[phase]:PHASES.flatMap(p=>i.phaseWriteScopes[p])):Array.isArray(i?.writeScope)?i.writeScope.filter(w=>obj(w)&&w.mode!=='read_only'&&(!phase||w.phases?.includes(phase))):[];
function staticLocks(i,phase){return (i.requiredLocks||[]).filter(l=>{if(TEMPLATES.includes(l))return false;if(!phase)return true;const bindings=(i.writeScope||[]).filter(w=>w.lock===l);return bindings.length?bindings.some(w=>w.phases.includes(phase)):phase!=='prepare';});}
export function conflictReasons(a,b,options={}){const out=[],conflictWrites=(item,phase)=>writable(item,phase).filter(w=>w.canonicalWriteAllowed===undefined||w.canonicalWriteAllowed===true);for(const l of staticLocks(a,options.phaseA))if(staticLocks(b,options.phaseB).includes(l))out.push(`lock:${l}`);for(const x of conflictWrites(a,options.phaseA))for(const y of conflictWrites(b,options.phaseB))if(overlap(trimTerminalSlash(x.path),trimTerminalSlash(y.path)))out.push(`path:${x.path}|${y.path}`);return [...new Set(out)];}
const exactList=(x,expected)=>Array.isArray(x)&&x.length===expected.length&&x.every((v,i)=>v===expected[i]);
const resourceToken=x=>text(x)&&x===x.trim()&&x===x.normalize('NFC')&&!/[\x00-\x1f\x7f]/.test(x);
const resourceRepoPath=x=>resourceToken(x)&&safePath(trimTerminalSlash(x),true)&&!x.includes('*');
const physicalPath=x=>{if(!resourceToken(x)||x.includes('\\')||x.endsWith('/'))return false;let body;if(x.startsWith('//')){const parts=x.slice(2).split('/');return parts.length>=2&&parts.every(s=>s&&s!=='.'&&s!=='..');}if(x.startsWith('/'))body=x.slice(1);else if(/^[A-Za-z]:\//.test(x))body=x.slice(3);else return false;return body.length>0&&body.split('/').every(s=>s&&s!=='.'&&s!=='..');};
const physicalOverlap=(a,b)=>{const x=a.normalize('NFC').toLowerCase().replace(/\/$/,''),y=b.normalize('NFC').toLowerCase().replace(/\/$/,'');return x===y||x.startsWith(y+'/')||y.startsWith(x+'/');};
function resourceContractShape(c,issueNumber=null){if(!obj(c)||!TEMPLATES.includes(c.template)||!exactList(c.phases,RESOURCE_PHASES))return false;if(c.template==='workspace-files-v1'){const issue13=issueNumber===13;return keys(c,['template','phases','requiredFields','pathsFrom','keyExpansion','failure'])&&exactList(c.requiredFields,issue13?ISSUE13_WORKSPACE_FIELDS:WORKSPACE_FIELDS)&&c.pathsFrom==='phaseWriteScopes'&&c.keyExpansion===(issue13?ISSUE13_WORKSPACE_EXPANSION:WORKSPACE_EXPANSION)&&c.failure===(issue13?ISSUE13_WORKSPACE_FAILURE:WORKSPACE_FAILURE);}if(c.template==='unity-editor-v1')return keys(c,['template','phases','requiredFields','generatedOutputRoots','identityKeys','liveValues','rule'])&&exactList(c.requiredFields,UNITY_FIELDS)&&Array.isArray(c.generatedOutputRoots)&&c.generatedOutputRoots.length>0&&unique(c.generatedOutputRoots)&&c.generatedOutputRoots.every(resourceRepoPath)&&exactList(c.identityKeys,UNITY_IDENTITIES)&&c.liveValues===null&&c.rule===UNITY_RULE;if(c.template==='measurement-run-v1')return keys(c,['template','phases','requiredFields','identityKeys','liveValues','rule'])&&exactList(c.requiredFields,MEASUREMENT_FIELDS)&&exactList(c.identityKeys,MEASUREMENT_IDENTITIES)&&c.liveValues===null&&c.rule===MEASUREMENT_RULE;return false;}
function resourceContractsShape(item){if(!Array.isArray(item?.resourceBindings)||!item.resourceBindings.length||!unique(item.resourceBindings.map(x=>x?.template))||!item.resourceBindings.every(contract=>resourceContractShape(contract,item.number)))return false;const workspace=item.resourceBindings.find(x=>x.template==='workspace-files-v1');if(!workspace||!phaseWriteScopesShape(item.phaseWriteScopes)||RESOURCE_PHASES.some(phase=>!item.phaseWriteScopes[phase].length))return false;return item.resourceBindings.every(x=>x.template==='workspace-files-v1'||exactList(x.phases,workspace.phases));}
function reviewedResourceModelShape(model){if(!obj(model)||!Array.isArray(model.dynamicTemplates))return false;const relevant=model.dynamicTemplates.filter(x=>TEMPLATES.includes(x?.id));return relevant.length===TEMPLATES.length&&unique(relevant.map(x=>x.id))&&TEMPLATES.every(id=>relevant.some(x=>keys(x,id==='workspace-files-v1'?['id','canonicalization','permissions']:['id','canonicalization'])&&x.id===id&&x.canonicalization===RESOURCE_CANONICALIZATION[id]&&(id!=='workspace-files-v1'||x.permissions===WORKSPACE_PERMISSIONS)));}
const WORKSPACE_BINDING_KEYS=[...WORKSPACE_FIELDS,'generatedClosure'];
const UNITY_BINDING_KEYS=[...UNITY_FIELDS,'hostProcessNamespace'];
const PROCESS_KEYS=['pid','start'],processStart=x=>resourceToken(x)||(Number.isSafeInteger(x)&&x>=0),PORT_KEYS=['networkNamespace','protocol','bindAddress','port'],DEVICE_KEYS=['physicalDeviceId'],ROOM_KEYS=['serviceEndpoint','roomId'];
function validBinding(template,b){if(!obj(b))return false;if(template==='workspace-files-v1')return keys(b,WORKSPACE_BINDING_KEYS)&&workspaceID(b.workspaceId)&&physicalPath(b.realCheckoutPath)&&resourceToken(b.volumeIdentity)&&resourceToken(b.branchRef)&&sha(b.baseRef)&&Array.isArray(b.selectedWritePaths)&&b.selectedWritePaths.length>0&&unique(b.selectedWritePaths)&&b.selectedWritePaths.every(resourceRepoPath)&&Array.isArray(b.generatedClosure)&&unique(b.generatedClosure)&&b.generatedClosure.every(resourceRepoPath)&&resourceToken(b.leaseOwner)&&resourceToken(b.fencingToken);if(template==='unity-editor-v1')return keys(b,UNITY_BINDING_KEYS)&&workspaceID(b.workspaceId)&&resourceToken(b.editorInstanceId)&&integer(b.editorPid)&&processStart(b.editorProcessStart)&&resourceToken(b.hostProcessNamespace)&&Array.isArray(b.generatedOutputRoots)&&b.generatedOutputRoots.length>0&&unique(b.generatedOutputRoots)&&b.generatedOutputRoots.every(resourceRepoPath)&&Array.isArray(b.projectSettingsPaths)&&unique(b.projectSettingsPaths)&&b.projectSettingsPaths.every(resourceRepoPath);if(template==='measurement-run-v1')return keys(b,MEASUREMENT_FIELDS)&&resourceToken(b.runId)&&physicalPath(b.outputRoot)&&resourceToken(b.hostResourceNamespace)&&Array.isArray(b.processGroup)&&unique(b.processGroup.map(x=>JSON.stringify(x)))&&b.processGroup.every(x=>keys(x,PROCESS_KEYS)&&integer(x.pid)&&processStart(x.start))&&Array.isArray(b.ports)&&unique(b.ports.map(x=>JSON.stringify(x)))&&b.ports.every(x=>keys(x,PORT_KEYS)&&resourceToken(x.networkNamespace)&&['tcp','udp'].includes(x.protocol)&&resourceToken(x.bindAddress)&&integer(x.port)&&x.port<=65535)&&Array.isArray(b.devices)&&unique(b.devices.map(x=>JSON.stringify(x)))&&b.devices.every(x=>keys(x,DEVICE_KEYS)&&resourceToken(x.physicalDeviceId))&&Array.isArray(b.serviceRooms)&&unique(b.serviceRooms.map(x=>JSON.stringify(x)))&&b.serviceRooms.every(x=>keys(x,ROOM_KEYS)&&resourceToken(x.serviceEndpoint)&&resourceToken(x.roomId))&&resourceToken(b.leaseOwner)&&resourceToken(b.fencingToken);return false;}
const canonical=x=>JSON.stringify(Array.isArray(x)?x.map(v=>JSON.parse(canonical(v))).sort((a,b)=>JSON.stringify(a).localeCompare(JSON.stringify(b))):obj(x)?Object.fromEntries(Object.keys(x).sort().map(k=>[k,JSON.parse(canonical(x[k]))])):x);
const pathSet=x=>[...new Set(x)].sort((a,b)=>a.localeCompare(b));
const exactSet=(a,b)=>Array.isArray(a)&&canonical(pathSet(a))===canonical(pathSet(b));
const joinPhysical=(root,relative)=>root+'/'+trimTerminalSlash(relative);
function physicalClaims(template,b,workspace=null){if(!validBinding(template,b))return null;if(template==='workspace-files-v1')return {checkout:{volume:b.volumeIdentity,path:b.realCheckoutPath},paths:[...b.selectedWritePaths,...b.generatedClosure].map(path=>({volume:b.volumeIdentity,path:joinPhysical(b.realCheckoutPath,path)})),processes:[],ports:[],devices:[],rooms:[]};if(template==='unity-editor-v1'){if(!validBinding('workspace-files-v1',workspace)||workspace.workspaceId!==b.workspaceId)return null;return {checkout:{volume:workspace.volumeIdentity,path:workspace.realCheckoutPath},paths:[...b.generatedOutputRoots,...b.projectSettingsPaths].map(path=>({volume:workspace.volumeIdentity,path:joinPhysical(workspace.realCheckoutPath,path)})),processes:[[b.hostProcessNamespace,b.editorPid,b.editorProcessStart]],ports:[],devices:[],rooms:[]};}return {checkout:null,paths:[{volume:null,path:b.outputRoot}],processes:b.processGroup.map(x=>[b.hostResourceNamespace,x.pid,x.start]),ports:b.ports.map(x=>[x.networkNamespace,x.protocol,x.bindAddress,x.port]),devices:b.devices.map(x=>x.physicalDeviceId),rooms:b.serviceRooms.map(x=>[x.serviceEndpoint,x.roomId])};}
function resourceCollision(ta,a,tb,b,workspaceA=null,workspaceB=null){const x=physicalClaims(ta,a,workspaceA),y=physicalClaims(tb,b,workspaceB);if(!x||!y)return true;if(ta==='unity-editor-v1'&&tb==='unity-editor-v1'&&x.checkout.volume===y.checkout.volume&&physicalOverlap(x.checkout.path,y.checkout.path))return true;for(const p of x.paths)for(const q of y.paths)if((p.volume===null||q.volume===null||p.volume===q.volume)&&physicalOverlap(p.path,q.path))return true;for(const p of x.processes)if(y.processes.some(q=>canonical(p)===canonical(q)))return true;for(const p of x.ports)if(y.ports.some(q=>canonical(p)===canonical(q)))return true;for(const d of x.devices)if(y.devices.includes(d))return true;for(const r of x.rooms)if(y.rooms.some(q=>canonical(r)===canonical(q)))return true;return false;}
const artifactKey=(p,o,phase)=>JSON.stringify([p,o,phase]);
function graphArtifact(g,id){for(const producer of [...(g.items||[]),...(g.references||[])]){const output=producer?.outputs?.find(o=>o?.id===id);if(output)return {issue:producer.number,output};}return null;}
function branchGuards(g,alternative){const explicit=(alternative?.guards||[]).map(x=>({...x,source:'guard'})),additional=(alternative?.additionalInputs||[]).map(x=>{const found=graphArtifact(g,x.artifact);return {issue:found?.issue,artifact:x.artifact,producerPhase:found?.output?.producerPhase,accessGuard:x.accessGuard,consumerPhase:x.consumerPhase,reason:x.reason,source:'additionalInput'};});return [...explicit,...additional];}
const qualifiedKey=x=>artifactKey(x.producerIssue,x.outputId,x.producerPhase);
const depKey=d=>JSON.stringify([d.issue,d.artifact,d.consumerPhase,d.producerPhase]);
const depFromEdge=(e,successor=false)=>({issue:nodeNumber(successor?e.from:e.to),artifact:e.artifact,consumerPhase:e.consumerPhase,producerPhase:e.producerPhase,reason:e.reason});
const DEP_KEYS=['issue','artifact','consumerPhase','producerPhase','reason'];
const depValid=(d,allowAccess=false)=>keys(d,allowAccess?[...DEP_KEYS,'accessGuard']:DEP_KEYS)&&integer(d.issue)&&text(d.artifact)&&ARTIFACT_PHASES.includes(d.consumerPhase)&&ARTIFACT_PHASES.includes(d.producerPhase)&&text(d.reason)&&(d.accessGuard===undefined||accessGuardShape(d.accessGuard));
function phaseDependencies(g){const adj=new Map(g.items.flatMap(i=>PHASES.map(p=>[`${i.number}@${p}`,[]])));const add=(c,cp,p,pp)=>{const k=`${c}@${cp}`;if(!adj.has(k))adj.set(k,[]);adj.get(k).push(`${p}@${pp}`);};for(const i of g.items){add(i.number,'candidate',i.number,'prepare');add(i.number,'accept',i.number,'candidate');for(const x of i.inputs||[])if(x?.fromIssue!==null&&integer(x?.fromIssue))add(i.number,x.consumerPhase,x.fromIssue,x.producerPhase);for(const group of i.oneOfInputs||[])for(const a of group?.alternatives||[]){add(i.number,group.consumerPhase,a.issue,a.producerPhase);for(const guard of branchGuards(g,a))add(i.number,group.consumerPhase,guard.issue,guard.producerPhase);}}for(const e of g.edges)if(e.relation==='requires')add(nodeNumber(e.from),e.consumerPhase,nodeNumber(e.to),e.producerPhase);return adj;}
function reaches(adj,from,target){const seen=new Set(),todo=[...(adj.get(from)||[])];while(todo.length){const n=todo.pop();if(n===target)return true;if(seen.has(n))continue;seen.add(n);todo.push(...(adj.get(n)||[]));}return false;}
export function ancestors(g,n,relation='requires'){const seen=new Set(),todo=[n];while(todo.length){const at=todo.pop();for(const e of g.edges||[])if(e?.relation===relation&&e.from===ID(at)){const k=nodeNumber(e.to);if(integer(k)&&!seen.has(k)){seen.add(k);todo.push(k);}}}return seen;}
const LIVE_KEYS=['observedAt','issueNumber','issueUrl','sourceRef','reviewedContext','status','operation','phase','stage','claim','phaseEvidence','artifactReceipts','expectedArtifacts','selectedInputs','verifiedInputs','verifiedSources','sourceAccessEvidence','predicateEvidence','resourceBindings','leases','resolvedAuthorities','writerContext'];
const CLAIM_KEYS=['issueNumber','holder','sourceRef','evidence','phase'];
const EXPECTED_KEYS=['producerIssue','outputId','producerPhase','sourceRef','outputDigest','qualification','scope'];
const RECEIPT_KEYS=[...EXPECTED_KEYS,'available','accepted','evidence','reviewRef'];
const GUARD_KEYS=['issue','artifact','producerPhase','expectedSourceRef','expectedDigest','expectedScope'];
const ALTERNATIVE_KEYS=['issue','artifact','producerPhase','guards','additionalInputs'];
const ADDITIONAL_INPUT_KEYS=['artifact','consumerPhase','reason','accessGuard'];
const SELECTED_KEYS=[...RECEIPT_KEYS,'group','consumerPhase','compatible','compatibilityEvidence','guardBindings'];
const VERIFIED_INPUT_KEYS=['id','fromIssue','qualification','sourceRef','digest','verified','evidence'];
const VERIFIED_SOURCE_KEYS=['path','sourceRef','digest','available','evidence'];
const PHASE_EVIDENCE_KEYS=['phase','sourceRef','verified','evidence','inputBindings','selectedInputs','sourceAccessBindings','predicateBindings'];
const LEASE_KEYS=['key','issue','holder','baseRef','record','expiresAt','scopePaths','scope'];
const SOURCE_ACCESS_EVIDENCE_KEYS=['guardId','consumerPhase','provider','manifestEntries','observedOperations','observedAt','evidenceDigest','evidence','reviewRef'];
const MANIFEST_ENTRY_KEYS=['path','sourceRef','digest'];
const OBSERVED_ACCESS_KEYS=['path','operation','sourceRef','digest','accessible','evidence'];
const PREDICATE_EVIDENCE_KEYS=['id','kind','consumerPhase','verified','sourceRef','digest','observedAt','evidence','verifier'];
const WRITER_CONTEXT_KEYS=['issueNumber','phase','workspaceId','proposalRoot','observedAt','registration','bindings','leases'];
const WRITER_REGISTRATION_KEYS=['issueNumber','workspaceId','proposalRoot','owner','baseSourceRef','observedAt','evidence'];
const WRITER_BINDING_KEYS=['phase','scopePath','physicalBinding','resolvedPhysicalBinding','owner','baseSourceRef','evidence'];
const WRITER_LEASE_KEYS=['phase','scopePath','resource','owner','baseSourceRef','acquiredAt','expiresAt','fence','evidence'];
const workspaceID=x=>typeof x==='string'&&/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/.test(x);
function writerContextShape(x){if(!keys(x,WRITER_CONTEXT_KEYS)||!integer(x.issueNumber)||!PHASES.includes(x.phase)||!workspaceID(x.workspaceId)||!safePath(x.proposalRoot)||!Number.isFinite(Date.parse(x.observedAt))||!Array.isArray(x.bindings)||!Array.isArray(x.leases))return false;if(x.registration!==undefined&&(!keys(x.registration,WRITER_REGISTRATION_KEYS)||!integer(x.registration.issueNumber)||!workspaceID(x.registration.workspaceId)||!safePath(x.registration.proposalRoot)||!text(x.registration.owner)||!sha(x.registration.baseSourceRef)||!Number.isFinite(Date.parse(x.registration.observedAt))||!text(x.registration.evidence)))return false;for(const b of x.bindings)if(!keys(b,WRITER_BINDING_KEYS)||!PHASES.includes(b.phase)||!text(b.scopePath)||!text(b.physicalBinding)||!text(b.resolvedPhysicalBinding)||!text(b.owner)||!sha(b.baseSourceRef)||!text(b.evidence))return false;for(const l of x.leases)if(!keys(l,WRITER_LEASE_KEYS)||!PHASES.includes(l.phase)||!text(l.scopePath)||!text(l.resource)||!text(l.owner)||!sha(l.baseSourceRef)||!Number.isFinite(Date.parse(l.acquiredAt))||!Number.isFinite(Date.parse(l.expiresAt))||!text(l.fence)||!text(l.evidence))return false;return true;}
const sourceAccessEvidenceShape=x=>keys(x,SOURCE_ACCESS_EVIDENCE_KEYS)&&text(x.guardId)&&ARTIFACT_PHASES.includes(x.consumerPhase)&&expectedShape(x.provider)&&Array.isArray(x.manifestEntries)&&x.manifestEntries.length>0&&x.manifestEntries.every(v=>keys(v,MANIFEST_ENTRY_KEYS)&&safePath(v.path)&&sha(v.sourceRef)&&digest(v.digest))&&unique(x.manifestEntries.map(v=>v.path))&&Array.isArray(x.observedOperations)&&x.observedOperations.length>0&&x.observedOperations.every(v=>keys(v,OBSERVED_ACCESS_KEYS)&&safePath(v.path)&&['read','read_write'].includes(v.operation)&&sha(v.sourceRef)&&digest(v.digest)&&v.accessible===true&&text(v.evidence))&&unique(x.observedOperations.map(v=>JSON.stringify([v.path,v.operation])))&&Number.isFinite(Date.parse(x.observedAt))&&digest(x.evidenceDigest)&&text(x.evidence)&&text(x.reviewRef);
const predicateEvidenceShape=x=>keys(x,PREDICATE_EVIDENCE_KEYS)&&text(x.id)&&text(x.kind)&&ARTIFACT_PHASES.includes(x.consumerPhase)&&x.verified===true&&sha(x.sourceRef)&&digest(x.digest)&&Number.isFinite(Date.parse(x.observedAt))&&text(x.evidence)&&text(x.verifier);
const expectedShape=x=>keys(x,EXPECTED_KEYS);
const selectedShape=x=>keys(x,SELECTED_KEYS)&&(x.guardBindings===undefined||(Array.isArray(x.guardBindings)&&x.guardBindings.every(expectedShape)))&&(x.available===undefined||typeof x.available==='boolean')&&(x.accepted===undefined||typeof x.accepted==='boolean')&&(x.evidence===undefined||text(x.evidence))&&(x.reviewRef===undefined||text(x.reviewRef));
const guardShape=x=>keys(x,GUARD_KEYS)&&integer(x.issue)&&text(x.artifact)&&ARTIFACT_PHASES.includes(x.producerPhase)&&(x.expectedSourceRef===undefined||sha(x.expectedSourceRef))&&(x.expectedDigest===undefined||digest(x.expectedDigest))&&(x.expectedScope===undefined||text(x.expectedScope));
function liveShapeErrors(state){
 const errors=[],bad=(label)=>errors.push('unknown or malformed nested fields in '+label),checkArray=(name,allowed,optional=false)=>{const a=state[name];if(optional&&a===undefined)return;if(!Array.isArray(a)){bad(name);return;}for(const v of a)if(!keys(v,allowed))bad(name);};
 if(!keys(state,LIVE_KEYS))bad('live state');
 if(!keys(state.claim,CLAIM_KEYS))bad('claim');
 checkArray('artifactReceipts',RECEIPT_KEYS);checkArray('expectedArtifacts',EXPECTED_KEYS);checkArray('selectedInputs',SELECTED_KEYS,true);checkArray('verifiedInputs',VERIFIED_INPUT_KEYS);checkArray('verifiedSources',VERIFIED_SOURCE_KEYS);checkArray('sourceAccessEvidence',SOURCE_ACCESS_EVIDENCE_KEYS,true);checkArray('predicateEvidence',PREDICATE_EVIDENCE_KEYS,true);checkArray('leases',LEASE_KEYS);
 for(const s of Array.isArray(state.selectedInputs)?state.selectedInputs:[])if(!selectedShape(s))bad('selectedInputs.guardBindings');
 for(const x of Array.isArray(state.sourceAccessEvidence)?state.sourceAccessEvidence:[])if(!sourceAccessEvidenceShape(x))bad('sourceAccessEvidence');for(const x of Array.isArray(state.predicateEvidence)?state.predicateEvidence:[])if(!predicateEvidenceShape(x))bad('predicateEvidence');
 if(!Array.isArray(state.phaseEvidence))bad('phaseEvidence');else for(const e of state.phaseEvidence){if(!keys(e,PHASE_EVIDENCE_KEYS)||!Array.isArray(e.inputBindings)||!Array.isArray(e.selectedInputs)){bad('phaseEvidence');continue;}for(const b of e.inputBindings)if(!expectedShape(b))bad('phaseEvidence.inputBindings');for(const s of e.selectedInputs)if(!selectedShape(s))bad('phaseEvidence.selectedInputs');if(e.sourceAccessBindings!==undefined&&(!Array.isArray(e.sourceAccessBindings)||!e.sourceAccessBindings.every(sourceAccessEvidenceShape)))bad('phaseEvidence.sourceAccessBindings');if(e.predicateBindings!==undefined&&(!Array.isArray(e.predicateBindings)||!e.predicateBindings.every(predicateEvidenceShape)))bad('phaseEvidence.predicateBindings');}
 for(const s of Array.isArray(state.selectedInputs)?state.selectedInputs:[])if(obj(s)&&((s.available!==undefined&&typeof s.available!=='boolean')||(s.accepted!==undefined&&typeof s.accepted!=='boolean')||(s.evidence!==undefined&&!text(s.evidence))||(s.reviewRef!==undefined&&!text(s.reviewRef))))bad('selectedInputs optional receipt fields');
 if(!obj(state.resourceBindings)||!Object.keys(state.resourceBindings).every(k=>TEMPLATES.includes(k)))bad('resourceBindings');else for(const [template,binding] of Object.entries(state.resourceBindings))if(!validBinding(template,binding))bad('resourceBindings.'+template);
 for(const l of Array.isArray(state.leases)?state.leases:[])if(obj(l)){if(TEMPLATES.includes(l.key)){if(!validBinding(l.key,l.scope))bad('lease.scope');}else if(l.scope!==undefined)bad('lease.scope');}
 if(state.resolvedAuthorities!==undefined&&(!Array.isArray(state.resolvedAuthorities)||!state.resolvedAuthorities.every(text)))bad('resolvedAuthorities');
 if(state.writerContext!==undefined&&!writerContextShape(state.writerContext))bad('writerContext');
 return [...new Set(errors)];
}
const REFERENCE_KEYS=['number','workId','title','kind','state','stateReason','closedAt','url','outputs','limits','qualification','sourceRef','observedAt','absorbedHistoricalContext'];
const ABSORBED_KEYS=['scheduled','readOnly','executable','currentOutput','nativeIssueEdge','closureDisposition','closureCommentUrl','dataRejected','acceptedAsCompleted','sourceIssueUpdatedAt','existingLaboratoryScope','transferredRemainingScope','contextArtifacts'];
const LAB_SCOPE_KEYS=['calibrationHoldoutFormatAlreadyExisted','stecklerCases','juelichCases','totalCases','region13Accepted','duplicateMeasurementRequired'];
const CONTEXT_ARTIFACT_KEYS=['artifact','producerIssue','path','pathKind','proposed','state','satisfied','role','producerKind','wholeIssueAcceptance','availability','attribution','contextConsumers'];
const CONTEXT_AVAILABILITY_KEYS=['state','access','teamAccessible','sourceRef','capturedLocalDigest','observedAt','requiresAvailabilityArtifact','sourceEvidence'];
const HISTORICAL_ATTRIBUTION_KEYS=['sourceIssue','issueState','issueStateReason','closedAt','closureDisposition','closureCommentUrl','dataRejected','completedProducerClaim','currentOutput','legacyProducerPhaseLabel','legacyPhaseIsExecutionClaim','executable','existingLaboratoryScope','transferredRemainingScope'];
const CONTEXT_CONSUMER_KEYS=['issue','readWhen','kind','executable','acceptanceInput','nativeIssueEdge'];
const ABSORBED_124_CLOSED_AT='2026-09-12T05:23:52Z',ABSORBED_124_OBSERVED_AT='2026-09-12T09:39:41Z',ABSORBED_124_COMMENT='https://github.com/xrlab-dau/CHOOGuard/issues/124#issuecomment-5643741218';
function labScopeShape(x){return keys(x,LAB_SCOPE_KEYS)&&x.calibrationHoldoutFormatAlreadyExisted===true&&integer(x.stecklerCases)&&integer(x.juelichCases)&&x.totalCases===x.stecklerCases+x.juelichCases&&x.region13Accepted===false&&x.duplicateMeasurementRequired===false;}
function transferShape(x){return obj(x)&&Object.keys(x).length>0&&Object.keys(x).every(text)&&Object.values(x).every(integer);}
function absorbedHistoricalErrors(r,g,map){const errors=[],bad=m=>errors.push(m),h=r.absorbedHistoricalContext;if(!keys(h,ABSORBED_KEYS)){bad('unknown or missing absorbed-history field');return errors;}if(r.state!=='CLOSED'||r.stateReason!=='NOT_PLANNED'||r.kind!=='absorbed_superseded_context'||!Number.isFinite(Date.parse(r.closedAt))||!issueURL(r.url,r.number)||!Array.isArray(r.outputs)||r.outputs.length)bad('absorbed history must remain CLOSED/NOT_PLANNED with no outputs');if(!Number.isFinite(Date.parse(r.observedAt))||r.observedAt!==g.snapshot?.capturedAt||!Number.isFinite(Date.parse(h.sourceIssueUpdatedAt))||Date.parse(h.sourceIssueUpdatedAt)>Date.parse(r.observedAt))bad('absorbed history needs exact frozen observation distinct from source update');if(h.scheduled!==false||h.readOnly!==true||h.executable!==false||h.currentOutput!==false||h.nativeIssueEdge!==false||h.closureDisposition!=='absorption_transfer'||h.dataRejected!==false||h.acceptedAsCompleted!==false||!text(h.closureCommentUrl)||!h.closureCommentUrl.startsWith(r.url+'#issuecomment-'))bad('absorbed history disposition/read-only boundary');if(!labScopeShape(h.existingLaboratoryScope)||!transferShape(h.transferredRemainingScope))bad('absorbed history scope/transfer shape');if(!Array.isArray(h.contextArtifacts)||!h.contextArtifacts.length||!unique(h.contextArtifacts.map(a=>a?.artifact)))bad('absorbed history context artifacts');for(const [scope,target] of Object.entries(obj(h.transferredRemainingScope)?h.transferredRemainingScope:{}))if(!text(scope)||!integer(target)||!map.has(target))bad('absorbed transfer target is not an active issue');for(const a of Array.isArray(h.contextArtifacts)?h.contextArtifacts:[]){if(!keys(a,CONTEXT_ARTIFACT_KEYS)||!text(a.artifact)||a.producerIssue!==null||!safePath(a.path)||a.pathKind!=='file'||a.proposed!==false||a.state!=='local_unpublished_absorbed_context_unqualified'||a.satisfied!==false||a.role!=='absorbed_historical_context_not_produced'||a.producerKind!=='historical_attribution_only_not_current_output'||a.wholeIssueAcceptance!==false)bad('invalid absorbed context artifact identity');const v=a.availability;if(!keys(v,CONTEXT_AVAILABILITY_KEYS)||v.state!=='local_unpublished_context_only'||v.access!=='local_unpublished'||v.teamAccessible!==false||v.sourceRef!==null||!digest(v.capturedLocalDigest)||v.observedAt!==r.observedAt||!text(v.requiresAvailabilityArtifact)||!Array.isArray(v.sourceEvidence)||!v.sourceEvidence.length||!v.sourceEvidence.every(text))bad('invalid absorbed context availability');const availabilityProvider=graphArtifact(g,v?.requiresAvailabilityArtifact);if(!availabilityProvider||!map.has(availabilityProvider.issue)||availabilityProvider.output.producerPhase!=='accept')bad('absorbed context availability artifact is not an active accepted-phase declaration');const at=a.attribution;if(!keys(at,HISTORICAL_ATTRIBUTION_KEYS)||at.sourceIssue!==r.number||at.issueState!=='CLOSED'||at.issueStateReason!=='NOT_PLANNED'||at.closedAt!==r.closedAt||at.closureDisposition!==h.closureDisposition||at.closureCommentUrl!==h.closureCommentUrl||at.dataRejected!==false||at.completedProducerClaim!==false||at.currentOutput!==false||at.legacyProducerPhaseLabel!=='candidate'||at.legacyPhaseIsExecutionClaim!==false||at.executable!==false||canonical(at.existingLaboratoryScope)!==canonical(h.existingLaboratoryScope)||canonical(at.transferredRemainingScope)!==canonical(h.transferredRemainingScope))bad('invalid absorbed historical attribution');if(!Array.isArray(a.contextConsumers)||!a.contextConsumers.length||!unique(a.contextConsumers.map(c=>JSON.stringify([c?.issue,c?.readWhen,c?.kind])))||a.contextConsumers.some(c=>!keys(c,CONTEXT_CONSUMER_KEYS)||!map.has(c.issue)||c.readWhen!=='prepare'||c.kind!=='absorbed_historical_snapshot_context'||c.executable!==false||c.acceptanceInput!==false||c.nativeIssueEdge!==false))bad('invalid absorbed read-only context consumers');}if(r.number===124){if(r.closedAt!==ABSORBED_124_CLOSED_AT||r.observedAt!==ABSORBED_124_OBSERVED_AT||h.sourceIssueUpdatedAt!==ABSORBED_124_CLOSED_AT||h.closureCommentUrl!==ABSORBED_124_COMMENT||canonical(h.transferredRemainingScope)!==canonical({fire13Region:122,crowd13Region:125})||canonical(h.existingLaboratoryScope)!==canonical({calibrationHoldoutFormatAlreadyExisted:true,stecklerCases:3,juelichCases:13,totalCases:16,region13Accepted:false,duplicateMeasurementRequired:false}))bad('#124 absorbed closure/transfer facts drifted');const expected=new Map([['artifact:124:historical-calibration-record:candidate','docs/evidence/foundation/2026-09-09-fire-core.json'],['artifact:124:historical-thermal-bounded-baseline:candidate','docs/evidence/foundation/2026-09-09-simulation-components.json']]);if(h.contextArtifacts.length!==expected.size||h.contextArtifacts.some(a=>expected.get(a.artifact)!==a.path))bad('#124 context artifact attribution drifted');}return [...new Set(errors)];}
const ROOT_KEYS=['schemaVersion','classification','project','generatedAt','snapshot','reviewScope','requirements','items','references','edges','ontology','limitations','review'];
const ITEM_KEYS=['number','workId','title','kind','parent','outcome','nonGoals','truth','context','codePointers','inputs','inputQualifiers','outputs','writeScope','phaseWriteScopes','artifactOutputBindings','checks','checkEvidenceMappings','generatedOutputClosure','acceptance','stopConditions','handoff','reviewNotes','hardPredecessors','hardSuccessors','oneOfInputs','guardPredicates','relationshipDeclarations','requiredLocks','resourceBindings','board','assigneePolicy','claimEvidence','authorityBlock','related','requirements'];
const OPTIONAL_ITEM_KEYS=new Set(['claimEvidence','authorityBlock','inputQualifiers','relationshipDeclarations','checkEvidenceMappings','generatedOutputClosure']);
const RELATIONSHIP_TYPES=['parallel_with','conflicts_with'];
const RELATIONSHIP_DECLARATION_KEYS=['parallel_with','conflicts_with'];
const RELATIONSHIP_BLOCK_KEYS=['evaluatedPairs','items','reasonedNone'];
const RELATIONSHIP_ITEM_KEYS=['issue','fromPhase','toPhase','reason'];
const relationshipItemShape=(item,self)=>keys(item,RELATIONSHIP_ITEM_KEYS)&&integer(item.issue)&&item.issue!==self&&PHASES.includes(item.fromPhase)&&PHASES.includes(item.toPhase)&&text(item.reason);
const relationshipPairKey=item=>JSON.stringify([item?.issue,item?.fromPhase,item?.toPhase]);
function relationshipDeclarationsShape(x,self){if(!keys(x,RELATIONSHIP_DECLARATION_KEYS))return false;for(const relation of RELATIONSHIP_TYPES){const block=x[relation];if(!keys(block,RELATIONSHIP_BLOCK_KEYS)||!Array.isArray(block.evaluatedPairs)||!block.evaluatedPairs.length||!unique(block.evaluatedPairs.map(relationshipPairKey))||!block.evaluatedPairs.every(item=>relationshipItemShape(item,self))||!Array.isArray(block.items)||!unique(block.items.map(relationshipPairKey))||!block.items.every(item=>relationshipItemShape(item,self))||!(block.reasonedNone===null||text(block.reasonedNone)))return false;const evaluated=new Set(block.evaluatedPairs.map(relationshipPairKey));if(block.items.some(item=>!evaluated.has(relationshipPairKey(item))))return false;if(block.items.length===0&&!text(block.reasonedNone))return false;if(block.items.length>0&&block.reasonedNone!==null)return false;}return true;}
const INPUT_QUALIFIER_KEYS=['inputId','qualification','targets','notRequiredFor'];
const INPUT_QUALIFIER_TARGET_KEYS=['artifact','phase','use'];
function inputQualifiersShape(x,item){if(!Array.isArray(x)||!x.length||!unique(x.map(v=>v?.inputId)))return false;for(const q of x){if(!keys(q,INPUT_QUALIFIER_KEYS)||!text(q.inputId)||!text(q.qualification)||!Array.isArray(q.targets)||!q.targets.length||!unique(q.targets.map(v=>JSON.stringify([v?.artifact,v?.phase])))||!Array.isArray(q.notRequiredFor)||!q.notRequiredFor.length||!unique(q.notRequiredFor)||!q.notRequiredFor.every(text)||item.inputs.some(i=>i.id===q.inputId))return false;for(const target of q.targets){if(!keys(target,INPUT_QUALIFIER_TARGET_KEYS)||!text(target.artifact)||!ARTIFACT_PHASES.includes(target.phase)||!text(target.use))return false;const output=item.outputs.find(o=>o.id===target.artifact);if(!output||output.producerPhase!==target.phase)return false;}}return true;}
function topologyInputQualifiersShape(x,issue,record,registry){return inputQualifiersShape(x,{outputs:(issue.produces||[]).map(id=>({id,producerPhase:registry.get(id)?.producerPhase})),inputs:(record?.inputs||[]).map(input=>({consumerPhase:input.consumerPhase??(input.fromIssue==null?'prepare':undefined),...input}))});}
const CHECK_KEYS=['id','command','inspection','expected','negativeCase','evidence','proposed','phases','evidenceByPhase','resultProtocol'];
const CHECK_MAPPING_KEYS=['rawCheckEvidence','candidate','accept'];
const CHECK_MAPPING_PHASE_KEYS=['artifact','indexPath','rawReceiptDirectory'];
const ISSUE13_CHECK_MAPPING_KEYS=['checkId','candidate','accept'];
const exactKeysCore=(value,expected)=>keys(value,expected)&&expected.every(key=>Object.hasOwn(value,key));
const nonemptyTextList=value=>Array.isArray(value)&&value.length>0&&unique(value)&&value.every(text);
function issue13HandoffShape(h){if(!exactKeysCore(h,['consumer','exactReadSet','doNotAssume','authorityRule','ownership','phaseAcceptance','protectedPathAndHashCoverage','delegatedValidation','sensitiveReturn','returnFields','limits'])||!nonemptyTextList(h.consumer)||!nonemptyTextList(h.exactReadSet)||!nonemptyTextList(h.doNotAssume)||!text(h.authorityRule)||!nonemptyTextList(h.returnFields)||!text(h.limits))return false;const o=h.ownership;if(!exactKeysCore(o,['issue13Candidate','issue13Accept','issue43','issue44','collision'])||!Object.values(o).every(text))return false;const p=h.phaseAcceptance;if(!exactKeysCore(p,['candidate','accept'])||!exactKeysCore(p.candidate,['reportableUnknowns','blocking','passRule'])||!nonemptyTextList(p.candidate.reportableUnknowns)||!nonemptyTextList(p.candidate.blocking)||!text(p.candidate.passRule)||!exactKeysCore(p.accept,['nonblockingUnknowns','blocking','passRule'])||!nonemptyTextList(p.accept.nonblockingUnknowns)||!nonemptyTextList(p.accept.blocking)||!text(p.accept.passRule))return false;const c=h.protectedPathAndHashCoverage;if(!exactKeysCore(c,['protectedRoots','ownerPredicate','requiredPatterns','bootstrapExactDigests','manifestPredicate'])||!nonemptyTextList(c.protectedRoots)||!text(c.ownerPredicate)||!nonemptyTextList(c.requiredPatterns)||!obj(c.bootstrapExactDigests)||Object.keys(c.bootstrapExactDigests).length!==5||!Object.values(c.bootstrapExactDigests).every(digest)||!text(c.manifestPredicate))return false;const d=h.delegatedValidation;if(!exactKeysCore(d,['executionOwner','issue13ExecutionAuthority','selectedSourceRef','prospectiveCommand','concreteWriteClosure','requiredRunnerFacts','resultSemantics','knownLimitsRef'])||!text(d.executionOwner)||d.issue13ExecutionAuthority!==false||!text(d.selectedSourceRef)||!text(d.prospectiveCommand)||!nonemptyTextList(d.concreteWriteClosure)||!nonemptyTextList(d.requiredRunnerFacts)||!text(d.resultSemantics)||!text(d.knownLimitsRef))return false;const s=h.sensitiveReturn;return exactKeysCore(s,['publicAllowed','publicForbidden','privateStop','unsafeSanitization'])&&nonemptyTextList(s.publicAllowed)&&nonemptyTextList(s.publicForbidden)&&text(s.privateStop)&&text(s.unsafeSanitization);}
function receiptCheckTargetShape(target,phase,item,allowDelegated=false){const expected=allowDelegated?['artifact','indexPath','runDirectory','delegatedInput']:['artifact','indexPath','runDirectory'];if(!exactKeysCore(target,expected)||!text(target.artifact)||!safePath(target.indexPath)||!text(target.runDirectory)||!target.runDirectory.endsWith('/')||!safePath(target.runDirectory.slice(0,-1)))return false;const output=item.outputs.find(o=>o.id===target.artifact&&o.producerPhase===phase&&o.path===target.indexPath),binding=item.artifactOutputBindings?.[phase]?.find(b=>b.artifact===target.artifact&&b.path===target.indexPath);if(!output||!binding||binding.receiptStorage?.mode!=='separate_immutable_raw_records'||binding.receiptStorage.rawScopePath!==target.runDirectory+'**')return false;const rawScope=item.phaseWriteScopes?.[phase]?.find(s=>s.path===target.runDirectory);if(!rawScope||rawScope.physicalBinding!==binding.receiptStorage.rawLockId)return false;if(allowDelegated&&!item.inputs.some(input=>input.id===target.delegatedInput&&input.consumerPhase===phase))return false;return true;}
function oracleCheckTargetShape(target,phase,item,requireInputs=false){const expected=requireInputs?['artifact','policyPath','evidenceArtifact','requiredInputs']:['artifact','policyPath','evidenceArtifact'];if(!exactKeysCore(target,expected)||!text(target.artifact)||!safePath(target.policyPath)||!text(target.evidenceArtifact))return false;const policy=item.outputs.find(o=>o.id===target.artifact&&o.producerPhase===phase&&o.path===target.policyPath),evidence=item.outputs.find(o=>o.id===target.evidenceArtifact&&o.producerPhase===phase),binding=item.artifactOutputBindings?.[phase]?.find(b=>b.artifact===target.evidenceArtifact);if(!policy||!evidence||!binding||binding.receiptStorage?.mode!=='separate_immutable_raw_records')return false;if(requireInputs&&(!Array.isArray(target.requiredInputs)||!target.requiredInputs.length||!unique(target.requiredInputs)||target.requiredInputs.some(id=>!item.inputs.some(input=>input.id===id&&input.consumerPhase===phase))))return false;return true;}
const ISSUE13_EXACT_CHECK_MAPPINGS=JSON.parse('[{"checkId":"M0-00-C01-SOURCE-SELECTION","candidate":{"artifact":"artifact:13:M0-00-review-record:candidate","indexPath":"docs/evidence/M0-00/review-record-candidate-index.json","runDirectory":"docs/evidence/M0-00/review-record-candidate-runs/"},"accept":{"artifact":"artifact:13:M0-00-review-record:accept","indexPath":"docs/evidence/M0-00/review-record-accept-index.json","runDirectory":"docs/evidence/M0-00/review-record-accept-runs/"}},{"checkId":"M0-00-C02-STATIC-WRITE-CLOSURE","candidate":{"artifact":"artifact:13:M0-00-review-record:candidate","indexPath":"docs/evidence/M0-00/review-record-candidate-index.json","runDirectory":"docs/evidence/M0-00/review-record-candidate-runs/"},"accept":{"artifact":"artifact:13:M0-00-review-record:accept","indexPath":"docs/evidence/M0-00/review-record-accept-index.json","runDirectory":"docs/evidence/M0-00/review-record-accept-runs/","delegatedInput":"artifact:44:R-04-evidence:accept"}},{"checkId":"M0-00-C03-SENSITIVE-RETURN","candidate":{"artifact":"artifact:13:M0-00-review-record:candidate","indexPath":"docs/evidence/M0-00/review-record-candidate-index.json","runDirectory":"docs/evidence/M0-00/review-record-candidate-runs/"},"accept":{"artifact":"artifact:13:M0-00-review-record:accept","indexPath":"docs/evidence/M0-00/review-record-accept-index.json","runDirectory":"docs/evidence/M0-00/review-record-accept-runs/","delegatedInput":"artifact:44:R-04-evidence:accept"}},{"checkId":"M0-00-C04-CANDIDATE-ORACLE","candidate":{"artifact":"artifact:13:M0-00-policy-baseline:candidate","policyPath":"docs/team/M0-00/policy-baseline.json","evidenceArtifact":"artifact:13:M0-00-review-record:candidate"},"accept":null},{"checkId":"M0-00-C05-ACCEPT-ORACLE","candidate":null,"accept":{"artifact":"artifact:13:M0-00-policy-baseline:accept","policyPath":"docs/team/M0-00/policy-baseline.json","evidenceArtifact":"artifact:13:M0-00-review-record:accept","requiredInputs":["artifact:43:R-03-governance-diff:accept","artifact:44:R-04-evidence:accept"]}}]');
function issue13CheckEvidenceMappingsShape(mappings,item){if(canonical(mappings)!==canonical(ISSUE13_EXACT_CHECK_MAPPINGS))return false;const ids=['M0-00-C01-SOURCE-SELECTION','M0-00-C02-STATIC-WRITE-CLOSURE','M0-00-C03-SENSITIVE-RETURN','M0-00-C04-CANDIDATE-ORACLE','M0-00-C05-ACCEPT-ORACLE'];if(item.number!==13||!Array.isArray(mappings)||mappings.length!==ids.length||!unique(mappings.map(x=>x?.checkId))||!ids.every(id=>mappings.some(x=>x?.checkId===id)))return false;for(const mapping of mappings){if(!exactKeysCore(mapping,ISSUE13_CHECK_MAPPING_KEYS)||!item.checks.some(check=>check.id===mapping.checkId))return false;if(ids.slice(0,3).includes(mapping.checkId)){if(!receiptCheckTargetShape(mapping.candidate,'candidate',item)||!receiptCheckTargetShape(mapping.accept,'accept',item,mapping.checkId!=='M0-00-C01-SOURCE-SELECTION'))return false;}else if(mapping.checkId==='M0-00-C04-CANDIDATE-ORACLE'){if(mapping.accept!==null||!oracleCheckTargetShape(mapping.candidate,'candidate',item,false))return false;}else if(mapping.candidate!==null||!oracleCheckTargetShape(mapping.accept,'accept',item,true))return false;}return true;}
function checkEvidenceMappingsShape(mappings,item){if(Array.isArray(mappings)&&mappings.some(mapping=>Object.hasOwn(mapping??{},'checkId')))return issue13CheckEvidenceMappingsShape(mappings,item);if(!Array.isArray(mappings)||!mappings.length||!unique(mappings.map(x=>x?.rawCheckEvidence)))return false;for(const mapping of mappings){if(!keys(mapping,CHECK_MAPPING_KEYS)||!text(mapping.rawCheckEvidence)||!item.checks.some(c=>c.evidence===mapping.rawCheckEvidence))return false;for(const phase of ARTIFACT_PHASES){const target=mapping[phase];if(!keys(target,CHECK_MAPPING_PHASE_KEYS)||!text(target.artifact)||!safePath(target.indexPath)||!text(target.rawReceiptDirectory)||!target.rawReceiptDirectory.endsWith('/')||!safePath(target.rawReceiptDirectory.slice(0,-1)))return false;const output=item.outputs.find(o=>o.id===target.artifact&&o.producerPhase===phase&&o.path===target.indexPath),binding=item.artifactOutputBindings?.[phase]?.find(b=>b.artifact===target.artifact&&b.path===target.indexPath);if(!output||!binding||binding.receiptStorage?.mode!=='separate_immutable_raw_records'||binding.receiptStorage.rawScopePath!==target.rawReceiptDirectory+'**')return false;const rawScope=item.phaseWriteScopes?.[phase]?.find(s=>s.path===target.rawReceiptDirectory);if(!rawScope||rawScope.physicalBinding!==binding.receiptStorage.rawLockId)return false;}}return true;}
function topologyCheckEvidenceMappingsShape(x,issue,record,registry){return checkEvidenceMappingsShape(x,{number:issue.number,checks:record?.checks||[],inputs:(record?.inputs||[]).map(input=>({...input,consumerPhase:input.consumerPhase??'accept'})),outputs:(issue.produces||[]).map(id=>({id,path:registry.get(id)?.path,producerPhase:registry.get(id)?.producerPhase})),artifactOutputBindings:issue.artifactOutputBindings,phaseWriteScopes:issue.phaseWriteScopes});}
const CHECK_EVIDENCE_PHASE_KEYS=['artifact','path'];
const CHECK_PROTOCOL_KEYS=['states','receiptFields','notRunReasonRequiredWhen'];
const CHECK_PROTOCOL_STATES=['PASS','FAIL','NOT_RUN'];
const CHECK_PROTOCOL_FIELDS=['artifactPath','artifactDigest','provenance','thresholdEvaluation','result','notRunReason'];
const checkResultProtocolShape=x=>keys(x,CHECK_PROTOCOL_KEYS)&&exactList(x.states,CHECK_PROTOCOL_STATES)&&exactList(x.receiptFields,CHECK_PROTOCOL_FIELDS)&&x.notRunReasonRequiredWhen==='NOT_RUN';
function checkEvidenceByPhaseShape(check,item){if(check.evidenceByPhase===undefined&&check.resultProtocol===undefined)return true;if(check.evidenceByPhase===undefined||!checkResultProtocolShape(check.resultProtocol))return false;if(!obj(check.evidenceByPhase)||!Array.isArray(check.phases)||Object.keys(check.evidenceByPhase).sort().join(',')!==[...check.phases].sort().join(','))return false;for(const phase of check.phases){if(!ARTIFACT_PHASES.includes(phase))return false;const e=check.evidenceByPhase[phase];if(!keys(e,CHECK_EVIDENCE_PHASE_KEYS)||!text(e.artifact)||!safePath(e.path))return false;const output=item.outputs.find(o=>o.id===e.artifact&&o.producerPhase===phase&&o.path===e.path),binding=item.artifactOutputBindings?.[phase]?.find(b=>b.artifact===e.artifact&&b.path===e.path);if(!output||!binding||!binding.receiptStorage||!receiptStorageShape(binding.receiptStorage))return false;}return true;}
const BOARD_KEYS=['status','phase','reason','capturedProjectStatus'];
const CAPTURED_PROJECT_KEYS=['value','observedAt','sourceSnapshotCapturedAt','sourceIssueUpdatedAt','source'];
function capturedProjectStatusShape(x){if(!keys(x,CAPTURED_PROJECT_KEYS)||!(x.value===null||STATUSES.includes(x.value))||!(x.observedAt===null||Number.isFinite(Date.parse(x.observedAt)))||!Number.isFinite(Date.parse(x.sourceSnapshotCapturedAt))||!Number.isFinite(Date.parse(x.sourceIssueUpdatedAt))||!text(x.source))return false;if(Date.parse(x.sourceIssueUpdatedAt)>Date.parse(x.sourceSnapshotCapturedAt))return false;return x.value===null?x.observedAt===null:x.observedAt===x.sourceSnapshotCapturedAt;}
function inspect(g){const errors=[],fail=(p,m)=>errors.push(`${p}: ${m}`);if(!obj(g))return {valid:false,errors:['graph must be object']};if(!keys(g,ROOT_KEYS))fail('graph','unknown field');for(const k of ROOT_KEYS)if(!(k in g))fail('graph',`missing ${k}`);if(g.schemaVersion!==1||g.classification!=='PUBLIC_PROJECT_CONTEXT')fail('graph','schema/classification');if(!content(g.project)||!obj(g.snapshot)||!obj(g.ontology)||!obj(g.review)||!Array.isArray(g.limitations)||!g.limitations.every(text)||!/^\d{4}-\d{2}-\d{2}T/.test(g.generatedAt)||!Number.isFinite(Date.parse(g.generatedAt)))fail('graph','invalid metadata');if(g.snapshot?.sourceRef!==undefined&&!sha(g.snapshot.sourceRef))fail('snapshot','sourceRef');for(const k of ['items','references','edges','requirements'])if(!Array.isArray(g[k])||!g[k].every(obj))fail(k,'array of objects required');if(errors.length)return {valid:false,errors};if(!g.items.length||g.items.length>500||g.references.length>500||g.edges.length>20000)fail('graph','size limit');
 const map=new Map(g.items.map(i=>[i.number,i])),external=new Map(g.references.map(r=>[r.number,r])),all=new Map([...external,...map]);const nodeIDs=new Set([...all.keys()].map(ID).concat(g.requirements.map(r=>r.id)));
 for(const [n,a] of [['issue number',g.items.map(i=>i.number)],['Work ID',g.items.map(i=>i.workId)],['reference',g.references.map(r=>r.number)],['requirement ID',g.requirements.map(r=>r.id)]])if(!unique(a))fail(n,'duplicates');
 if(!Array.isArray(g.reviewScope?.openIssueNumbers)||!unique(g.reviewScope.openIssueNumbers)||!g.reviewScope.openIssueNumbers.every(integer)||g.reviewScope.openIssueNumbers.toSorted((a,b)=>a-b).join()!==[...map.keys()].sort((a,b)=>a-b).join())fail('reviewScope','coverage');
 const absorbedArtifactIDs=new Set();for(const r of g.references){if(!keys(r,REFERENCE_KEYS)||!integer(r.number)||map.has(r.number)||r.state!=='CLOSED'||!['COMPLETED','NOT_PLANNED'].includes(r.stateReason))fail('reference','explicit CLOSED and exact stateReason required');for(const k of ['workId','title','kind','closedAt','qualification','observedAt'])if(r[k]!==undefined&&!text(r[k]))fail('reference',`invalid optional ${k}`);if(r.url!==undefined&&!issueURL(r.url,r.number))fail('reference','wrong URL');if(r.sourceRef!==undefined&&!sha(r.sourceRef))fail('reference','invalid sourceRef');if(r.outputs!==undefined&&(!Array.isArray(r.outputs)||!r.outputs.every(obj)))fail('reference','invalid outputs');if(r.number===124&&r.absorbedHistoricalContext===undefined)fail('reference','#124 requires typed absorbedHistoricalContext');if(r.absorbedHistoricalContext!==undefined){for(const e of absorbedHistoricalErrors(r,g,map))fail(ID(r.number),e);for(const a of r.absorbedHistoricalContext?.contextArtifacts||[]){if(absorbedArtifactIDs.has(a?.artifact))fail(ID(r.number),'duplicate absorbed context artifact');absorbedArtifactIDs.add(a?.artifact);}}}
 const spellings=new Map();const pathCheck=(p,glob,where)=>{if(!safePath(p,glob)){fail(where,'ambiguous path');return;}const k=p.toLowerCase();if(spellings.has(k)&&spellings.get(k)!==p)fail(where,'case alias');else spellings.set(k,p);};const outputIDs=new Set();
 for(const i of g.items){const p=ID(i.number);if(!keys(i,ITEM_KEYS)||!integer(i.number))fail(p,'unknown field/number');for(const k of ITEM_KEYS.filter(k=>!OPTIONAL_ITEM_KEYS.has(k)))if(!(k in i))fail(p,`missing ${k}`);for(const k of ['workId','title','outcome'])if(!text(i[k]))fail(p,`blank ${k}`);if(!['goal','work_item'].includes(i.kind))fail(p,'kind');for(const k of ['nonGoals','truth','context','inputs','outputs','writeScope','checks','acceptance','stopConditions'])if(!Array.isArray(i[k])||!i[k].length)fail(p,`nonempty ${k}`);for(const k of ['codePointers','hardPredecessors','hardSuccessors','oneOfInputs','guardPredicates','requiredLocks','resourceBindings','related','requirements'])if(!Array.isArray(i[k]))fail(p,`${k} array`);if(!(content(i.handoff)||(i.number===13&&issue13HandoffShape(i.handoff)))||!content(i.reviewNotes))fail(p,'handoff/reviewNotes');if(!Array.isArray(i.context)||i.context.length<2||i.context.length>4)fail(p,'context 2..4');if(!Array.isArray(i.acceptance)||i.acceptance.length<4||i.acceptance.length>7||!i.acceptance.every(text))fail(p,'acceptance 4..7');const nullBoard=i.board?.status===null&&i.board?.phase===null&&i.board?.capturedProjectStatus?.value===null&&i.board?.capturedProjectStatus?.observedAt===null;if(!keys(i.board,BOARD_KEYS)||!(STATUSES.includes(i.board.status)||nullBoard)||!text(i.board.reason)||!(PHASES.includes(i.board.phase)||nullBoard)||(i.board.capturedProjectStatus!==undefined&&!capturedProjectStatusShape(i.board.capturedProjectStatus)))fail(p,'explicit board status/phase/reason or provenance-backed absent Project row');if(!['unassigned_until_claim','preserve_evidenced_claim'].includes(i.assigneePolicy)||(i.assigneePolicy==='preserve_evidenced_claim'&&!content(i.claimEvidence)))fail(p,'assignee/claim evidence');if(i.authorityBlock!==undefined&&!(text(i.authorityBlock)||(keys(i.authorityBlock,['id','phases'])&&text(i.authorityBlock.id)&&phaseList(i.authorityBlock.phases))))fail(p,'authorityBlock');if(i.parent!==null&&(!integer(i.parent)||!map.has(i.parent)))fail(p,'parent');
  for(const t of i.truth||[])if(!obj(t)||!LEVELS.includes(t.level)||!text(t.text)||!text(t.source))fail(p,'truth');for(const c of i.context||[]){if(!obj(c)){fail(p,'context object');continue;}pathCheck(c.path,false,p);if(!text(c.selector)||!text(c.proves)||!text(c.limits)||!['published','local_unpublished','proposed'].includes(c.availability)||!PHASES.includes(c.readWhen))fail(p,'context fields');if(c.sourceRef!==undefined&&!sha(c.sourceRef))fail(p,'context ref');if(c.availability==='local_unpublished'&&c.readWhen==='prepare'&&i.number!==120)fail(p,'unpublished preparation source');}
  for(const c of i.codePointers||[])if(text(c))pathCheck(c,false,p);else if(obj(c))pathCheck(c.path,false,p);else fail(p,'codePointer');
  for(const x of i.inputs||[]){if(!keys(x,['id','fromIssue','qualification','consumerPhase','producerPhase','expectedSourceRef','expectedDigest','expectedScope'])||!text(x.id)||!text(x.qualification)||!PHASES.includes(x.consumerPhase)||(x.fromIssue!==null&&(!integer(x.fromIssue)||!ARTIFACT_PHASES.includes(x.consumerPhase)||!ARTIFACT_PHASES.includes(x.producerPhase)))||(x.fromIssue===null&&x.producerPhase!==undefined))fail(p,'input/phase');if(x.expectedSourceRef!==undefined&&!sha(x.expectedSourceRef))fail(p,'input ref');if(x.expectedDigest!==undefined&&!digest(x.expectedDigest))fail(p,'input digest');if(x.expectedScope!==undefined&&!text(x.expectedScope))fail(p,'input scope');}
  if(Array.isArray(i.inputs)&&!unique(i.inputs.map(x=>JSON.stringify([x?.id,x?.fromIssue,x?.consumerPhase]))))fail(p,'duplicate input');
  for(const w of i.writeScope||[]){if(!keys(w,['path','mode','lock','phases'])){fail(p,'write scope object/field');continue;}pathCheck(w.path,true,p);if(!['exclusive','isolated_proposal','read_only'].includes(w.mode)||!phaseList(w.phases)||(w.lock!==undefined&&!text(w.lock)))fail(p,'write mode/phases/optional lock');if(w.mode==='exclusive'&&(!text(w.lock)||!i.requiredLocks?.includes(w.lock)))fail(p,'exclusive lock');if(w.phases?.includes('prepare')&&!['isolated_proposal','read_only'].includes(w.mode))fail(p,'prepare cannot write canonical exclusive surface');}
  if(!phaseWriteScopesShape(i.phaseWriteScopes))fail(p,'invalid lossless phaseWriteScopes');else for(const phase of PHASES)for(const w of i.phaseWriteScopes[phase]){const q=phase==='prepare'?w.path.slice('{proposalRoot}/'.length):w.path,path=trimTerminalSlash(q);if(!path.includes('{'))pathCheck(path,true,p);}
  if(!artifactOutputBindingsShape(i.artifactOutputBindings))fail(p,'invalid lossless artifactOutputBindings');
  if(i.relationshipDeclarations!==undefined&&!relationshipDeclarationsShape(i.relationshipDeclarations,i.number))fail(p,'relationshipDeclarations');
  if(i.inputQualifiers!==undefined&&!inputQualifiersShape(i.inputQualifiers,i))fail(p,'inputQualifiers');
  if(i.checkEvidenceMappings!==undefined&&!checkEvidenceMappingsShape(i.checkEvidenceMappings,i))fail(p,'checkEvidenceMappings');
  for(const c of i.checks||[])if(!obj(c)||!keys(c,CHECK_KEYS)||(c.id!==undefined&&!text(c.id))||!(text(c.command)||text(c.inspection))||!text(c.expected)||!text(c.negativeCase)||!text(c.evidence)||(c.proposed!==undefined&&typeof c.proposed!=='boolean')||(c.phases!==undefined&&!phaseList(c.phases))||!checkEvidenceByPhaseShape(c,i))fail(p,'check');if(!unique((i.checks||[]).filter(c=>c.id!==undefined).map(c=>c.id)))fail(p,'duplicate check id');
  for(const k of ['nonGoals','stopConditions','requiredLocks'])if(Array.isArray(i[k])&&(!i[k].every(text)||!unique(i[k])))fail(p,`invalid ${k}`);if(i.generatedOutputClosure!==undefined&&(!Array.isArray(i.generatedOutputClosure)||!unique(i.generatedOutputClosure)||!i.generatedOutputClosure.every(resourceRepoPath)))fail(p,'invalid generatedOutputClosure');if(i.number===13&&i.generatedOutputClosure!==undefined&&i.generatedOutputClosure.length!==0)fail(p,'issue #13 generatedOutputClosure must remain exact empty closure');if(i.requiredLocks?.some(l=>TEMPLATES.includes(l)))fail(p,'dynamic templates belong in resourceBindings, not static requiredLocks');
  if(!resourceContractsShape(i))fail(p,'unsupported or mismapped normative resource contract');
  if(Array.isArray(i.hardPredecessors)&&(!i.hardPredecessors.every(d=>depValid(d,true))||!unique(i.hardPredecessors.map(depKey))))fail(p,'invalid phase-qualified hardPredecessors');if(Array.isArray(i.hardSuccessors)&&(!i.hardSuccessors.every(d=>depValid(d,false))||!unique(i.hardSuccessors.map(depKey))))fail(p,'invalid phase-qualified hardSuccessors');for(const predicate of i.guardPredicates||[])if(!guardPredicateShape(predicate))fail(p,'invalid guardPredicate');if(Array.isArray(i.guardPredicates)&&!unique(i.guardPredicates.map(x=>x?.id)))fail(p,'duplicate guardPredicate');if(Array.isArray(i.related)&&(!i.related.every(n=>integer(n)&&all.has(n))||!unique(i.related)))fail(p,'related');if(Array.isArray(i.requirements)&&!i.requirements.every(text))fail(p,'requirements must be nonblank strings');
 }
 if(errors.length)return {valid:false,errors};
 for(const [n,producer] of all)for(const o of producer.outputs||[]){if(!keys(o,['id','producerIssue','producerPhase','path','qualification','scope','digestRequirements','acceptance','proposed','sourceRef','outputDigest','wholeIssueAcceptance','historicalSnapshot'])||!text(o.id)||o.producerIssue!==n||!ARTIFACT_PHASES.includes(o.producerPhase)||!text(o.qualification)||!text(o.scope)||!text(o.acceptance)||!keys(o.digestRequirements,['sourceRef','outputDigest'])||o.digestRequirements.sourceRef!=='required'||o.digestRequirements.outputDigest!=='sha256'){fail(ID(n),'invalid phase-qualified output');continue;}pathCheck(o.path,false,ID(n));if(outputIDs.has(o.id)||absorbedArtifactIDs.has(o.id))fail('outputs','globally duplicate artifact id');outputIDs.add(o.id);if(o.proposed!==undefined&&typeof o.proposed!=='boolean')fail(ID(n),'proposed boolean');if(o.sourceRef!==undefined&&!sha(o.sourceRef))fail(ID(n),'invalid optional output sourceRef');if(o.outputDigest!==undefined&&!digest(o.outputDigest))fail(ID(n),'invalid optional output digest');if(o.wholeIssueAcceptance!==undefined&&(typeof o.wholeIssueAcceptance!=='boolean'||(o.wholeIssueAcceptance&&o.producerPhase!=='accept')))fail(ID(n),'wholeIssueAcceptance only for explicitly final scope');if(o.historicalSnapshot!==undefined){const h=o.historicalSnapshot;if(map.has(n))fail(ID(n),'active output cannot be a historical snapshot');if(!keys(h,['readOnly','executable','currentOutput','nativeIssueEdge','sourceIssue','testedInputCodeRef','reportPublicationRef','reportByteSha256'])||h.readOnly!==true||h.executable!==false||h.currentOutput!==false||h.nativeIssueEdge!==false||h.sourceIssue!==n||!sha(h.testedInputCodeRef)||h.reportPublicationRef!==o.sourceRef||h.reportByteSha256!==o.outputDigest)fail(ID(n),'invalid bounded historical snapshot metadata');}}
 for(const i of g.items){const entries=OUTPUT_BINDING_PHASES.flatMap(container=>(i.artifactOutputBindings?.[container]||[]).map(binding=>({container,binding})));if(!unique(entries.map(x=>x.binding.artifact)))fail(ID(i.number),'artifact output binding duplicated across containers');for(const o of i.outputs){const matches=entries.filter(x=>x.binding.artifact===o.id);if(matches.length!==1){fail(ID(i.number),'output needs exactly one lossless artifactOutputBinding '+o.id);continue;}const {container,binding}=matches[0];if(bindingOutputPath(binding)!==o.path)fail(ID(i.number),'output binding path/pathKind drift '+o.id);if(container==='readOnlySnapshots'){if(binding.bindingKind!=='existing_read_only_snapshot'||o.proposed!==false||binding.scopeMode!=='read_only'||binding.lockId!==null||binding.canonicalWriteAllowed!==false)fail(ID(i.number),'read-only output binding grants or implies a write '+o.id);}else{if(container!==o.producerPhase||binding.bindingKind==='existing_read_only_snapshot')fail(ID(i.number),'output binding phase/kind drift '+o.id);const scope=i.phaseWriteScopes?.[container]?.find(s=>s.path===binding.scopePath);if(!scope||scope.mode!==binding.scopeMode||scope.physicalBinding!==binding.lockId||scope.canonicalWriteAllowed!==binding.canonicalWriteAllowed||!topoScopeCovers(scope.path,binding.path))fail(ID(i.number),'output binding scope/lock/canonical drift '+o.id);}}for(const {container,binding} of entries){if(!i.outputs.some(o=>o.id===binding.artifact))fail(ID(i.number),'artifactOutputBinding references unknown output '+binding.artifact);if(binding.receiptStorage?.mode==='separate_immutable_raw_records'){if(!ARTIFACT_PHASES.includes(container)){fail(ID(i.number),'raw receipt storage cannot be read-only');continue;}const rawScopePath=binding.receiptStorage.rawScopePath.endsWith('/**')?binding.receiptStorage.rawScopePath.slice(0,-2):binding.receiptStorage.rawScopePath,raw=i.phaseWriteScopes?.[container]?.find(s=>s.path===rawScopePath);if(binding.receiptStorage.stableIndexPath!==binding.path||!/never\s+overwrite/i.test(binding.rawReceiptPolicy)||binding.receiptStorage.rawLockId===binding.lockId||overlap(binding.receiptStorage.stableIndexPath,binding.receiptStorage.rawScopePath)||!raw||raw.physicalBinding!==binding.receiptStorage.rawLockId||raw.mode!==binding.scopeMode||raw.canonicalWriteAllowed!==binding.canonicalWriteAllowed)fail(ID(i.number),'separate raw receipt scope/lock is missing or drifted '+binding.artifact);}}}
 const output=(n,id,phase,where)=>{const producer=all.get(n),o=producer?.outputs?.find(x=>x.id===id);if(!o||o.producerIssue!==n||o.producerPhase!==phase){fail(where,`wrong producer/output/phase ${n}/${id}/${phase}`);return;}if(external.has(n)&&!(producer.stateReason==='COMPLETED'&&o.producerPhase==='accept'&&sha(o.sourceRef)&&digest(o.outputDigest)&&text(o.qualification)))fail(where,'historical input needs COMPLETED exact accepted scope/ref/digest');};
 const accessGuardIds=[];for(const i of g.items){for(const x of i.inputs)if(x.fromIssue!==null){output(x.fromIssue,x.id,x.producerPhase,'input');if(!g.edges.some(e=>e.relation==='requires'&&e.from===ID(i.number)&&e.to===ID(x.fromIssue)&&e.artifact===x.id&&e.consumerPhase===x.consumerPhase&&e.producerPhase===x.producerPhase))fail('input','mandatory producer input missing canonical requires edge');}for(const d of i.hardPredecessors)if(d.accessGuard){accessGuardIds.push(d.accessGuard.id);if(!accessGuardContext(d.accessGuard,{consumerPhase:d.consumerPhase,providerArtifact:d.artifact,direct:true})||!accessGuardMatchesItemContext(i,d.accessGuard))fail('accessGuard','direct guard activation/provider/context-availability mismatch');}for(const group of i.oneOfInputs){if(!keys(group,['artifact','consumerPhase','alternatives','resolution'])||!text(group.artifact)||!ARTIFACT_PHASES.includes(group.consumerPhase)||!text(group.resolution)||!Array.isArray(group.alternatives)||!group.alternatives.length){fail(ID(i.number),'oneOf group/phase');continue;}for(const a of group.alternatives){if(!keys(a,ALTERNATIVE_KEYS)||!integer(a.issue)||!text(a.artifact)||!ARTIFACT_PHASES.includes(a.producerPhase)||(a.guards!==undefined&&(!Array.isArray(a.guards)||!a.guards.length))||(a.additionalInputs!==undefined&&(!Array.isArray(a.additionalInputs)||!a.additionalInputs.length))){fail('oneOf','invalid alternative');continue;}output(a.issue,a.artifact,a.producerPhase,'oneOf');if(Array.isArray(a.guards)){if(!unique(a.guards.map(guard=>artifactKey(guard?.issue,guard?.artifact,guard?.producerPhase))))fail('oneOf','duplicate guard');for(const guard of a.guards){if(!guardShape(guard)){fail('oneOf','invalid guard');continue;}output(guard.issue,guard.artifact,guard.producerPhase,'oneOf guard');}}for(const additional of a.additionalInputs||[]){if(!keys(additional,ADDITIONAL_INPUT_KEYS)||!text(additional.artifact)||additional.consumerPhase!==group.consumerPhase||(additional.reason!==undefined&&!text(additional.reason))){fail('oneOf','invalid additional input');continue;}const found=graphArtifact(g,additional.artifact);if(!found){fail('oneOf','unknown additional artifact');continue;}output(found.issue,additional.artifact,found.output.producerPhase,'oneOf additional input');if(additional.accessGuard){accessGuardIds.push(additional.accessGuard.id);if(!accessGuardContext(additional.accessGuard,{consumerPhase:group.consumerPhase,providerArtifact:additional.artifact,group:group.artifact,selectedArtifact:a.artifact,direct:false})||!accessGuardMatchesItemContext(i,additional.accessGuard))fail('accessGuard','selected branch activation/provider/context-availability mismatch');}}const branch=branchGuards(g,a);if(!unique(branch.map(guard=>artifactKey(guard.issue,guard.artifact,guard.producerPhase))))fail('oneOf','duplicate branch guard artifact');}if(!unique(group.alternatives.map(a=>artifactKey(a.issue,a.artifact,a.producerPhase))))fail('oneOf','duplicate alternative');}if(!unique(i.oneOfInputs.map(o=>JSON.stringify([o?.artifact,o?.consumerPhase]))))fail('oneOf','duplicate group');}if(!unique(accessGuardIds))fail('accessGuard','globally duplicate guard id');
 if(!unique(g.edges.map(e=>JSON.stringify([e.relation,e.from,e.to,e.artifact??null,e.consumerPhase??null,e.producerPhase??null,e.fromPhase??null,e.toPhase??null]))))fail('edges','duplicate');
 for(const e of g.edges){const base=['from','to','relation','reason'];const allowed=e.relation==='implements'?REQUIREMENT_EDGE_KEYS:e.relation==='requires'?[...base,'artifact','consumerPhase','producerPhase']:e.relation==='contains'?[...base,'purpose']:['parallel_with','conflicts_with'].includes(e.relation)?[...base,'fromPhase','toPhase']:base;if(!RELATIONS.includes(e.relation)||!keys(e,allowed)||!nodeIDs.has(e.from)||!nodeIDs.has(e.to)||!text(e.reason)||e.from===e.to){fail('edge','relation-specific fields, endpoints or reason invalid');continue;}if(e.relation==='requires'){if(!map.has(nodeNumber(e.from))||!ARTIFACT_PHASES.includes(e.consumerPhase)||!ARTIFACT_PHASES.includes(e.producerPhase)||!text(e.artifact))fail('requires','phase-qualified artifact required');else output(nodeNumber(e.to),e.artifact,e.producerPhase,'requires');}if(e.relation==='contains'&&(!map.has(nodeNumber(e.from))||!map.has(nodeNumber(e.to))||!['organization','decomposition','review'].includes(e.purpose)))fail('contains','active endpoints and explicit purpose required');if(['parallel_with','conflicts_with'].includes(e.relation)&&(!map.has(nodeNumber(e.from))||!map.has(nodeNumber(e.to))||(e.fromPhase!==undefined&&!PHASES.includes(e.fromPhase))||(e.toPhase!==undefined&&!PHASES.includes(e.toPhase))))fail('edge','invalid conflict/parallel endpoint phase');}
 for(const i of g.items){if(i.relationshipDeclarations===undefined)continue;for(const relation of RELATIONSHIP_TYPES){const block=i.relationshipDeclarations[relation],incident=g.edges.filter(e=>e.relation===relation&&(e.from===ID(i.number)||e.to===ID(i.number)));if(block.items.length!==incident.length)fail(ID(i.number),relation+' declaration count contradicts emitted relationships');for(const declared of block.items){const matches=incident.filter(e=>{const ownFrom=e.from===ID(i.number),peer=nodeNumber(ownFrom?e.to:e.from),fromPhase=ownFrom?e.fromPhase:e.toPhase,toPhase=ownFrom?e.toPhase:e.fromPhase;return peer===declared.issue&&fromPhase===declared.fromPhase&&toPhase===declared.toPhase&&e.reason===declared.reason;});if(matches.length!==1)fail(ID(i.number),relation+' declaration does not match exactly one emitted relationship');}}}
 if(errors.length)return {valid:false,errors};
 for(const i of g.items){for(const [field,successor] of [['hardPredecessors',false],['hardSuccessors',true]]){const projected=g.edges.filter(e=>e.relation==='requires'&&(successor?e.to:e.from)===ID(i.number)).map(e=>depKey(depFromEdge(e,successor))).sort();if(projected.join()!==i[field].map(depKey).sort().join())fail(ID(i.number),`${field} projection`);}const parents=g.edges.filter(e=>e.relation==='contains'&&e.to===ID(i.number)).map(e=>nodeNumber(e.from));if(parents.length>1||parents.join()!==(i.parent===null?[]:[i.parent]).join())fail(ID(i.number),'parent projection');const containing=new Set();let p=i.parent;while(p!==null&&map.has(p)){if(containing.has(p)){fail('contains','cycle');break;}containing.add(p);p=map.get(p).parent;}
  const deps=[...i.hardPredecessors,...i.oneOfInputs.flatMap(o=>o.alternatives.flatMap(a=>[{issue:a.issue,producerPhase:a.producerPhase},...branchGuards(g,a).map(guard=>({issue:guard.issue,producerPhase:guard.producerPhase}))])),...i.inputs.filter(x=>x.fromIssue!==null).map(x=>({issue:x.fromIssue,producerPhase:x.producerPhase}))];if(deps.some(d=>d.producerPhase==='accept'&&containing.has(d.issue)&&map.get(d.issue)?.kind==='goal'))fail(ID(i.number),'child requires containing goal final acceptance');
  if(i.kind==='goal'){if(!g.edges.some(e=>e.relation==='contains'&&e.from===ID(i.number)&&e.purpose==='decomposition'))fail(ID(i.number),'goal needs decomposition children');if(!i.hardPredecessors.some(d=>d.consumerPhase==='accept')&&!i.oneOfInputs.some(o=>o.consumerPhase==='accept')&&!i.inputs.some(x=>x.fromIssue!==null&&x.consumerPhase==='accept'))fail(ID(i.number),'goal final acceptance must consume qualified output; use work_item for planning');}
 }
 const adj=phaseDependencies(g);for(const n of adj.keys())if(reaches(adj,n,n))fail('phase DAG',`cycle at ${n}`);
 for(const e of g.edges.filter(e=>e.relation==='parallel_with')){const a=map.get(nodeNumber(e.from)),b=map.get(nodeNumber(e.to)),aps=e.fromPhase?[e.fromPhase]:PHASES,bps=e.toPhase?[e.toPhase]:PHASES;if(!a||!b)continue;if(a.kind==='goal'||b.kind==='goal'||aps.some(ap=>bps.some(bp=>reaches(adj,`${a.number}@${ap}`,`${b.number}@${bp}`)||reaches(adj,`${b.number}@${bp}`,`${a.number}@${ap}`)))||conflictReasons(a,b,{phaseA:e.fromPhase,phaseB:e.toPhase}).length)fail('parallel','not independent at declared phases');}
 for(let a=0;a<g.items.length;a++)for(let b=a+1;b<g.items.length;b++){const x=g.items[a],y=g.items[b];if(conflictReasons(x,y).length&&!g.edges.some(e=>e.relation==='conflicts_with'&&((e.from===ID(x.number)&&e.to===ID(y.number))||(e.from===ID(y.number)&&e.to===ID(x.number)))))fail('conflicts',`missing ${x.number}/${y.number}`);}
 return {valid:!errors.length,errors};
}
export function validateGraph(g){try{requirementProjection(g);return {...inspect(g),scope:'phase-qualified planning structure; not live readiness or product acceptance'};}catch(e){return {valid:false,errors:[e.message?.startsWith('requirements:')?e.message:'malformed graph: failed closed'],scope:'planning only'};}}
export function writerBoundary(g,n,phase){const v=validateGraph(g);if(!v.valid)throw Error('Invalid graph: '+v.errors.join('; '));const item=g.items.find(i=>i.number===n);if(!item||!PHASES.includes(phase))throw Error('Unknown issue/phase');return {issueNumber:n,phase,phaseWriteScopes:structuredClone(item.phaseWriteScopes[phase]),artifactOutputBindings:structuredClone(item.artifactOutputBindings[phase]||[]),resourceContracts:structuredClone(item.resourceBindings.filter(x=>x.phases.includes(phase))),requiresExplicitWriterContext:true,authorization:false,startPermission:false,scope:'lossless planning and caller-evidence boundary only; no lock acquisition, routing, permissions, or execution claim'};}
export function absorbedHistoricalContext(g,n){const v=validateGraph(g);if(!v.valid)throw Error('Invalid graph: '+v.errors.join('; '));const reference=g.references.find(r=>r.number===n&&r.absorbedHistoricalContext);if(!reference)throw Error('Unknown absorbed historical reference');return {issueNumber:n,state:reference.state,stateReason:reference.stateReason,closedAt:reference.closedAt,observedAt:reference.observedAt,...structuredClone(reference.absorbedHistoricalContext),authorization:false,startPermission:false,scope:'read-only historical attribution only; never a current output, producer, dependency, or execution claim'};}
export function inputProjections(g,n){const i=g.items.find(x=>x.number===n);if(!i)throw Error('Unknown issue');return Object.fromEntries(PHASES.map(phase=>[phase,{hard:i.hardPredecessors.filter(d=>d.consumerPhase===phase),oneOf:i.oneOfInputs.filter(o=>o.consumerPhase===phase),declared:i.inputs.filter(x=>x.consumerPhase===phase)}]));}
export function conditionalNeighborhood(g,n){const item=g.items.find(i=>i.number===n);if(!item)throw Error('Unknown issue');const asConsumer=item.oneOfInputs.map(group=>({group:group.artifact,consumerPhase:group.consumerPhase,resolution:group.resolution,alternatives:group.alternatives.map(a=>({selectedAlternative:{issue:a.issue,artifact:a.artifact,producerPhase:a.producerPhase},guards:a.guards||[],additionalInputs:a.additionalInputs||[]}))})),asProducer=[];for(const consumer of g.items)for(const group of consumer.oneOfInputs)for(const a of group.alternatives){const selectedAlternative={issue:a.issue,artifact:a.artifact,producerPhase:a.producerPhase};if(a.issue===n)asProducer.push({role:'alternative',consumerIssue:consumer.number,group:group.artifact,consumerPhase:group.consumerPhase,selectedAlternative,artifact:a.artifact,producerPhase:a.producerPhase});for(const guard of branchGuards(g,a))if(guard.issue===n)asProducer.push({role:'guard',consumerIssue:consumer.number,group:group.artifact,consumerPhase:group.consumerPhase,selectedAlternative,artifact:guard.artifact,producerPhase:guard.producerPhase,guard});}return {asConsumer,asProducer};}
export function neighborhood(g,n,{full=false,maxBytes=12000}={}){const v=validateGraph(g);if(!v.valid)throw Error('Invalid graph: '+v.errors.join('; '));const original=g.items.find(i=>i.number===n);if(!original)throw Error('Unknown issue');const all=g.edges.filter(e=>e.from===ID(n)||e.to===ID(n)),projections=inputProjections(g,n),conditional=conditionalNeighborhood(g,n);if(full)return {item:original,edges:all,inputProjections:projections,conditional,liveStatus:'UNKNOWN: verify captured status and live phase frontier separately'};if(!Number.isInteger(maxBytes)||maxBytes<512||maxBytes>32000)throw Error('Invalid brief budget');const wanted=['number','workId','title','kind','parent','outcome','nonGoals','truth','context','codePointers','inputQualifiers','outputs','writeScope','phaseWriteScopes','artifactOutputBindings','checks','checkEvidenceMappings','relationshipDeclarations','acceptance','stopConditions','hardSuccessors','requiredLocks','resourceBindings','authorityBlock'];const item=Object.fromEntries(wanted.filter(k=>k in original).map(k=>[k,original[k]]));const packet={item,inputProjections:projections,conditional,containment:all.filter(e=>e.relation==='contains'),optionalNeighborCount:all.filter(e=>!['requires','contains'].includes(e.relation)).length,continuation:`node scripts/context/work_graph.mjs brief --issue ${n} --full`,liveStatus:'UNKNOWN: captured status is separate from planned eligibility; use a fresh read-only phase-frontier projection'};if(Buffer.byteLength(JSON.stringify(packet),'utf8')>maxBytes)throw Error(`Required context exceeds ${maxBytes}-byte budget; request a scoped view or brief --issue ${n} --full. No required phase input omitted.`);return packet;}
const sameVersion=(a,b)=>!!(a&&b&&a.producerIssue===b.producerIssue&&a.outputId===b.outputId&&a.producerPhase===b.producerPhase&&a.sourceRef===b.sourceRef&&a.outputDigest===b.outputDigest&&a.qualification===b.qualification&&a.scope===b.scope);
const identityMatch=(a,p,id,phase)=>a?.producerIssue===p&&a?.outputId===id&&a?.producerPhase===phase;
const guardConstraint=(guard,a)=>(guard.expectedSourceRef===undefined||a?.sourceRef===guard.expectedSourceRef)&&(guard.expectedDigest===undefined||a?.outputDigest===guard.expectedDigest)&&(guard.expectedScope===undefined||a?.scope===guard.expectedScope);
function sourceAccessSatisfied(guard,list,expected,has,snapshotAt,now){const matches=(list||[]).filter(x=>x.guardId===guard.id&&x.consumerPhase===guard.consumerPhase);if(matches.length!==1)return false;const x=matches[0],age=now-Date.parse(x.observedAt);if(!sourceAccessEvidenceShape(x)||age<0||age>900000||Date.parse(x.observedAt)>Date.parse(snapshotAt)||x.provider.outputId!==guard.manifestProviderArtifact||!sameVersion(x.provider,expected.get(qualifiedKey(x.provider)))||!has(x.provider.producerIssue,x.provider.outputId,x.provider.producerPhase))return false;if(x.observedOperations.length!==guard.operations.length)return false;return guard.operations.every(operation=>{const manifest=x.manifestEntries.find(v=>v.path===operation.path),observed=x.observedOperations.find(v=>v.path===operation.path&&v.operation===operation.operation);return !!(manifest&&observed&&manifest.sourceRef===observed.sourceRef&&manifest.digest===observed.digest&&observed.accessible===true);});}
function predicateSatisfied(predicate,list,stateSourceRef,snapshotAt,now){const matches=(list||[]).filter(x=>x.id===predicate.id);if(matches.length!==1)return false;const x=matches[0],age=now-Date.parse(x.observedAt);return predicateEvidenceShape(x)&&x.kind===predicate.kind&&x.consumerPhase===predicate.consumerPhase&&x.sourceRef===stateSourceRef&&age>=0&&age<=900000&&Date.parse(x.observedAt)<=Date.parse(snapshotAt);}
function retainedSourceAccessSatisfied(guard,list,expected){const matches=(list||[]).filter(x=>x.guardId===guard.id&&x.consumerPhase===guard.consumerPhase);if(matches.length!==1)return false;const x=matches[0];if(!sourceAccessEvidenceShape(x)||x.provider.outputId!==guard.manifestProviderArtifact||!sameVersion(x.provider,expected.get(qualifiedKey(x.provider)))||x.observedOperations.length!==guard.operations.length)return false;return guard.operations.every(operation=>{const manifest=x.manifestEntries.find(v=>v.path===operation.path),observed=x.observedOperations.find(v=>v.path===operation.path&&v.operation===operation.operation);return !!(manifest&&observed&&manifest.sourceRef===observed.sourceRef&&manifest.digest===observed.digest&&observed.accessible===true);});}
function retainedPredicateSatisfied(predicate,list,stateSourceRef){const matches=(list||[]).filter(x=>x.id===predicate.id);return matches.length===1&&predicateEvidenceShape(matches[0])&&matches[0].kind===predicate.kind&&matches[0].consumerPhase===predicate.consumerPhase&&matches[0].sourceRef===stateSourceRef;}
function retainedGateErrors(g,item,phase,e,projection,expected,stateSourceRef){const errors=[],sourceGuards=projection.hard.filter(x=>x.accessGuard).map(x=>x.accessGuard);for(const selected of e.selectedInputs||[]){const group=projection.oneOf.find(x=>x.artifact===selected.group&&x.consumerPhase===phase),alt=alternativeFor(group,selected);if(alt)sourceGuards.push(...branchGuards(g,alt).filter(x=>x.accessGuard).map(x=>x.accessGuard));}const sourceBindings=e.sourceAccessBindings||[];for(const guard of sourceGuards)if(!retainedSourceAccessSatisfied(guard,sourceBindings,expected))errors.push('prior source access provenance missing/drifted '+guard.id);for(const binding of sourceBindings)if(!sourceGuards.some(x=>x.id===binding.guardId))errors.push('prior source access provenance is extra '+binding.guardId);const predicates=item.guardPredicates.filter(x=>x.consumerPhase===phase),predicateBindings=e.predicateBindings||[];for(const predicate of predicates)if(!retainedPredicateSatisfied(predicate,predicateBindings,stateSourceRef))errors.push('prior predicate provenance missing/drifted '+predicate.id);for(const binding of predicateBindings)if(!predicates.some(x=>x.id===binding.id))errors.push('prior predicate provenance is extra '+binding.id);return errors;}
function alternativeFor(group,s){return group?.alternatives?.find(a=>a.issue===s?.producerIssue&&a.artifact===s?.outputId&&a.producerPhase===s?.producerPhase);}
function guardBindingsValid(g,alt,s,expected,has,requireAvailable=true){const guards=branchGuards(g,alt),bindings=s?.guardBindings||[];if(!Array.isArray(bindings)||bindings.length!==guards.length)return false;return guards.every(guard=>{const matches=bindings.filter(b=>identityMatch(b,guard.issue,guard.artifact,guard.producerPhase));if(matches.length!==1)return false;const b=matches[0];return expectedShape(b)&&sameVersion(b,expected.get(qualifiedKey(b)))&&guardConstraint(guard,b)&&(!requireAvailable||has(guard.issue,guard.artifact,guard.producerPhase));});}
function selectionValid(g,group,s,expected,has,requireAvailable=true){const alt=alternativeFor(group,s);return !!(alt&&selectedShape(s)&&text(s.group)&&s.compatible===true&&text(s.compatibilityEvidence)&&s.consumerPhase===group.consumerPhase&&sameVersion(s,expected.get(qualifiedKey(s)))&&(!requireAvailable||has(s.producerIssue,s.outputId,s.producerPhase))&&guardBindingsValid(g,alt,s,expected,has,requireAvailable));}
function retainedSelectionValid(g,group,s,e,expected){if(!selectionValid(g,group,s,expected,()=>true,false))return false;const alt=alternativeFor(group,s),required=[s,...(s.guardBindings||[])];if(required.length!==branchGuards(g,alt).length+1)return false;return required.every(r=>e.inputBindings.some(b=>sameVersion(b,r)&&sameVersion(b,expected.get(qualifiedKey(b)))));}
const PROJECTION_KEYS=['snapshotKind','observedAt','sourceRef','reviewedContext','frontiers','artifactReceipts','expectedArtifacts','resolvedAuthorities'];
const FRONTIER_KEYS=['issueNumber','issueUrl','plannedPhase','capturedStatus','phaseEvidence','selectedInputs','verifiedInputs','verifiedSources','sourceAccessEvidence','predicateEvidence'];
const CAPTURED_STATUS_KEYS=['status','observedAt','sourceSnapshotCapturedAt','sourceIssueUpdatedAt','scope'];
function projectionCapturedStatusShape(x){if(!keys(x,CAPTURED_STATUS_KEYS)||!(x.status===null||STATUSES.includes(x.status))||!(x.observedAt===null||Number.isFinite(Date.parse(x.observedAt)))||!Number.isFinite(Date.parse(x.sourceSnapshotCapturedAt))||!Number.isFinite(Date.parse(x.sourceIssueUpdatedAt))||!text(x.scope))return false;if(Date.parse(x.sourceIssueUpdatedAt)>Date.parse(x.sourceSnapshotCapturedAt))return false;return x.status===null?x.observedAt===null:x.observedAt===x.sourceSnapshotCapturedAt;}
function projectionShapeErrors(state){
 const errors=[],bad=label=>errors.push('unknown or malformed projection fields in '+label);
 if(!keys(state,PROJECTION_KEYS))return ['unknown or malformed projection state'];
 if(!Array.isArray(state.artifactReceipts)||!state.artifactReceipts.every(x=>keys(x,RECEIPT_KEYS)))bad('artifactReceipts');
 if(!Array.isArray(state.expectedArtifacts)||!state.expectedArtifacts.every(expectedShape))bad('expectedArtifacts');
 if(!Array.isArray(state.resolvedAuthorities)||!state.resolvedAuthorities.every(text))bad('resolvedAuthorities');
 if(!Array.isArray(state.frontiers)){bad('frontiers');return [...new Set(errors)];}
 for(const f of state.frontiers){
  if(!keys(f,FRONTIER_KEYS)||!projectionCapturedStatusShape(f?.capturedStatus))bad('phaseFrontier');
  for(const name of ['phaseEvidence','selectedInputs','verifiedInputs','verifiedSources','sourceAccessEvidence','predicateEvidence'])if(!Array.isArray(f?.[name]))bad('phaseFrontier.'+name);
  for(const s of Array.isArray(f?.selectedInputs)?f.selectedInputs:[])if(!selectedShape(s))bad('phaseFrontier.selectedInputs');
  for(const e of Array.isArray(f?.phaseEvidence)?f.phaseEvidence:[]){if(!keys(e,PHASE_EVIDENCE_KEYS)||!Array.isArray(e.inputBindings)||!Array.isArray(e.selectedInputs)){bad('phaseFrontier.phaseEvidence');continue;}if(!e.inputBindings.every(expectedShape)||!e.selectedInputs.every(selectedShape))bad('phaseFrontier.phaseEvidence bindings');if(e.sourceAccessBindings!==undefined&&(!Array.isArray(e.sourceAccessBindings)||!e.sourceAccessBindings.every(sourceAccessEvidenceShape)))bad('phaseFrontier.phaseEvidence source access');if(e.predicateBindings!==undefined&&(!Array.isArray(e.predicateBindings)||!e.predicateBindings.every(predicateEvidenceShape)))bad('phaseFrontier.phaseEvidence predicate');}
  if(Array.isArray(f?.verifiedInputs)&&!f.verifiedInputs.every(x=>keys(x,VERIFIED_INPUT_KEYS)))bad('phaseFrontier.verifiedInputs');
  if(Array.isArray(f?.verifiedSources)&&!f.verifiedSources.every(x=>keys(x,VERIFIED_SOURCE_KEYS)))bad('phaseFrontier.verifiedSources');
  if(Array.isArray(f?.sourceAccessEvidence)&&!f.sourceAccessEvidence.every(sourceAccessEvidenceShape))bad('phaseFrontier.sourceAccessEvidence');if(Array.isArray(f?.predicateEvidence)&&!f.predicateEvidence.every(predicateEvidenceShape))bad('phaseFrontier.predicateEvidence');
 }
 return [...new Set(errors)];
}
function projectionArtifactRegistry(g,state,errors){
 const all=new Map([...g.references,...g.items].map(i=>[i.number,i])),receipts=new Map(),expected=new Map();
 for(const [list,dest,isReceipt] of [[state.expectedArtifacts,expected,false],[state.artifactReceipts,receipts,true]])for(const a of list){
  if(!obj(a)||!integer(a.producerIssue)||!text(a.outputId)||!ARTIFACT_PHASES.includes(a.producerPhase)||!sha(a.sourceRef)||!digest(a.outputDigest)||!text(a.qualification)||!text(a.scope)||(isReceipt&&(a.available!==true||!text(a.evidence)||!text(a.reviewRef)||(a.producerPhase==='candidate'?a.accepted!==false:a.accepted!==true)))){errors.push('malformed phase-qualified projection artifact');continue;}
  const k=qualifiedKey(a),p=all.get(a.producerIssue),o=p?.outputs?.find(x=>x.id===a.outputId);
  if(dest.has(k))errors.push('duplicate projection artifact identity');
  if(!o||o.producerIssue!==a.producerIssue||o.producerPhase!==a.producerPhase||o.qualification!==a.qualification||o.scope!==a.scope)errors.push('projection artifact not in exact producer registry');
  if(g.references.some(r=>r.number===a.producerIssue)&&!(p?.stateReason==='COMPLETED'&&a.producerPhase==='accept'&&o?.sourceRef===a.sourceRef&&o?.outputDigest===a.outputDigest))errors.push('projection historical artifact lacks completed exact qualification');
  dest.set(k,a);
 }
 const has=(p,o,phase)=>sameVersion(receipts.get(artifactKey(p,o,phase)),expected.get(artifactKey(p,o,phase)));
 for(const [k,a] of receipts)if(!sameVersion(a,expected.get(k)))errors.push('projection receipt expected version/scope mismatch');
 return {all,receipts,expected,has};
}
function issueCycle(adjacency){for(const start of adjacency.keys()){const seen=new Set(),todo=[...(adjacency.get(start)||[])];while(todo.length){const at=todo.pop();if(at===start)return true;if(seen.has(at))continue;seen.add(at);todo.push(...(adjacency.get(at)||[]));}}return false;}
export function projectEligibility(g,n,state,now=Date.now()){
 const scope='read-only supplied evidence projection only; never grants start permission or authorizes GitHub writes';
 const invalid=(reasons=[],freshness={snapshotKind:['live','historical'].includes(state?.snapshotKind)?state.snapshotKind:null,ageMs:null,fresh:false,current:false})=>({valid:false,reasons,snapshotAsOf:text(state?.observedAt)?state.observedAt:null,plannedPhase:null,capturedStatus:null,freshness,nativeBlockedBy:null,artifactEligible:false,preparationAvailable:null,nextPhaseEligible:null,unresolvedSelections:[],missingInputs:[],sourceAccessGates:[],predicateGates:[],nonNativeArtifactGates:[],startPermission:false,scope});
 const graphValidation=validateGraph(g);if(!graphValidation.valid)return invalid(['invalid graph',...graphValidation.errors]);if(!integer(n)||!g.items.some(i=>i.number===n)||!obj(state))return invalid(['unknown issue/malformed projection state']);
 try{
  const errors=projectionShapeErrors(state);if(errors.length)return invalid(errors);
  const age=now-Date.parse(state.observedAt),fresh=Number.isFinite(age)&&age>=0&&age<=900000,snapshotKind=['live','historical'].includes(state.snapshotKind)?state.snapshotKind:null,current=snapshotKind==='live'&&fresh;
  const freshness={snapshotKind,ageMs:Number.isFinite(age)?age:null,fresh,current};
  if(!['live','historical'].includes(state.snapshotKind))errors.push('snapshotKind must be live or historical');
  if(!text(state.observedAt)||!Number.isFinite(Date.parse(state.observedAt)))errors.push('projection observedAt must be an ISO date-time');
  if(state.snapshotKind==='live'&&!fresh)errors.push('live projection requires a fresh non-future snapshot');
  if(!sha(state.sourceRef)||state.reviewedContext!==true)errors.push('projection requires exact sourceRef and reviewedContext true');if(!unique(state.resolvedAuthorities))errors.push('duplicate resolved authority');
  const byIssue=new Map();for(const f of state.frontiers){if(!integer(f.issueNumber)||byIssue.has(f.issueNumber))errors.push('duplicate/invalid phase frontier');else byIssue.set(f.issueNumber,f);}
  const activeNumbers=g.items.map(i=>i.number).sort((a,b)=>a-b),frontierNumbers=[...byIssue.keys()].sort((a,b)=>a-b);if(activeNumbers.join()!==frontierNumbers.join())errors.push('phase frontier must cover every active issue exactly once');
  const {expected,has}=projectionArtifactRegistry(g,state,errors),nativeByIssue=new Map(activeNumbers.map(x=>[x,new Set()])),missingByIssue=new Map(activeNumbers.map(x=>[x,[]])),unresolvedByIssue=new Map(activeNumbers.map(x=>[x,[]])),eligibilityByIssue=new Map(activeNumbers.map(x=>[x,[]])),preparationByIssue=new Map(activeNumbers.map(x=>[x,[]])),sourceGateByIssue=new Map(activeNumbers.map(x=>[x,[]])),predicateGateByIssue=new Map(activeNumbers.map(x=>[x,[]])),nonNativeByIssue=new Map(activeNumbers.map(x=>[x,[]]));
  const addMissing=(consumer,record)=>{missingByIssue.get(consumer)?.push(record);eligibilityByIssue.get(consumer)?.push('missing '+record.role+' artifact '+record.producerIssue+'/'+record.artifact+'/'+record.producerPhase);if(g.items.some(i=>i.number===record.producerIssue)&&record.producerIssue!==consumer)nativeByIssue.get(consumer)?.add(record.producerIssue);else if(!g.items.some(i=>i.number===record.producerIssue))nonNativeByIssue.get(consumer)?.push({...record,nativeIssueEdge:false});};
  for(const item of g.items){
   const f=byIssue.get(item.number);if(!f)continue;const where='frontier issue '+item.number;
   if(!issueURL(f.issueUrl,item.number)||!(f.plannedPhase===null||PHASES.includes(f.plannedPhase)))errors.push(where+' has invalid URL/planned phase');
   if(!projectionCapturedStatusShape(f.capturedStatus)||Date.parse(f.capturedStatus.sourceSnapshotCapturedAt)>Date.parse(state.observedAt))errors.push(where+' has invalid captured status metadata');
   const evidence=new Map();for(const e of f.phaseEvidence){if(!obj(e)||!PHASES.includes(e.phase)||e.verified!==true||e.sourceRef!==state.sourceRef||!text(e.evidence)||!Array.isArray(e.inputBindings)||!Array.isArray(e.selectedInputs)){errors.push(where+' has invalid phase evidence');continue;}if(evidence.has(e.phase))errors.push(where+' has duplicate phase evidence');evidence.set(e.phase,e);for(const b of e.inputBindings)if(!sameVersion(b,expected.get(qualifiedKey(b))))errors.push(where+' retained input binding drift');}
   const phaseIndex=PHASES.indexOf(f.plannedPhase),requiredPrior=f.plannedPhase===null?[]:PHASES.slice(0,Math.max(0,phaseIndex));if([...evidence.keys()].some(p=>!requiredPrior.includes(p))||requiredPrior.some(p=>!evidence.has(p)))errors.push(where+' inconsistent phase frontier evidence');
   const projections=inputProjections(g,item.number);if(f.plannedPhase===null){if(f.selectedInputs.length||f.sourceAccessEvidence.length||f.predicateEvidence.length)errors.push(where+' no planned phase cannot carry active gate evidence');continue;}
   for(const phase of requiredPrior){const e=evidence.get(phase);if(!e)continue;const p=projections[phase];for(const d of [...p.hard,...p.declared.filter(x=>x.fromIssue!==null).map(x=>({issue:x.fromIssue,artifact:x.id,producerPhase:x.producerPhase}))])if(!e.inputBindings.some(b=>identityMatch(b,d.issue,d.artifact,d.producerPhase)&&sameVersion(b,expected.get(qualifiedKey(b)))))errors.push(where+' prior evidence missing '+d.artifact);for(const group of p.oneOf)if(!e.selectedInputs.some(s=>s.group===group.artifact&&retainedSelectionValid(g,group,s,e,expected)))errors.push(where+' prior evidence missing guarded OR selection '+group.artifact);for(const s of e.selectedInputs){const group=p.oneOf.find(o=>o.artifact===s.group);if(!group||!retainedSelectionValid(g,group,s,e,expected))errors.push(where+' prior OR provenance invalid '+(s?.group||'unknown'));}for(const gateError of retainedGateErrors(g,item,phase,e,p,expected,state.sourceRef))errors.push(where+' '+gateError);}
   if(f.plannedPhase==='accept')for(const out of item.outputs.filter(o=>o.producerPhase==='candidate'))if(!has(item.number,out.id,'candidate'))errors.push(where+' candidate output unavailable at accept frontier '+out.id);
   const verified=new Map();for(const v of f.verifiedInputs){if(!obj(v)||!text(v.id)||!(v.fromIssue===null||integer(v.fromIssue))||!text(v.qualification)||v.verified!==true||!sha(v.sourceRef)||!digest(v.digest)||!text(v.evidence)){errors.push(where+' malformed verified input');continue;}const k=JSON.stringify([v.fromIssue,v.id]);if(verified.has(k))errors.push(where+' duplicate verified input');verified.set(k,v);}
   for(const x of item.inputs)if(x.fromIssue===null&&PHASES.indexOf(x.consumerPhase)<=phaseIndex){const v=verified.get(JSON.stringify([null,x.id]));if(!v||v.qualification!==x.qualification||v.sourceRef!==(x.expectedSourceRef||state.sourceRef)||(x.expectedDigest&&v.digest!==x.expectedDigest)){const message='unverified applicable source/manual input '+x.id;eligibilityByIssue.get(item.number).push(message);if(x.consumerPhase==='prepare')preparationByIssue.get(item.number).push(message);}}
   const sourceMap=new Map();for(const s of f.verifiedSources){if(!obj(s)||!safePath(s.path)||!sha(s.sourceRef)||!digest(s.digest)||s.available!==true||!text(s.evidence)){errors.push(where+' invalid verified source');continue;}if(sourceMap.has(s.path))errors.push(where+' duplicate verified source');sourceMap.set(s.path,s);}for(const c of item.context.filter(c=>PHASES.indexOf(c.readWhen)<=phaseIndex)){if(c.sourceRef&&c.sourceRef!==state.sourceRef){const message='context ref mismatch '+c.path;eligibilityByIssue.get(item.number).push(message);if(c.readWhen==='prepare')preparationByIssue.get(item.number).push(message);}if(c.availability==='local_unpublished'&&sourceMap.get(c.path)?.sourceRef!==state.sourceRef){const message='current source unavailable '+c.path;eligibilityByIssue.get(item.number).push(message);if(c.readWhen==='prepare')preparationByIssue.get(item.number).push(message);}}
   const authority=text(item.authorityBlock)?{id:item.authorityBlock,phases:['candidate','accept']}:item.authorityBlock;if(authority?.phases.includes(f.plannedPhase)&&!state.resolvedAuthorities.includes(authority.id)){const message='unresolved authority '+authority.id;eligibilityByIssue.get(item.number).push(message);if(authority.phases.includes('prepare'))preparationByIssue.get(item.number).push(message);}
   const currentProjection=projections[f.plannedPhase];for(const d of currentProjection.hard){if(!has(d.issue,d.artifact,d.producerPhase))addMissing(item.number,{role:'direct',producerIssue:d.issue,artifact:d.artifact,producerPhase:d.producerPhase,consumerPhase:f.plannedPhase});if(d.accessGuard&&!sourceAccessSatisfied(d.accessGuard,f.sourceAccessEvidence,expected,has,state.observedAt,now)){const gate={id:d.accessGuard.id,consumerPhase:f.plannedPhase,manifestProviderArtifact:d.accessGuard.manifestProviderArtifact,operations:d.accessGuard.operations.map(x=>({path:x.path,operation:x.operation})),nativeIssueEdge:false};sourceGateByIssue.get(item.number).push(gate);eligibilityByIssue.get(item.number).push('unresolved source access '+d.accessGuard.id);}}
   const currentGroups=currentProjection.oneOf,seenSelection=new Set();for(const s of f.selectedInputs){const k=JSON.stringify([s.group,s.consumerPhase,qualifiedKey(s)]);if(seenSelection.has(k))errors.push(where+' duplicate selected input');seenSelection.add(k);const group=currentGroups.find(o=>o.artifact===s.group&&o.consumerPhase===s.consumerPhase),alt=alternativeFor(group,s);if(!group||!alt){errors.push(where+' selection does not belong to planned phase');continue;}const branchAvailable=sameVersion(s,expected.get(qualifiedKey(s)))&&has(s.producerIssue,s.outputId,s.producerPhase);if(!branchAvailable)addMissing(item.number,{role:'selected-alternative',group:group.artifact,selectedAlternative:{issue:alt.issue,artifact:alt.artifact,producerPhase:alt.producerPhase},producerIssue:alt.issue,artifact:alt.artifact,producerPhase:alt.producerPhase,consumerPhase:group.consumerPhase});const bindings=s.guardBindings||[];if(bindings.length!==branchGuards(g,alt).length)eligibilityByIssue.get(item.number).push('selected branch guard bindings incomplete '+group.artifact);for(const guard of branchGuards(g,alt)){const b=bindings.find(x=>identityMatch(x,guard.issue,guard.artifact,guard.producerPhase)),available=!!(b&&sameVersion(b,expected.get(qualifiedKey(b)))&&guardConstraint(guard,b)&&has(guard.issue,guard.artifact,guard.producerPhase));if(!available)addMissing(item.number,{role:'selected-guard',group:group.artifact,selectedAlternative:{issue:alt.issue,artifact:alt.artifact,producerPhase:alt.producerPhase},producerIssue:guard.issue,artifact:guard.artifact,producerPhase:guard.producerPhase,consumerPhase:group.consumerPhase});if(guard.accessGuard&&!sourceAccessSatisfied(guard.accessGuard,f.sourceAccessEvidence,expected,has,state.observedAt,now)){const gate={id:guard.accessGuard.id,consumerPhase:group.consumerPhase,group:group.artifact,selectedAlternative:alt.artifact,manifestProviderArtifact:guard.accessGuard.manifestProviderArtifact,operations:guard.accessGuard.operations.map(x=>({path:x.path,operation:x.operation})),nativeIssueEdge:false};sourceGateByIssue.get(item.number).push(gate);eligibilityByIssue.get(item.number).push('unresolved selected source access '+guard.accessGuard.id);}}for(const b of bindings)if(!branchGuards(g,alt).some(guard=>identityMatch(b,guard.issue,guard.artifact,guard.producerPhase)))errors.push(where+' extra selected guard binding');}
   for(const group of currentGroups)if(!f.selectedInputs.some(s=>s.group===group.artifact&&s.consumerPhase===group.consumerPhase)){unresolvedByIssue.get(item.number).push(group.artifact);eligibilityByIssue.get(item.number).push('unresolved OR selection '+group.artifact);}const activeSourceGuards=[...currentProjection.hard.filter(x=>x.accessGuard).map(x=>x.accessGuard)];for(const selected of f.selectedInputs.filter(x=>x.consumerPhase===f.plannedPhase)){const group=currentGroups.find(x=>x.artifact===selected.group),alt=alternativeFor(group,selected);if(alt)activeSourceGuards.push(...branchGuards(g,alt).filter(x=>x.accessGuard).map(x=>x.accessGuard));}for(const proof of f.sourceAccessEvidence)if(!activeSourceGuards.some(x=>x.id===proof.guardId&&x.consumerPhase===proof.consumerPhase))errors.push(where+' inactive or unknown source access evidence '+proof.guardId);for(const proof of f.predicateEvidence)if(!item.guardPredicates.some(p=>p.id===proof.id&&p.consumerPhase===f.plannedPhase))errors.push(where+' inactive or unknown predicate evidence '+proof.id);for(const predicate of item.guardPredicates.filter(p=>p.consumerPhase===f.plannedPhase))if(!predicateSatisfied(predicate,f.predicateEvidence,state.sourceRef,state.observedAt,now)){const gate={id:predicate.id,phase:f.plannedPhase,kind:predicate.kind,state:'unverified',required:predicate.required,nativeIssueEdge:false};predicateGateByIssue.get(item.number).push(gate);eligibilityByIssue.get(item.number).push('unresolved non-artifact predicate '+predicate.id);}
  }
  const adjacency=new Map(activeNumbers.map(x=>[x,[...nativeByIssue.get(x)]]));if(issueCycle(adjacency))errors.push('native phase-frontier blocker projection cycle');
  const f=byIssue.get(n),targetReasons=[...errors,...(eligibilityByIssue.get(n)||[])],canProjectCurrent=current&&!errors.length&&f?.plannedPhase!==null,nativeBlockedBy=canProjectCurrent?[...nativeByIssue.get(n)].sort((a,b)=>a-b):null,unresolvedSelections=unresolvedByIssue.get(n)||[],missingInputs=missingByIssue.get(n)||[],sourceAccessGates=sourceGateByIssue.get(n)||[],predicateGates=predicateGateByIssue.get(n)||[],nonNativeArtifactGates=nonNativeByIssue.get(n)||[];
  const artifactEligible=canProjectCurrent&&!missingInputs.length&&!unresolvedSelections.length,preparationAvailable=canProjectCurrent?!(preparationByIssue.get(n)||[]).length:null,nextPhaseEligible=canProjectCurrent?artifactEligible&&!sourceAccessGates.length&&!predicateGates.length&&!(eligibilityByIssue.get(n)||[]).length:null;return {valid:!errors.length,reasons:[...new Set(targetReasons)],snapshotAsOf:text(state.observedAt)?state.observedAt:null,plannedPhase:f?.plannedPhase??null,capturedStatus:f?.capturedStatus??null,freshness,nativeBlockedBy,artifactEligible,preparationAvailable,nextPhaseEligible,unresolvedSelections,missingInputs,sourceAccessGates,predicateGates,nonNativeArtifactGates,startPermission:false,scope};
 }catch{return invalid(['malformed projection state: failed closed']);}
}

const stable=x=>Array.isArray(x)?'['+x.map(stable).join(',')+']':obj(x)?'{'+Object.keys(x).sort().map(k=>JSON.stringify(k)+':'+stable(x[k])).join(',')+'}':JSON.stringify(x);
const TOPOLOGY_DERIVATION_CONTRACT={version:1,canonicalAuthorities:['issues[].hardPredecessors (including accessGuard)','issues[].oneOfInputs[].alternatives (including additionalInputs/accessGuard)','issues[].externalInputs','issues[].guardPredicates','issues[].contextOnlyInputs (non-executable context only)','artifactRegistry records excluding consumers/contextConsumers','issues[].phaseWriteScopes','baseline/project1-items.json + baseline/captured-at.txt','reviews/historical-artifact-source-note.md + frozen #69 receipt bytes/ref'],derivedOnly:['issues[].candidateInputs','issues[].acceptanceInputs','issues[].phaseStates','issues[].successors','issues[].nativeBlockers','issues[].nativeBlockersByPhase','issues[].nativeBlockerProjection','issues[].orGateProjection','issues[].predicateGateProjection','issues[].sourceAccessPredicates','issues[].authoredInputResolutions','issues[].artifactOutputBindings (phase outputs plus typed read-only snapshots)','artifactRegistry[].consumers','artifactRegistry[].contextConsumers','producedArtifacts','derivedSuccessors','phaseGraph','nativeProjection'],rule:'Derived fields are replace-only compiler output. Exact-set mismatch is an error; no manually patched cache has precedence.'};
const topoPhaseRank=x=>({prepare:0,candidate:1,accept:2}[x]??99);
const topoSortStrings=a=>[...new Set(a)].sort((x,y)=>x.localeCompare(y));
function topoDedupe(a,key){const seen=new Set(),out=[];for(const x of a){const k=key(x);if(!seen.has(k)){seen.add(k);out.push(x);}}return out;}
const topoQualified=a=>a?.satisfied===true||a?.state==='qualified';
function topoOrGate(group,registry){const selectedId=group.selection?.artifact??null,alternativeArtifacts=group.alternatives.map(x=>x.artifact);if(!selectedId)return {group:group.id,phase:group.consumerPhase,status:'selection_required',selectedArtifact:null,alternativeArtifacts,rule:'No producer issue edge is emitted until explicit selection; alternatives are not AND-ed.'};const alternative=group.alternatives.find(x=>x.artifact===selectedId);if(!alternative)return {group:group.id,phase:group.consumerPhase,status:'invalid_selection',selectedArtifact:selectedId,alternativeArtifacts,rule:'Selected artifact is not an alternative; no native edge is publishable.'};const bindingComplete=Boolean(group.selection?.outputDigest&&group.selection?.scopeDigest&&group.selection?.sourceRef);return {group:group.id,phase:group.consumerPhase,status:topoQualified(registry.get(selectedId))&&bindingComplete?'selected_eligible':'selected_unqualified_or_unbound',selectedArtifact:selectedId,alternativeArtifacts,bindingComplete,rule:'Only the selected artifact and its additional guards project; stale/unbound selection is ineligible.'};}
function topoArtifactGate(artifactId,phase,reason,issueSet,registry,metadata={}){const artifact=registry.get(artifactId);if(!artifact)throw Error('missing artifact '+artifactId);if(topoQualified(artifact))return {blocker:null,nonNativeGate:null};const native=Number.isInteger(artifact.producerIssue)&&issueSet.has(artifact.producerIssue)&&!artifact.id.startsWith('external:')&&!artifact.id.startsWith('context-input:');if(!native)return {blocker:null,nonNativeGate:{artifact:artifactId,consumerPhase:phase,reason,state:artifact.state,producerIssue:artifact.producerIssue,kind:artifact.role??(artifact.id.startsWith('external:')?'external_qualification':'historical_snapshot_requalification'),nativeIssueEdge:false,...metadata}};return {blocker:{issue:artifact.producerIssue,artifact:artifactId,producerPhase:artifact.producerPhase,consumerPhase:phase,reason,qualified:false,direct:true,...metadata},nonNativeGate:null};}
function topoPhaseProjection(topology,issue,phase,registry,issueSet){const blockers=[],orGates=[],nonNativeArtifactGates=[],predicateGates=[];const add=x=>{if(x.blocker)blockers.push(x.blocker);if(x.nonNativeGate)nonNativeArtifactGates.push(x.nonNativeGate);};for(const edge of issue.hardPredecessors.filter(x=>x.consumerPhase===phase))add(topoArtifactGate(edge.artifact,phase,edge.reason,issueSet,registry,edge.accessGuard?{guardId:edge.accessGuard.id,selectedPaths:edge.accessGuard.operations.map(x=>x.path)}:{}));for(const group of issue.oneOfInputs.filter(x=>x.consumerPhase===phase)){const gate=topoOrGate(group,registry);orGates.push(gate);if(!gate.selectedArtifact||gate.status==='invalid_selection')continue;const alt=group.alternatives.find(x=>x.artifact===gate.selectedArtifact);add(topoArtifactGate(alt.artifact,phase,group.qualification,issueSet,registry,{orGroup:group.id,selectedAlternative:alt.artifact}));for(const guard of alt.additionalInputs||[]){const guardPhase=guard.consumerPhase??group.consumerPhase;if(guardPhase!==phase)continue;add(topoArtifactGate(guard.artifact,phase,guard.reason??('Branch guard for '+alt.artifact),issueSet,registry,{orGroup:group.id,selectedAlternative:alt.artifact,branchGuard:true,...(guard.accessGuard?{guardId:guard.accessGuard.id,selectedPaths:guard.accessGuard.operations.map(x=>x.path)}:{})}));}}for(const external of (issue.externalInputs||[]).filter(x=>x.consumerPhase===phase))add(topoArtifactGate(external.artifact,phase,external.qualification,issueSet,registry,{external:true}));for(const p of (issue.guardPredicates||[]).filter(x=>x.consumerPhase===phase&&x.state!=='satisfied'))predicateGates.push({id:p.id,phase,kind:p.kind,state:p.state,required:p.required,nativeIssueEdge:false});return {blockers:topoDedupe(blockers,x=>[x.issue,x.artifact,x.consumerPhase,x.orGroup??'',x.selectedAlternative??'',x.branchGuard??false,x.guardId??''].join('|')).sort((a,b)=>Number(a.issue)-Number(b.issue)||a.artifact.localeCompare(b.artifact)||String(a.guardId??'').localeCompare(String(b.guardId??''))),orGates:topoDedupe(orGates,x=>x.group).sort((a,b)=>a.group.localeCompare(b.group)),nonNativeArtifactGates:topoDedupe(nonNativeArtifactGates,x=>[x.artifact,x.consumerPhase,x.orGroup??'',x.selectedAlternative??'',x.guardId??''].join('|')).sort((a,b)=>a.artifact.localeCompare(b.artifact)),predicateGates:topoDedupe(predicateGates,x=>x.id).sort((a,b)=>a.id.localeCompare(b.id))};}
function topoSourcePredicates(issue){const out=[];for(const edge of issue.hardPredecessors)if(edge.accessGuard)out.push({...structuredClone(edge.accessGuard),dependencyArtifact:edge.artifact,edgeType:'requires'});for(const group of issue.oneOfInputs)for(const alt of group.alternatives)for(const guard of alt.additionalInputs||[])if(guard.accessGuard)out.push({...structuredClone(guard.accessGuard),dependencyArtifact:guard.artifact,edgeType:'OR_branch_guard',orGroup:group.id,selectedAlternative:alt.artifact});return topoDedupe(out,x=>[x.id,x.dependencyArtifact,x.orGroup??'',x.selectedAlternative??''].join('|')).sort((a,b)=>topoPhaseRank(a.consumerPhase)-topoPhaseRank(b.consumerPhase)||a.id.localeCompare(b.id));}
function topoScopeCovers(scopePath,artifactPath){if(scopePath===artifactPath)return true;const at=scopePath.search(/\*/);const prefix=at>=0?scopePath.slice(0,at):scopePath;return at>=0?artifactPath.startsWith(prefix):scopePath.endsWith('/')&&artifactPath.startsWith(scopePath);}
function topoOutputBindings(topology,issue){const out={candidate:[],accept:[],readOnlySnapshots:[]};for(const artifact of topology.artifactRegistry.filter(x=>x.producerIssue===issue.number)){const scopes=issue.phaseWriteScopes?.[artifact.producerPhase]??[],matching=scopes.filter(x=>topoScopeCovers(x.path,artifact.path)).sort((a,b)=>b.path.replace(/\*/g,'').length-a.path.replace(/\*/g,'').length||a.path.localeCompare(b.path)),scope=matching[0];if(!scope){const ro=(issue.readOnlyPaths||[]).find(x=>topoScopeCovers(x,artifact.path));if(artifact.proposed!==false||!ro)throw Error('output has no write/read-only scope '+artifact.id);out.readOnlySnapshots.push({artifact:artifact.id,path:artifact.path,pathKind:artifact.pathKind,bindingKind:'existing_read_only_snapshot',scopePath:ro,scopeMode:'read_only',lockId:null,canonicalWriteAllowed:false,rawReceiptPolicy:artifact.immutableReceiptIndex?.overwritePolicy??null});continue;}out[artifact.producerPhase].push({artifact:artifact.id,path:artifact.path,pathKind:artifact.pathKind,bindingKind:artifact.role==='diagnostic_context_not_runnable'?'diagnostic_context_output_not_runnable':(artifact.proposed===false?'requalifiable_phase_output':'phase_output'),scopePath:scope.path,scopeMode:scope.mode,lockId:scope.physicalBinding,canonicalWriteAllowed:scope.canonicalWriteAllowed===true,rawReceiptPolicy:artifact.immutableReceiptIndex?.overwritePolicy??null,...(artifact.immutableReceiptIndex?.receiptStorage?{receiptStorage:structuredClone(artifact.immutableReceiptIndex.receiptStorage)}:{})});}for(const p of ['candidate','accept'])out[p].sort((a,b)=>a.artifact.localeCompare(b.artifact));out.readOnlySnapshots.sort((a,b)=>a.artifact.localeCompare(b.artifact));return out;}
function topoOrder(nodes,edges){const indegree=new Map(nodes.map(x=>[x,0])),out=new Map(nodes.map(x=>[x,[]]));for(const [a,b] of edges){if(!indegree.has(a)||!indegree.has(b))throw Error('unknown native node');out.get(a).push(b);indegree.set(b,indegree.get(b)+1);}const ready=nodes.filter(x=>indegree.get(x)===0).sort(),order=[];while(ready.length){const x=ready.shift();order.push(x);for(const y of out.get(x).sort()){indegree.set(y,indegree.get(y)-1);if(indegree.get(y)===0){ready.push(y);ready.sort();}}}return {order,remaining:nodes.filter(x=>!order.includes(x)).sort()};}
const TOPOLOGY_NOTE='Derived from canonical hard inputs, OR alternatives/guards, external inputs, predicates, typed context-only snapshots, and the frozen artifact registry. Do not edit this projection manually.';
const topoConsumerKey=x=>[x.issue,x.consumerPhase,x.producerPhase??'',x.edgeType,x.group??'',x.selectedAlternative??'',x.guardId??''].join('|');
const topoSuccessorKey=x=>[x.issue,x.artifact,x.producerPhase??'',x.consumerPhase,x.conditional,x.orGroup??'',x.selectedAlternative??'',x.branchGuard??false,x.guardId??''].join('|');
function topoRelations(topology,registry){const consumers=new Map(topology.artifactRegistry.map(x=>[x.id,[]])),contexts=new Map(topology.artifactRegistry.map(x=>[x.id,[]]));const add=(id,x)=>{if(!consumers.has(id))throw Error('consumer references missing artifact '+id);consumers.get(id).push(x);};for(const issue of topology.issues){for(const edge of issue.hardPredecessors)add(edge.artifact,{issue:issue.number,consumerPhase:edge.consumerPhase,producerPhase:edge.producerPhase,edgeType:'requires',when:null,...(edge.accessGuard?{guardId:edge.accessGuard.id}:{})});for(const group of issue.oneOfInputs)for(const alt of group.alternatives){add(alt.artifact,{issue:issue.number,consumerPhase:group.consumerPhase,producerPhase:alt.producerPhase,edgeType:'selected_OR',group:group.id});for(const guard of alt.additionalInputs||[])add(guard.artifact,{issue:issue.number,consumerPhase:guard.consumerPhase??group.consumerPhase,producerPhase:registry.get(guard.artifact)?.producerPhase,edgeType:'OR_branch_guard',group:group.id,selectedAlternative:alt.artifact,...(guard.accessGuard?{guardId:guard.accessGuard.id}:{})});}for(const external of issue.externalInputs||[])add(external.artifact,{issue:issue.number,consumerPhase:external.consumerPhase,edgeType:'external_qualification'});for(const context of issue.contextOnlyInputs||[])if(context.artifact){if(!contexts.has(context.artifact))throw Error('context references missing artifact '+context.artifact);contexts.get(context.artifact).push({issue:issue.number,readWhen:context.readWhen??'prepare',kind:context.kind,executable:false,acceptanceInput:false,nativeIssueEdge:false});}}
 for(const [id,list] of consumers)consumers.set(id,topoDedupe(list,topoConsumerKey).sort((a,b)=>Number(a.issue)-Number(b.issue)||topoPhaseRank(a.consumerPhase)-topoPhaseRank(b.consumerPhase)||String(a.edgeType).localeCompare(String(b.edgeType))||String(a.group??'').localeCompare(String(b.group??''))||String(a.selectedAlternative??'').localeCompare(String(b.selectedAlternative??''))));for(const [id,list] of contexts)contexts.set(id,topoDedupe(list,x=>[x.issue,x.readWhen,x.kind].join('|')).sort((a,b)=>Number(a.issue)-Number(b.issue)||topoPhaseRank(a.readWhen)-topoPhaseRank(b.readWhen)||a.kind.localeCompare(b.kind)));
 const successors=new Map(topology.issues.map(x=>[x.number,[]]));for(const artifact of topology.artifactRegistry){if(!successors.has(artifact.producerIssue))continue;for(const consumer of consumers.get(artifact.id))if(['requires','selected_OR','OR_branch_guard'].includes(consumer.edgeType))successors.get(artifact.producerIssue).push({issue:consumer.issue,artifact:artifact.id,producerPhase:artifact.producerPhase,consumerPhase:consumer.consumerPhase,conditional:consumer.edgeType!=='requires',...(consumer.group?{orGroup:consumer.group}:{}),...(consumer.selectedAlternative?{selectedAlternative:consumer.selectedAlternative}:{}),...(consumer.edgeType==='OR_branch_guard'?{branchGuard:true}:{}),...(consumer.guardId?{guardId:consumer.guardId}:{})});}for(const [id,list] of successors)successors.set(id,topoDedupe(list,topoSuccessorKey).sort((a,b)=>Number(a.issue)-Number(b.issue)||topoPhaseRank(a.consumerPhase)-topoPhaseRank(b.consumerPhase)||a.artifact.localeCompare(b.artifact)||String(a.orGroup??'').localeCompare(String(b.orGroup??''))));return {consumers,contexts,successors};}
function topoPhaseStates(issue,candidateProjection,acceptProjection){const captured=issue.board.capturedProjectStatus.value,planned=issue.board.plannedExecutablePhaseEligibility.phase,capturedPhase=issue.board.capturedExecutionPhase,preparation=issue.board.preparationAvailable.value,candidateInputs=topoSortStrings(issue.hardPredecessors.filter(x=>x.consumerPhase==='candidate').map(x=>x.artifact)),acceptanceInputs=topoSortStrings(issue.hardPredecessors.filter(x=>x.consumerPhase==='accept').map(x=>x.artifact)),candidateExternal=topoSortStrings((issue.externalInputs||[]).filter(x=>x.consumerPhase==='candidate').map(x=>x.artifact)),acceptExternal=topoSortStrings((issue.externalInputs||[]).filter(x=>x.consumerPhase==='accept').map(x=>x.artifact)),candidatePredicates=topoSortStrings(issue.guardPredicates.filter(x=>x.consumerPhase==='candidate').map(x=>x.id)),acceptPredicates=topoSortStrings(issue.guardPredicates.filter(x=>x.consumerPhase==='accept').map(x=>x.id));return {prepare:{status:capturedPhase?'previously_started':(preparation?'bounded_preparation_available_not_execution':'not_inferred'),evidence:'No new preparation or execution completed by this topology audit.'},candidate:{status:planned==null?'not_evaluated_no_planned_phase':(capturedPhase==='candidate'?'captured_claim_liveness_not_reverified':(candidateProjection.blockers.length||candidateProjection.orGates.some(x=>x.status!=='selected_eligible')||candidateProjection.nonNativeArtifactGates.length||candidateProjection.predicateGates.length?'frozen_preclaim_requirements_unmet':'eligible_only_after_live_claim_and_resource_check')),inputs:candidateInputs,orGroups:issue.oneOfInputs.filter(x=>x.consumerPhase==='candidate').map(x=>x.id),externalInputs:candidateExternal,predicateInputs:candidatePredicates},accept:{status:planned==null?'not_evaluated_no_planned_phase':'not_verified',inputs:acceptanceInputs,orGroups:issue.oneOfInputs.filter(x=>x.consumerPhase==='accept').map(x=>x.id),externalInputs:acceptExternal,predicateInputs:acceptPredicates},capturedExecutionState:{projectStatus:captured,observedAt:issue.board.capturedProjectStatus.observedAt,sourceSnapshotCapturedAt:issue.board.capturedProjectStatus.sourceSnapshotCapturedAt,sourceIssueUpdatedAt:issue.board.capturedProjectStatus.sourceIssueUpdatedAt,phase:capturedPhase},preparationAvailable:structuredClone(issue.board.preparationAvailable),plannedExecutablePhaseEligibility:structuredClone(issue.board.plannedExecutablePhaseEligibility)};}
const COMPILER_SOURCE_KEYS=['records','annotations','effectiveRecordAmendments'];
function topologyCompilerSources(source){if(!keys(source,COMPILER_SOURCE_KEYS)||!Array.isArray(source.records)||!Array.isArray(source.annotations)||(source.effectiveRecordAmendments!==undefined&&!Array.isArray(source.effectiveRecordAmendments)))throw Error('explicit compilerSources {records,annotations,effectiveRecordAmendments?} required');const recordMap=new Map(),annotations=new Map();for(const record of source.records){if(!keys(record,['number','inputs','checks'])||!integer(record.number)||!Array.isArray(record.inputs)||(record.checks!==undefined&&!Array.isArray(record.checks))||recordMap.has(record.number))throw Error('invalid or duplicate compilerSources record');recordMap.set(record.number,{number:record.number,inputs:structuredClone(record.inputs),checks:structuredClone(record.checks??[])});}for(const entry of source.annotations){if(!keys(entry,['issue','inputId','value'])||!integer(entry.issue)||!text(entry.inputId)||!obj(entry.value))throw Error('invalid compilerSources annotation');const key=entry.issue+':'+entry.inputId;if(annotations.has(key))throw Error('duplicate compilerSources annotation');annotations.set(key,structuredClone(entry.value));}const effectiveRecordAmendments=structuredClone(source.effectiveRecordAmendments??[]);if(!unique(effectiveRecordAmendments.map(x=>x?.issueNumber))||effectiveRecordAmendments.some(x=>!amendmentEntryShape(x)))throw Error('invalid compilerSources effective-record amendment provenance');return {recordMap,annotations,effectiveRecordAmendments};}
function topoRawOrGroups(issue,input,registry){const out=[];for(const group of issue.oneOfInputs){if(group.alternatives.some(x=>x.issue===input.fromIssue)||group.alternatives.some(x=>(x.additionalInputs||[]).some(y=>registry.get(y.artifact)?.producerIssue===input.fromIssue)))out.push(group.id);}if(input.fromIssue==null&&issue.oneOfInputs.length&&/(OR|branch|selected|selection|선택)/i.test(String(input.id)+' '+String(input.qualification??'')))out.push(...issue.oneOfInputs.map(x=>x.id));return topoSortStrings(out);}
function topoAuthoredResolutions(issue,registry,compilerSources){const {recordMap,annotations}=compilerSources,record=recordMap.get(issue.number);if(!record)throw Error('missing immutable current shard #'+issue.number);return (record.inputs||[]).map(input=>{const inferNull=input.fromIssue==null&&/(unpublished|미게시|rubric|source[-_ ]?manifest)/i.test(String(input.id)+' '+String(input.qualification??'')),hard=issue.hardPredecessors.filter(x=>input.fromIssue==null?(inferNull&&x.accessGuard):x.issue===input.fromIssue).map(x=>({artifact:x.artifact,producerPhase:x.producerPhase,consumerPhase:x.consumerPhase,...(x.accessGuard?{guardId:x.accessGuard.id}:{})})),orGroups=topoRawOrGroups(issue,input,registry),matchingExternal=(issue.externalInputs||[]).filter(x=>(String(input.id)+' '+String(input.qualification??'')).toLowerCase().includes(x.artifact.split(':').at(-1).replace(/-/g,' ').toLowerCase())),contextArtifacts=topoSortStrings((issue.contextOnlyInputs||[]).filter(x=>x.artifact&&input.fromIssue!=null&&(x.issue===input.fromIssue||x.historicalSourceIssue===input.fromIssue)).map(x=>x.artifact)),matchingPredicates=issue.guardPredicates.filter(p=>(issue.number===68&&input.id==='current-progress-snapshot'&&p.id==='predicate:68:progress-snapshot')||(issue.number===144&&input.id==='independent-executor-identity'&&p.id==='predicate:144:independent-executor')||(issue.number===146&&input.id==='existing-branch'&&p.id==='predicate:146:existing-branch-diff')||(issue.number===146&&input.id==='org-owner-action'&&p.id==='predicate:146:org-owner-authorization')),annotation=structuredClone(annotations.get(issue.number+':'+input.id)||{});let qualification=input.qualification??null;if(issue.number===101&&input.id==='thermal-calibration-record'){annotation.authoredQualificationBeforeHistoricalReconciliation=qualification;qualification='Read-only preparation context for the already-existing laboratory calibration/holdout format. It is not an acceptance input; active #122/#123 receipts satisfy the aggregate.';annotation.classification='absorbed_historical_context_only';}if(issue.number===123&&input.id==='thermal-baseline'){annotation.authoredQualificationBeforeHistoricalReconciliation=qualification;qualification='Active #122 bounded thermal candidate with matching cell/opening/forcing scope. #122 final closure is not required; absorbed #124 laboratory history is preparation context only.';annotation.classification='active_transfer_replaces_obsolete_historical_OR';}if(issue.number===146&&input.id==='existing-branch'){for(const k of Object.keys(annotation))delete annotation[k];annotation.classification='external_branch_snapshot_qualification';}if(issue.number===146&&input.id==='org-owner-action'){for(const k of Object.keys(annotation))delete annotation[k];annotation.classification='external_authorization';}if(issue.number===68&&input.id==='current-progress-snapshot')annotation.classification='frozen_or_requeried_snapshot_qualification';if(issue.number===144&&input.id==='independent-executor-identity')annotation.classification='external_identity_predicate';if(issue.number===10&&input.id==='pm-current-scope-decision'){annotation.externalArtifact='artifact:10:sc01-explicit-method-decision:accept';annotation.classification='split_method_authority_only_not_native_input';}const externalArtifacts=topoSortStrings(matchingExternal.map(x=>x.artifact).concat(annotation.externalArtifact?[annotation.externalArtifact]:[])),predicateIds=topoSortStrings(matchingPredicates.map(x=>x.id)),mapped=hard.length||orGroups.length||externalArtifacts.length||predicateIds.length;return {originalId:input.id,originalProducer:input.fromIssue??null,qualification,resolution:topoDedupe(hard,x=>[x.artifact,x.producerPhase,x.consumerPhase,x.guardId??''].join('|')).sort((a,b)=>topoPhaseRank(a.consumerPhase)-topoPhaseRank(b.consumerPhase)||a.artifact.localeCompare(b.artifact)),orGroups,externalArtifacts,predicateIds,contextArtifacts,contextOnly:!mapped,note:TOPOLOGY_NOTE,...annotation};});}

function topologyCompilerValidation(topology,compilerSourceDependency){const errors=[],bad=x=>errors.push(x);try{if(!obj(topology)||!Array.isArray(topology.issues)||!Array.isArray(topology.artifactRegistry))return {valid:false,errors:['malformed compiled topology']};const compilerSources=topologyCompilerSources(compilerSourceDependency);if(stable(topology.effectiveRecordAmendments??[])!==stable(compilerSources.effectiveRecordAmendments))bad('effective-record amendment provenance missing or drifted');if(stable(topology.derivationContract)!==stable(TOPOLOGY_DERIVATION_CONTRACT))bad('derivationContract missing, unknown, or drifted');if(!reviewedResourceModelShape(topology.resourceModel))bad('reviewed physical resource model missing or drifted');const registry=new Map();for(const artifact of topology.artifactRegistry){if(!text(artifact.id)||registry.has(artifact.id))bad('artifact registry duplicate/invalid id');registry.set(artifact.id,artifact);}const issueSet=new Set(topology.issues.map(x=>x.number)),phaseByIssue=new Map(),relations=topoRelations(topology,registry);for(const artifact of topology.artifactRegistry){if(stable(artifact.consumers)!==stable(relations.consumers.get(artifact.id)))bad('stale executable consumers '+artifact.id);const expectedContext=relations.contexts.get(artifact.id);if(expectedContext.length?(stable(artifact.contextConsumers)!==stable(expectedContext)):Object.hasOwn(artifact,'contextConsumers'))bad('stale/separated context consumers '+artifact.id);}for(const issue of topology.issues){if(!integer(issue.number)||!Array.isArray(issue.hardPredecessors)||!Array.isArray(issue.oneOfInputs)||!Array.isArray(issue.guardPredicates))bad('malformed canonical issue '+issue.number);if(issue.relationshipDeclarations!==undefined&&!relationshipDeclarationsShape(issue.relationshipDeclarations,issue.number))bad('invalid relationshipDeclarations #'+issue.number);if(issue.inputQualifiers!==undefined&&!topologyInputQualifiersShape(issue.inputQualifiers,issue,compilerSources.recordMap.get(issue.number),registry))bad('invalid inputQualifiers #'+issue.number);if(issue.checkEvidenceMappings!==undefined&&!topologyCheckEvidenceMappingsShape(issue.checkEvidenceMappings,issue,compilerSources.recordMap.get(issue.number),registry))bad('invalid checkEvidenceMappings #'+issue.number);if(!phaseWriteScopesShape(issue.phaseWriteScopes))bad('invalid or widened phaseWriteScopes #'+issue.number);if(!resourceContractsShape(issue))bad('unsupported or mismapped normative resource contract #'+issue.number);if(!artifactOutputBindingsShape(issue.artifactOutputBindings))bad('invalid artifactOutputBindings shape #'+issue.number);for(const edge of issue.hardPredecessors||[]){const artifact=registry.get(edge.artifact);if(!artifact||artifact.producerIssue!==edge.issue||artifact.producerPhase!==edge.producerPhase)bad('hard producer identity mismatch #'+issue.number);if(edge.accessGuard!==undefined&&(!accessGuardContext(edge.accessGuard,{consumerPhase:edge.consumerPhase,providerArtifact:edge.artifact,direct:true})||!edge.accessGuard.operations.every(x=>x.observedAt===topology.sourceSnapshot)))bad('invalid/unknown direct source access guard #'+issue.number);}for(const group of issue.oneOfInputs||[])for(const alt of group.alternatives||[]){const artifact=registry.get(alt.artifact);if(!artifact||artifact.producerIssue!==alt.issue||artifact.producerPhase!==alt.producerPhase)bad('OR producer identity mismatch #'+issue.number);for(const guard of alt.additionalInputs||[]){const provider=registry.get(guard.artifact);if(!keys(guard,['artifact','consumerPhase','reason','accessGuard'])||!provider||guard.consumerPhase!==group.consumerPhase||(guard.reason!==undefined&&!text(guard.reason)))bad('invalid/unknown OR branch guard #'+issue.number);if(guard.accessGuard!==undefined&&(!accessGuardContext(guard.accessGuard,{consumerPhase:group.consumerPhase,providerArtifact:guard.artifact,group:group.id,selectedArtifact:alt.artifact,direct:false})||!guard.accessGuard.operations.every(x=>x.observedAt===topology.sourceSnapshot)))bad('invalid/unknown branch source access guard #'+issue.number);}}if(!(issue.guardPredicates||[]).every(guardPredicateShape)||!unique(issue.guardPredicates.map(x=>x.id)))bad('invalid/unknown non-artifact predicate #'+issue.number);const expectedSource=topoSourcePredicates(issue);if(stable(issue.sourceAccessPredicates)!==stable(expectedSource))bad('stale sourceAccessPredicates #'+issue.number);const candidate=topoSortStrings(issue.hardPredecessors.filter(x=>x.consumerPhase==='candidate').map(x=>x.artifact)),accept=topoSortStrings(issue.hardPredecessors.filter(x=>x.consumerPhase==='accept').map(x=>x.artifact));if(stable(issue.candidateInputs)!==stable(candidate)||stable(issue.acceptanceInputs)!==stable(accept))bad('stale direct input cache #'+issue.number);const output=topoOutputBindings(topology,issue);if(stable(issue.artifactOutputBindings)!==stable(output))bad('stale or unsafe artifactOutputBindings #'+issue.number);if((issue.artifactOutputBindings?.readOnlySnapshots||[]).some(x=>x.canonicalWriteAllowed!==false||x.scopeMode!=='read_only'||x.lockId!==null))bad('read-only snapshot grants write authority #'+issue.number);const captured=issue.board?.capturedProjectStatus,snapshot=topology.sourceSnapshot;if(!keys(captured,['value','observedAt','sourceSnapshotCapturedAt','sourceIssueUpdatedAt','source'])||captured.sourceSnapshotCapturedAt!==snapshot||!Number.isFinite(Date.parse(captured.sourceIssueUpdatedAt))||Date.parse(captured.sourceIssueUpdatedAt)>Date.parse(captured.sourceSnapshotCapturedAt)||!text(captured.source)||(captured.value===null?(captured.observedAt!==null||issue.board.status!==null||issue.board.previouslyCapturedStatus!==null):(captured.observedAt!==snapshot||captured.value!==issue.board.status||captured.value!==issue.board.previouslyCapturedStatus)))bad('captured Project status provenance invalid #'+issue.number);if(issue.number===146&&(captured?.value!==null||issue.board.plannedExecutablePhase!==null||issue.board.plannedExecutablePhaseEligibility?.phase!==null||issue.board.preparationAvailable?.value!==false))bad('#146 state inferred from absent Project row');const candidateProjection=topoPhaseProjection(topology,issue,'candidate',registry,issueSet),acceptProjection=topoPhaseProjection(topology,issue,'accept',registry,issueSet);phaseByIssue.set(issue.number,{candidate:candidateProjection,accept:acceptProjection});if(stable(issue.orGateProjection)!==stable({candidate:candidateProjection.orGates,accept:acceptProjection.orGates}))bad('stale OR gate projection #'+issue.number);if(stable(issue.predicateGateProjection)!==stable({candidate:candidateProjection.predicateGates,accept:acceptProjection.predicateGates}))bad('stale predicate gate projection #'+issue.number);if(stable(issue.nonNativeArtifactGatesByPhase)!==stable({candidate:candidateProjection.nonNativeArtifactGates,accept:acceptProjection.nonNativeArtifactGates}))bad('stale non-native gate projection #'+issue.number);if(stable(issue.nativeBlockersByPhase)!==stable({candidate:candidateProjection.blockers,accept:acceptProjection.blockers}))bad('stale native blocker projection #'+issue.number);const planned=issue.board?.plannedExecutablePhaseEligibility?.phase??null,plannedProjection=planned==='accept'?acceptProjection:planned==='candidate'?candidateProjection:{blockers:[],orGates:[],nonNativeArtifactGates:[],predicateGates:[]};if(stable(issue.nativeBlockers)!==stable(plannedProjection.blockers))bad('stale planned native blockers #'+issue.number);const expectedPlanned={computedFromSnapshotAt:snapshot,liveStateClaim:false,phase:planned,preClaimVisibility:true,blockers:structuredClone(plannedProjection.blockers),orGates:structuredClone(plannedProjection.orGates),nonNativeArtifactGates:structuredClone(plannedProjection.nonNativeArtifactGates),predicateGates:structuredClone(plannedProjection.predicateGates),futureAcceptanceInputs:planned==='candidate'?structuredClone(acceptProjection.blockers):[],rule:topology.projectionRules.nativeBlockedBy};if(stable(issue.nativeBlockerProjection)!==stable(expectedPlanned))bad('stale planned native projection #'+issue.number);const expectedOrInputs=issue.oneOfInputs.map(x=>({id:x.id,phase:x.consumerPhase,selected:x.selection?.artifact??null})),candidateExternal=topoSortStrings((issue.externalInputs||[]).filter(x=>x.consumerPhase==='candidate').map(x=>x.artifact)),acceptExternal=topoSortStrings((issue.externalInputs||[]).filter(x=>x.consumerPhase==='accept').map(x=>x.artifact)),candidatePredicates=topoSortStrings(issue.guardPredicates.filter(x=>x.consumerPhase==='candidate').map(x=>x.id)),acceptPredicates=topoSortStrings(issue.guardPredicates.filter(x=>x.consumerPhase==='accept').map(x=>x.id));if(stable(issue.board.candidateInputs)!==stable(candidate)||stable(issue.board.acceptanceInputs)!==stable(accept)||stable(issue.board.orInputs)!==stable(expectedOrInputs)||stable(issue.board.externalInputs)!==stable({candidate:candidateExternal,accept:acceptExternal})||stable(issue.board.predicateInputs)!==stable({candidate:candidatePredicates,accept:acceptPredicates})||stable(issue.board.nativeBlockers)!==stable(plannedProjection.blockers)||stable(issue.board.orGates)!==stable(plannedProjection.orGates)||stable(issue.board.nonNativeArtifactGates)!==stable(plannedProjection.nonNativeArtifactGates)||stable(issue.board.predicateGates)!==stable(plannedProjection.predicateGates))bad('stale board-derived input/gate cache #'+issue.number);if(stable(issue.phaseStates)!==stable(topoPhaseStates(issue,candidateProjection,acceptProjection)))bad('stale phaseStates #'+issue.number);if(issue.statusProposal!==captured.value||issue.statusReason!==issue.board.reason)bad('stale status projection #'+issue.number);if(stable(issue.authoredInputResolutions)!==stable(topoAuthoredResolutions(issue,registry,compilerSources)))bad('stale authored input resolutions #'+issue.number);if(stable(issue.successors)!==stable(relations.successors.get(issue.number)))bad('stale successor cache #'+issue.number);}
 const expectedDerivedSuccessors=Object.fromEntries(topology.issues.map(issue=>[String(issue.number),structuredClone(relations.successors.get(issue.number))]));if(stable(topology.derivedSuccessors)!==stable(expectedDerivedSuccessors))bad('stale root derivedSuccessors');const expectedProduced=Object.fromEntries(topology.issues.map(issue=>[String(issue.number),topology.artifactRegistry.filter(x=>x.producerIssue===issue.number).map(x=>x.id)]));if(stable(topology.producedArtifacts)!==stable(expectedProduced))bad('stale producedArtifacts');const phaseNodes=topology.issues.flatMap(issue=>PHASES.map(phase=>issue.number+':'+phase)),artifactNodes=topology.artifactRegistry.map(x=>x.id),expandedNodes=[...phaseNodes,...artifactNodes],expandedSet=new Set(expandedNodes),graphEdges=topology.issues.flatMap(issue=>[[issue.number+':prepare',issue.number+':candidate'],[issue.number+':candidate',issue.number+':accept']]);for(const artifact of topology.artifactRegistry){if(issueSet.has(artifact.producerIssue))graphEdges.push([artifact.producerIssue+':'+artifact.producerPhase,artifact.id]);if(artifact.availability?.requiresAvailabilityArtifact)graphEdges.push([artifact.availability.requiresAvailabilityArtifact,artifact.id]);for(const consumer of relations.consumers.get(artifact.id))if(['requires','selected_OR','OR_branch_guard','external_qualification'].includes(consumer.edgeType))graphEdges.push([artifact.id,consumer.issue+':'+consumer.consumerPhase]);}const expandedEdges=topoDedupe(graphEdges,x=>x[0]+'|'+x[1]).sort((a,b)=>a[0].localeCompare(b[0])||a[1].localeCompare(b[1]));if(expandedEdges.some(([a,b])=>!expandedSet.has(a)||!expandedSet.has(b)))bad('phaseGraph has unknown node');const expandedOrder=topoOrder(expandedNodes,expandedEdges);if(expandedOrder.remaining.length)bad('cyclic conservative phaseGraph');const expectedPhaseGraph={internalEdges:topology.issues.flatMap(issue=>[[issue.number+':prepare',issue.number+':candidate'],[issue.number+':candidate',issue.number+':accept']]),artifactEdges:expandedEdges,topologicalOrder:expandedOrder.order,interpretation:'Round4 conservative phase/artifact union DAG. It includes all OR alternatives/branch guards only for cycle proof. It is not an all-accept native issue projection.'};if(stable(topology.phaseGraph)!==stable(expectedPhaseGraph))bad('stale phaseGraph');
 const frozen=[],issueEdges=[],orGates=[],nonIssue=[];for(const issue of topology.issues){const phase=issue.board?.plannedExecutablePhaseEligibility?.phase;if(!phase)continue;const p=phaseByIssue.get(issue.number)?.[phase];if(!p){bad('invalid planned phase #'+issue.number);continue;}frozen.push({issue:issue.number,phase,capturedExecutionPhase:issue.board.capturedExecutionPhase,statusAtCapture:issue.board.status,blockerArtifacts:p.blockers.map(x=>x.artifact),orGroups:p.orGates.map(x=>x.group),nonNativeArtifacts:p.nonNativeArtifactGates.map(x=>x.artifact),predicates:p.predicateGates.map(x=>x.id)});for(const x of p.blockers)issueEdges.push([x.issue,issue.number,x.artifact]);for(const x of p.orGates)orGates.push({issue:issue.number,...x});for(const x of p.nonNativeArtifactGates)nonIssue.push({issue:issue.number,...x});for(const x of p.predicateGates)nonIssue.push({issue:issue.number,...x});}const uniqueEdges=topoDedupe(issueEdges,x=>x.join('|')).sort((a,b)=>Number(a[0])-Number(b[0])||Number(a[1])-Number(b[1])||a[2].localeCompare(b[2])),pairs=topoDedupe(uniqueEdges.map(([a,b])=>[String(a),String(b)]),x=>x.join('|')),order=topoOrder(topology.issues.map(x=>String(x.number)),pairs);if(order.remaining.length)bad('cyclic native projection');const expectedNative={kind:'frozen_evidence_filtered_direct_frontier',sourceSnapshotCapturedAt:topology.sourceSnapshot,liveStateClaim:false,frontier:frozen.sort((a,b)=>a.issue-b.issue),issueEdges:uniqueEdges,unresolvedOrGates:orGates.sort((a,b)=>a.issue-b.issue||a.group.localeCompare(b.group)),nonNativeArtifactAndPredicateGates:nonIssue.sort((a,b)=>a.issue-b.issue||String(a.artifact??a.id).localeCompare(String(b.artifact??b.id))),collapsedIssueDag:{nodes:topology.issues.length,edges:pairs.length,acyclic:true,topologicalOrder:order.order},excludedProjection:'No hypothetical all-accept collapsed union is generated or publishable.'};if(stable(topology.nativeProjection)!==stable(expectedNative))bad('nativeProjection is not the exact canonical frozen frontier');
 const h69=(topology.externalHistoricalRefs||[]).find(x=>x.number===69),a69=registry.get(h69?.acceptedArtifact);if(!h69||h69.state!=='CLOSED'||h69.stateReason!=='COMPLETED'||h69.scheduled!==false||h69.executable!==false||!sha(h69.sourceRef)||!sha(h69.testedInputCodeRef)||!sha(h69.reportPublicationRef)||!digest(h69.reportByteSha256)||h69.sourceRef!==h69.reportPublicationRef||h69.testedInputCodeRef===h69.reportPublicationRef||!a69||a69.producerIssue!==69||a69.producerPhase!=='accept'||a69.sourceRef!==h69.reportPublicationRef||a69.outputDigest!==h69.reportByteSha256||!keys(a69.historicalSnapshot,['readOnly','executable','currentOutput','nativeIssueEdge','sourceIssue','testedInputCodeRef','reportPublicationRef','reportByteSha256'])||a69.historicalSnapshot?.readOnly!==true||a69.historicalSnapshot?.executable!==false||a69.historicalSnapshot?.currentOutput!==false||a69.historicalSnapshot?.nativeIssueEdge!==false||a69.historicalSnapshot?.sourceIssue!==69||a69.historicalSnapshot?.testedInputCodeRef!==h69.testedInputCodeRef||a69.historicalSnapshot?.reportPublicationRef!==h69.reportPublicationRef||a69.historicalSnapshot?.reportByteSha256!==h69.reportByteSha256)bad('bounded #69 historical snapshot provenance invalid');const h124=(topology.externalHistoricalRefs||[]).find(x=>x.number===124),a124=topology.artifactRegistry.filter(x=>x.historicalAttribution?.sourceIssue===124),h124Keys=['number','state','stateReason','title','role','scheduled','warning','kind','executable','closedAt','url','closureDisposition','closureCommentUrl','dataRejected','acceptedAsCompleted','existingLaboratoryScope','transferredRemainingScope'];if(!h124||!keys(h124,h124Keys)||h124.state!=='CLOSED'||h124.stateReason!=='NOT_PLANNED'||h124.role!=='external_historical_reference'||h124.scheduled!==false||!text(h124.warning)||h124.kind!=='absorbed_superseded_context'||h124.executable!==false||h124.closedAt!==ABSORBED_124_CLOSED_AT||topology.sourceSnapshot!==ABSORBED_124_OBSERVED_AT||!issueURL(h124.url,124)||h124.closureDisposition!=='absorption_transfer'||h124.closureCommentUrl!==ABSORBED_124_COMMENT||h124.dataRejected!==false||h124.acceptedAsCompleted!==false||!labScopeShape(h124.existingLaboratoryScope)||canonical(h124.existingLaboratoryScope)!==canonical({calibrationHoldoutFormatAlreadyExisted:true,stecklerCases:3,juelichCases:13,totalCases:16,region13Accepted:false,duplicateMeasurementRequired:false})||canonical(h124.transferredRemainingScope)!==canonical({fire13Region:122,crowd13Region:125})||issueSet.has(124)||a124.length!==2)bad('absorbed #124 closure/transfer provenance invalid');for(const a of a124){const at=a.historicalAttribution,v=a.availability;if(a.producerIssue!==null||a.producerPhase!=='candidate'||a.proposed!==false||a.state!=='local_unpublished_absorbed_context_unqualified'||a.satisfied!==false||a.role!=='absorbed_historical_context_not_produced'||a.historicalSourceIssue!==124||a.producerKind!=='historical_attribution_only_not_current_output'||a.wholeIssueAcceptance!==false||(a.consumers||[]).length||a.sourceRef!==undefined||a.outputDigest!==undefined||!keys(at,HISTORICAL_ATTRIBUTION_KEYS)||at.sourceIssue!==124||at.issueState!=='CLOSED'||at.issueStateReason!=='NOT_PLANNED'||at.closedAt!==h124?.closedAt||at.closureDisposition!=='absorption_transfer'||at.closureCommentUrl!==h124?.closureCommentUrl||at.dataRejected!==false||at.completedProducerClaim!==false||at.currentOutput!==false||at.legacyProducerPhaseLabel!=='candidate'||at.legacyPhaseIsExecutionClaim!==false||at.executable!==false||canonical(at.existingLaboratoryScope)!==canonical(h124?.existingLaboratoryScope)||canonical(at.transferredRemainingScope)!==canonical(h124?.transferredRemainingScope)||!keys(v,CONTEXT_AVAILABILITY_KEYS)||v.state!=='local_unpublished_context_only'||v.access!=='local_unpublished'||v.teamAccessible!==false||v.sourceRef!==null||!digest(v.capturedLocalDigest)||v.observedAt!==topology.sourceSnapshot||!text(v.requiresAvailabilityArtifact)||!Array.isArray(v.sourceEvidence)||!v.sourceEvidence.length||!v.sourceEvidence.every(text)||!Array.isArray(a.contextConsumers)||!a.contextConsumers.length||a.contextConsumers.some(c=>!keys(c,CONTEXT_CONSUMER_KEYS)||!issueSet.has(c.issue)||c.readWhen!=='prepare'||c.kind!=='absorbed_historical_snapshot_context'||c.executable!==false||c.acceptanceInput!==false||c.nativeIssueEdge!==false))bad('absorbed #124 history became a current/executable producer or lost typed attribution');}const expected124=new Map([['artifact:124:historical-calibration-record:candidate','docs/evidence/foundation/2026-09-09-fire-core.json'],['artifact:124:historical-thermal-bounded-baseline:candidate','docs/evidence/foundation/2026-09-09-simulation-components.json']]);if(a124.some(a=>expected124.get(a.id)!==a.path))bad('absorbed #124 context artifact identity/path drifted');if((topology.nativeProjection?.issueEdges||[]).some(x=>x[0]===124)||(topology.nativeProjection?.nonNativeArtifactAndPredicateGates||[]).some(x=>String(x.artifact||'').startsWith('artifact:124:')))bad('#124 leaked into native execution projection');
 }catch(e){bad('compiler validation failed closed: '+e.message);}return {valid:errors.length===0,errors};}

export function validateCompilerIR(topology,compilerSources){return topologyCompilerValidation(topology,compilerSources);}

function resolveWriterPhysical(scope,ctx){if(!phaseWriteScopeShape(scope,ctx.phase))return null;const value=scope.physicalBinding.replaceAll('{workspaceId}',ctx.workspaceId).replaceAll('{proposalRoot}',ctx.proposalRoot);return /[{}]/.test(value)?null:value;}
function writerRuntimeReasons(item,state,now){const reasons=[],ctx=state.writerContext,scopes=item.phaseWriteScopes?.[state.phase]||[];if(ctx===undefined)return ['unsupported writer operation: explicit writerContext with exact bindings and fenced leases required'];if(!writerContextShape(ctx))return ['malformed explicit writerContext'];if(ctx.issueNumber!==item.number||ctx.phase!==state.phase||ctx.observedAt!==state.observedAt)reasons.push('writer context issue/phase/observation mismatch');const registrationRequired=scopes.some(s=>s.requiresRegistration===true),registration=ctx.registration;if(registrationRequired&&!registration)reasons.push('registered bounded proposal root required');if(registration){const age=Date.parse(ctx.observedAt)-Date.parse(registration.observedAt);if(registration.issueNumber!==item.number||registration.workspaceId!==ctx.workspaceId||registration.proposalRoot!==ctx.proposalRoot||registration.owner!==state.claim?.holder||registration.baseSourceRef!==state.sourceRef||!Number.isFinite(age)||age<0||age>900000)reasons.push('writer registration root/workspace/owner/base/freshness mismatch');}if(ctx.bindings.length!==scopes.length)reasons.push('writer binding coverage must exactly match current phase scopes');if(ctx.leases.length!==scopes.length)reasons.push('writer lease coverage must exactly match current phase scopes');if(!unique(ctx.bindings.map(x=>JSON.stringify([x.phase,x.scopePath])))||!unique(ctx.leases.map(x=>JSON.stringify([x.phase,x.scopePath])))||!unique(ctx.leases.map(x=>x.fence)))reasons.push('duplicate writer binding, lease, or fence');for(const scope of scopes){const expected=resolveWriterPhysical(scope,ctx),bindings=ctx.bindings.filter(b=>b.phase===state.phase&&b.scopePath===scope.path),leases=ctx.leases.filter(l=>l.phase===state.phase&&l.scopePath===scope.path),binding=bindings[0],lease=leases[0];if(bindings.length!==1||!binding||binding.physicalBinding!==scope.physicalBinding||binding.resolvedPhysicalBinding!==expected||binding.owner!==state.claim?.holder||binding.baseSourceRef!==state.sourceRef)reasons.push('missing or drifted exact writer binding '+scope.path);if(leases.length!==1||!lease||lease.resource!==expected||lease.owner!==state.claim?.holder||lease.baseSourceRef!==state.sourceRef)reasons.push('missing or overbroad writer lease '+scope.path);else{const acquired=Date.parse(lease.acquiredAt),expires=Date.parse(lease.expiresAt),observed=Date.parse(state.observedAt);if(!Number.isFinite(acquired)||!Number.isFinite(expires)||acquired>observed||expires<=observed||expires<=now)reasons.push('writer lease acquisition/expiry invalid '+scope.path);if(!text(lease.fence))reasons.push('writer lease fence required '+scope.path);}}for(const binding of ctx.bindings)if(!scopes.some(s=>binding.phase===state.phase&&binding.scopePath===s.path))reasons.push('extra writer binding outside current phase');for(const lease of ctx.leases)if(!scopes.some(s=>lease.phase===state.phase&&lease.scopePath===s.path))reasons.push('extra writer lease outside current phase');return [...new Set(reasons)];}
const resourceScopePaths=(template,b)=>template==='workspace-files-v1'?pathSet([...b.selectedWritePaths,...b.generatedClosure]):template==='unity-editor-v1'?pathSet([...b.generatedOutputRoots,...b.projectSettingsPaths]):[];
function companionWorkspace(active,lease){const candidates=active.filter(x=>x.key==='workspace-files-v1'&&x.issue===lease.issue&&x.holder===lease.holder&&x.baseRef===lease.baseRef&&validBinding('workspace-files-v1',x.scope)&&x.scope.workspaceId===lease.scope?.workspaceId);return candidates.length===1?candidates[0].scope:null;}
function resourceRuntimeReasons(item,state,active){const reasons=[],contracts=item.resourceBindings.filter(x=>x.phases.includes(state.phase)),required=contracts.map(x=>x.template).sort(),provided=Object.keys(state.resourceBindings).sort(),claim=state.claim,workspace=state.resourceBindings['workspace-files-v1'],unityContract=contracts.find(x=>x.template==='unity-editor-v1'),unity=state.resourceBindings['unity-editor-v1'];if(canonical(required)!==canonical(provided))reasons.push('live resource bindings must exactly match current-phase reviewed contracts');if(workspace){const expectedPaths=item.phaseWriteScopes[state.phase].map(x=>x.path),expectedClosure=unityContract?[...unityContract.generatedOutputRoots,...(validBinding('unity-editor-v1',unity)?unity.projectSettingsPaths:[])]:[];if(workspace.workspaceId!==state.writerContext?.workspaceId||workspace.baseRef!==state.sourceRef||workspace.leaseOwner!==claim?.holder)reasons.push('workspace physical identity owner/base/workspace mismatch');if(!exactSet(workspace.selectedWritePaths,expectedPaths))reasons.push('workspace selectedWritePaths do not exactly map phaseWriteScopes');if(!exactSet(workspace.generatedClosure,expectedClosure))reasons.push('workspace generated closure does not exactly map reviewed generated outputs');}if(unityContract&&unity){if(!workspace||unity.workspaceId!==workspace.workspaceId||!exactList(unity.generatedOutputRoots,unityContract.generatedOutputRoots))reasons.push('Unity physical binding mismatches workspace or reviewed generated closure');}const measurement=state.resourceBindings['measurement-run-v1'];if(measurement&&measurement.leaseOwner!==claim?.holder)reasons.push('measurement lease owner mismatch');for(const lease of active.filter(x=>TEMPLATES.includes(x.key))){const scope=lease.scope,scopePaths=validBinding(lease.key,scope)?resourceScopePaths(lease.key,scope):null;if(scopePaths===null||!exactSet(lease.scopePaths,scopePaths))reasons.push('dynamic lease path scope mismatch '+lease.key);if(lease.key==='workspace-files-v1'&&(scope.leaseOwner!==lease.holder||scope.baseRef!==lease.baseRef))reasons.push('workspace lease owner/base mismatch');if(lease.key==='measurement-run-v1'&&scope.leaseOwner!==lease.holder)reasons.push('measurement lease owner mismatch');if(lease.key==='unity-editor-v1'&&!companionWorkspace(active,lease))reasons.push('Unity lease lacks exact companion workspace physical identity');}
 for(const contract of contracts){const template=contract.template,binding=state.resourceBindings[template];if(!validBinding(template,binding)){reasons.push('invalid/unbound dynamic resource '+template);continue;}const holders=active.filter(l=>l.key===template&&validBinding(template,l.scope)&&canonical(l.scope)===canonical(binding)),lease=holders[0];if(holders.length!==1||lease.issue!==item.number||lease.holder!==claim?.holder||lease.baseRef!==state.sourceRef)reasons.push('missing matching scoped lease '+template);for(const other of active.filter(x=>TEMPLATES.includes(x.key)&&(x.issue!==item.number||x.holder!==claim?.holder))){const otherWorkspace=other.key==='unity-editor-v1'?companionWorkspace(active,other):(other.key==='workspace-files-v1'?other.scope:null);if(resourceCollision(template,binding,other.key,other.scope,workspace,otherWorkspace))reasons.push('same physical dynamic resource '+template);}}
 return [...new Set(reasons)];}

export function checkStart(g,n,state,now=Date.now()){
 const valid=validateGraph(g);if(!valid.valid)return {canStart:false,reasons:['invalid graph',...valid.errors]};const item=g.items.find(i=>i.number===n),reasons=[];let nativeBlockedBy=null;const done=()=>({canStart:!reasons.length,reasons,phase:state?.phase,nativeBlockedBy,scope:'15-minute supplied snapshot only; no distributed lease acquisition or permissions'});if(!item||!obj(state))return {canStart:false,reasons:['unknown issue/malformed state']};
 try{const shapeErrors=liveShapeErrors(state);if(shapeErrors.length){reasons.push(...shapeErrors);return done();}const age=now-Date.parse(state.observedAt);if(!Number.isFinite(age)||age<0||age>900000)reasons.push('stale/future/invalid snapshot');if(state.issueNumber!==n||!issueURL(state.issueUrl,n)||!sha(state.sourceRef))reasons.push('exact issue URL/number/sourceRef required');if(state.reviewedContext!==true)reasons.push('reviewedContext must be boolean true');if(!PHASES.includes(state.phase)||(state.stage!==undefined&&state.stage!==state.phase))reasons.push('explicit live phase required');if(!((state.status==='Ready'&&state.operation==='start')||(state.status==='In progress'&&state.operation==='resume')))reasons.push('only live Ready/start or In progress/resume');const claim=state.claim;if(!obj(claim)||claim.issueNumber!==n||!text(claim.holder)||claim.sourceRef!==state.sourceRef||!text(claim.evidence)||(claim.phase!==undefined&&claim.phase!==state.phase))reasons.push('matching live claim required');if(item.kind==='goal')reasons.push('goal is not executable work');reasons.push(...writerRuntimeReasons(item,state,now));
  for(const k of ['artifactReceipts','expectedArtifacts','verifiedInputs','verifiedSources','sourceAccessEvidence','predicateEvidence','leases','phaseEvidence'])if(!Array.isArray(state[k]))reasons.push(`${k} must be array`);if(state.acceptedArtifacts!==undefined)reasons.push('use artifactReceipts with explicit available candidate vs accepted final');if(reasons.some(r=>r.endsWith('must be array')))return done();
  const all=new Map([...g.references,...g.items].map(i=>[i.number,i])),receipts=new Map(),expected=new Map();
  for(const [list,dest,isReceipt] of [[state.expectedArtifacts,expected,false],[state.artifactReceipts,receipts,true]])for(const a of list){if(!obj(a)||!integer(a.producerIssue)||!text(a.outputId)||!ARTIFACT_PHASES.includes(a.producerPhase)||!sha(a.sourceRef)||!digest(a.outputDigest)||!text(a.qualification)||!text(a.scope)||(isReceipt&&(!(a.available===true)||!text(a.evidence)||!text(a.reviewRef)||(a.producerPhase==='candidate'?a.accepted!==false:a.accepted!==true)))){reasons.push('malformed phase-qualified artifact receipt/expectation');continue;}const k=qualifiedKey(a),p=all.get(a.producerIssue),o=p?.outputs?.find(o=>o.id===a.outputId);if(dest.has(k))reasons.push('duplicate/conflicting artifact receipt');if(!o||o.producerIssue!==a.producerIssue||o.producerPhase!==a.producerPhase||o.qualification!==a.qualification||o.scope!==a.scope)reasons.push('producer/output/phase/scope not declared');if(g.references.some(r=>r.number===a.producerIssue)&&!(p.stateReason==='COMPLETED'&&a.producerPhase==='accept'&&o?.sourceRef===a.sourceRef&&o?.outputDigest===a.outputDigest))reasons.push('historical artifact lacks exact completed qualification');dest.set(k,a);}
  const has=(p,o,phase)=>sameVersion(receipts.get(artifactKey(p,o,phase)),expected.get(artifactKey(p,o,phase)));for(const [k,a] of receipts)if(!sameVersion(a,expected.get(k)))reasons.push('receipt expected version/scope mismatch');
  const projections=inputProjections(g,n),current=projections[state.phase]||{hard:[],oneOf:[],declared:[]};
  if(Number.isFinite(age)&&age>=0&&age<=900000&&PHASES.includes(state.phase))nativeBlockedBy=[...new Set(current.hard.filter(d=>g.items.some(i=>i.number===d.issue)&&!has(d.issue,d.artifact,d.producerPhase)).map(d=>d.issue))].sort((a,b)=>a-b);
  for(const d of current.hard)if(!has(d.issue,d.artifact,d.producerPhase))reasons.push(`missing ${state.phase} artifact ${d.issue}/${d.artifact}/${d.producerPhase}`);for(const d of current.hard)if(d.accessGuard&&!sourceAccessSatisfied(d.accessGuard,state.sourceAccessEvidence||[],expected,has,state.observedAt,now))reasons.push('source access guard unresolved '+d.accessGuard.id);
  const selected=Array.isArray(state.selectedInputs)?state.selectedInputs:[];if(state.selectedInputs!==undefined&&!Array.isArray(state.selectedInputs))reasons.push('selectedInputs array required');const seenSelection=new Set();for(const s of selected){if(!obj(s)||!text(s.group)||!ARTIFACT_PHASES.includes(s.consumerPhase)||s.compatible!==true||!text(s.compatibilityEvidence)||!integer(s.producerIssue)||!text(s.outputId)||!ARTIFACT_PHASES.includes(s.producerPhase)){reasons.push('malformed selected OR input');continue;}const k=JSON.stringify([s.group,s.consumerPhase,qualifiedKey(s)]);if(seenSelection.has(k))reasons.push('duplicate selected input');seenSelection.add(k);if(s.consumerPhase!==state.phase)reasons.push('selected OR input does not belong to current phase');const group=item.oneOfInputs.find(o=>o.artifact===s.group&&o.consumerPhase===s.consumerPhase),alt=alternativeFor(group,s);if(!selectionValid(g,group,s,expected,has,true))reasons.push(alt&&branchGuards(g,alt).length?'selected artifact or branch guard unavailable, incompatible or drifted':'selected artifact unavailable, incompatible or drifted');}
  for(const group of current.oneOf)if(!selected.some(s=>s.group===group.artifact&&selectionValid(g,group,s,expected,has,true)))reasons.push('no qualified selected OR input '+group.artifact);for(const s of selected.filter(s=>s.consumerPhase===state.phase)){const group=current.oneOf.find(o=>o.artifact===s.group),alt=alternativeFor(group,s);if(!alt)continue;for(const guard of branchGuards(g,alt))if(guard.accessGuard&&!sourceAccessSatisfied(guard.accessGuard,state.sourceAccessEvidence||[],expected,has,state.observedAt,now))reasons.push('selected source access guard unresolved '+guard.accessGuard.id);}const activeSourceGuards=[...current.hard.filter(x=>x.accessGuard).map(x=>x.accessGuard)];for(const selectedInput of selected.filter(x=>x.consumerPhase===state.phase)){const group=current.oneOf.find(x=>x.artifact===selectedInput.group),alt=alternativeFor(group,selectedInput);if(alt)activeSourceGuards.push(...branchGuards(g,alt).filter(x=>x.accessGuard).map(x=>x.accessGuard));}for(const proof of state.sourceAccessEvidence||[])if(!activeSourceGuards.some(x=>x.id===proof.guardId&&x.consumerPhase===proof.consumerPhase))reasons.push('inactive or unknown source access evidence '+proof.guardId);
  if(nativeBlockedBy!==null){const blockers=new Set(nativeBlockedBy);for(const s of selected.filter(s=>s.consumerPhase===state.phase)){const group=current.oneOf.find(o=>o.artifact===s.group),alt=alternativeFor(group,s);if(!alt)continue;if(g.items.some(i=>i.number===alt.issue)&&!(sameVersion(s,expected.get(qualifiedKey(s)))&&has(alt.issue,alt.artifact,alt.producerPhase)))blockers.add(alt.issue);for(const guard of branchGuards(g,alt)){const b=(s.guardBindings||[]).find(x=>identityMatch(x,guard.issue,guard.artifact,guard.producerPhase));if(g.items.some(i=>i.number===guard.issue)&&!(b&&sameVersion(b,expected.get(qualifiedKey(b)))&&guardConstraint(guard,b)&&has(guard.issue,guard.artifact,guard.producerPhase)))blockers.add(guard.issue);}}nativeBlockedBy=[...blockers].sort((a,b)=>a-b);}
  const predicateEvidence=state.predicateEvidence||[];for(const proof of predicateEvidence)if(!item.guardPredicates.some(p=>p.id===proof.id&&p.consumerPhase===state.phase))reasons.push('inactive or unknown predicate evidence '+proof.id);for(const predicate of item.guardPredicates.filter(p=>p.consumerPhase===state.phase))if(!predicateSatisfied(predicate,predicateEvidence,state.sourceRef,state.observedAt,now))reasons.push('unresolved non-artifact predicate '+predicate.id);
    const verified=new Map();for(const v of state.verifiedInputs){if(!obj(v)||!text(v.id)||!(v.fromIssue===null||integer(v.fromIssue))||!text(v.qualification)||v.verified!==true||!sha(v.sourceRef)||!digest(v.digest)||!text(v.evidence)){reasons.push('malformed verified input');continue;}const k=JSON.stringify([v.fromIssue,v.id]);if(verified.has(k))reasons.push('duplicate verified input');verified.set(k,v);}
  for(const x of item.inputs){if(x.fromIssue!==null){if(x.consumerPhase!==state.phase)continue;const a=receipts.get(artifactKey(x.fromIssue,x.id,x.producerPhase));if(!has(x.fromIssue,x.id,x.producerPhase)||(x.expectedSourceRef&&a?.sourceRef!==x.expectedSourceRef)||(x.expectedDigest&&a?.outputDigest!==x.expectedDigest)||(x.expectedScope&&a?.scope!==x.expectedScope))reasons.push('unqualified declared current-phase input '+x.id);}else if(PHASES.indexOf(x.consumerPhase)<=PHASES.indexOf(state.phase)){const v=verified.get(JSON.stringify([null,x.id]));if(!v||v.qualification!==x.qualification||v.sourceRef!==(x.expectedSourceRef||state.sourceRef)||(x.expectedDigest&&v.digest!==x.expectedDigest))reasons.push('unverified applicable source/manual input '+x.id);}}
  const phaseEvidence=new Map();for(const e of state.phaseEvidence){if(!obj(e)||!PHASES.includes(e.phase)||e.verified!==true||e.sourceRef!==state.sourceRef||!text(e.evidence)||!Array.isArray(e.inputBindings)||!Array.isArray(e.selectedInputs)){reasons.push('invalid prior phase evidence');continue;}if(phaseEvidence.has(e.phase))reasons.push('duplicate phase evidence');phaseEvidence.set(e.phase,e);for(const b of e.inputBindings)if(!obj(b)||!sameVersion(b,expected.get(qualifiedKey(b))))reasons.push('prior consumer evidence invalidated by input drift');}
  const requiredPriorPhases=PHASES.slice(0,Math.max(0,PHASES.indexOf(state.phase)));if([...phaseEvidence.keys()].some(phase=>!requiredPriorPhases.includes(phase)))reasons.push('inconsistent phase frontier evidence ahead of current phase');for(const phase of requiredPriorPhases){const e=phaseEvidence.get(phase);if(!e){reasons.push('missing prior phase evidence '+phase);continue;}const p=projections[phase],bindings=e.inputBindings;for(const d of [...p.hard,...p.declared.filter(x=>x.fromIssue!==null).map(x=>({issue:x.fromIssue,artifact:x.id,producerPhase:x.producerPhase}))])if(!bindings.some(b=>b.producerIssue===d.issue&&b.outputId===d.artifact&&b.producerPhase===d.producerPhase&&sameVersion(b,expected.get(qualifiedKey(b)))))reasons.push('prior phase evidence missing consumed artifact '+d.artifact);for(const group of p.oneOf)if(!e.selectedInputs.some(s=>s.group===group.artifact&&retainedSelectionValid(g,group,s,e,expected)))reasons.push('prior phase evidence missing guarded OR selection '+group.artifact);for(const s of e.selectedInputs){const group=p.oneOf.find(o=>o.artifact===s.group);if(!group||!retainedSelectionValid(g,group,s,e,expected))reasons.push('prior phase OR provenance invalid or drifted '+(s?.group||'unknown'));}reasons.push(...retainedGateErrors(g,item,phase,e,p,expected,state.sourceRef));}
  if(state.phase==='accept')for(const out of item.outputs.filter(o=>o.producerPhase==='candidate'))if(!has(item.number,out.id,'candidate'))reasons.push('inconsistent accept frontier: candidate output unavailable '+out.id);if(reasons.some(r=>/prior phase|prior consumer|accept frontier|phase frontier/.test(r)))nativeBlockedBy=null;
  const sourceMap=new Map();for(const s of state.verifiedSources){if(!obj(s)||!safePath(s.path)||!sha(s.sourceRef)||!digest(s.digest)||s.available!==true||!text(s.evidence)){reasons.push('invalid verified source');continue;}if(sourceMap.has(s.path))reasons.push('duplicate source');sourceMap.set(s.path,s);}for(const c of item.context.filter(c=>PHASES.indexOf(c.readWhen)<=PHASES.indexOf(state.phase))){if(c.sourceRef&&c.sourceRef!==state.sourceRef)reasons.push('context ref mismatch');if(c.availability==='local_unpublished'&&sourceMap.get(c.path)?.sourceRef!==state.sourceRef)reasons.push('current source unavailable '+c.path);}
  const authority=text(item.authorityBlock)?{id:item.authorityBlock,phases:['candidate','accept']}:item.authorityBlock;if(authority?.phases.includes(state.phase)&&(!Array.isArray(state.resolvedAuthorities)||!state.resolvedAuthorities.includes(authority.id)))reasons.push('unresolved authority '+authority.id);
  const active=[];for(const l of state.leases){const dynamic=obj(l)&&TEMPLATES.includes(l.key);if(!obj(l)||!text(l.key)||!integer(l.issue)||!text(l.holder)||!sha(l.baseRef)||!text(l.record)||!Number.isFinite(Date.parse(l.expiresAt))||!Array.isArray(l.scopePaths)||(!dynamic&&!l.scopePaths.length)||!l.scopePaths.every(p=>safePath(trimTerminalSlash(p),true))){reasons.push('invalid live lease');continue;}if(dynamic&&!validBinding(l.key,l.scope))reasons.push('invalid dynamic lease identity');if(Date.parse(l.expiresAt)>now)active.push(l);}if(!obj(state.resourceBindings))reasons.push('resourceBindings object required');
  for(const lock of staticLocks(item,state.phase)){const holders=active.filter(l=>l.key===lock),l=holders[0];if(holders.length!==1||l.issue!==n||l.holder!==claim?.holder||l.baseRef!==state.sourceRef)reasons.push('missing/exclusive matching lease '+lock);else for(const w of writable(item,state.phase).filter(w=>w.lock===lock))if(!l.scopePaths.some(p=>p===w.path||covers(p,w.path)))reasons.push('lease misses current write '+w.path);}
  reasons.push(...resourceRuntimeReasons(item,state,active));
  for(const l of active.filter(x=>!TEMPLATES.includes(x.key)))if(l.issue!==n||l.holder!==claim?.holder)for(const w of writable(item,state.phase))if(l.scopePaths.some(p=>overlap(trimTerminalSlash(p),trimTerminalSlash(w.path))))reasons.push('foreign canonical current-write overlap '+w.path);
  return done();
 }catch{return {canStart:false,reasons:[...reasons,'malformed state: failed closed']};}
}
function main(){const args=process.argv.slice(2),cmd=args.shift()||'validate',opt=k=>{const j=args.indexOf(k);return j<0?undefined:args[j+1];};const root=resolve(dirname(fileURLToPath(import.meta.url)),'../..'),g=JSON.parse(readFileSync(opt('--graph')||resolve(root,'docs/context/work-graph.json'),'utf8'));if(cmd==='validate-compiler-ir'){const sourcePath=opt('--compiler-sources'),compilerSources=sourcePath?JSON.parse(readFileSync(sourcePath,'utf8')):undefined,result=validateCompilerIR(g,compilerSources);console.log(JSON.stringify(result,null,2));if(!result.valid)process.exitCode=1;return;}const v=validateGraph(g);if(!v.valid){console.error(JSON.stringify(v,null,2));process.exitCode=1;return;}if(cmd==='validate'){console.log(JSON.stringify(v,null,2));return;}const n=Number(opt('--issue'));if(!integer(n))throw Error('--issue NUMBER required');if(cmd==='brief'){console.log(JSON.stringify(neighborhood(g,n,{full:args.includes('--full'),maxBytes:opt('--max-bytes')?Number(opt('--max-bytes')):12000}),null,2));return;}if(cmd==='check-start'){if(!opt('--state'))throw Error('--state required');const r=checkStart(g,n,JSON.parse(readFileSync(opt('--state'),'utf8')));console.log(JSON.stringify(r,null,2));if(!r.canStart)process.exitCode=2;return;}if(cmd==='project-eligibility'){if(!opt('--state'))throw Error('--state required');const r=projectEligibility(g,n,JSON.parse(readFileSync(opt('--state'),'utf8')));console.log(JSON.stringify(r,null,2));if(!r.valid||!r.freshness.current)process.exitCode=2;return;}throw Error('Unknown command');}
if(process.argv[1]&&resolve(process.argv[1])===fileURLToPath(import.meta.url)){try{main();}catch(e){console.error(e.message);process.exitCode=1;}}
