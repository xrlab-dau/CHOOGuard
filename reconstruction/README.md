# Reconstruction service

This directory will contain the versioned FFmpeg/OpenCV → DA3-1.1 → Open3D → SceneBundle pipeline.

CI runs only synthetic fixtures. Raw and restricted captures, checkpoints, and private SceneBundles stay outside GitHub.
