[project]
name = "choo-guard-research"
version = "0.1.0"
description = "choo guard 기술 조사 하네스 (Pydantic AI Harness Researcher + Exa). 구조화된 주장·근거 URL·조회일·제한·라이선스 기록을 생성한다."
requires-python = ">=3.11"
dependencies = [
  "pydantic-ai-harness[researcher,exa,anthropic,cli]==0.29.0",
  "pydantic-ai-slim[openai]==2.40.0",
]

[tool.uv]
package = false
